apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: kantheon
  labels:
    app: postgres
    tier: local-infra
spec:
  selector:
    app: postgres
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
