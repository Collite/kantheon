capability_id: "library.listNotebooks:v1"
category: "library"
version: "1.0.0"
description: |
  Marts (notebooks) visible to the caller — the Iris notebook picker source.
service_endpoint: "http://kallimachos-mcp.kantheon.svc.cluster.local:7262"
search_tags:
  - library
  - notebooks
  - marts
  - picker
cost_hints:
  typical_latency_ms: 40
  typical_cost_usd: 0.0
  is_idempotent: true
  max_concurrent: 16
