capability_id: "library.getContext:v1"
category: "library"
version: "1.0.0"
description: |
  Graph-primary, citation-bearing RAG context for a notebook (mart). Returns cited chunks Golem/Pythia/Kleio embed in answers.
service_endpoint: "http://kallimachos-mcp.kantheon.svc.cluster.local:7262"
search_tags:
  - library
  - rag
  - getcontext
  - retrieval
  - documents
  - knowledge
cost_hints:
  typical_latency_ms: 800
  typical_cost_usd: 0.0
  is_idempotent: true
  max_concurrent: 16
