⏺ PR is up and clean.

  PR #45 — ready for review

  https://github.com/BoraPerusic/kantheon/pull/45

  - Title: feat(midas): Phase 1 — core (DB/RLS/REST/MCP) + Excel loader
  - Base: main ← Head: feat/midas-p1-s1.1-bootstrap
  - State: OPEN · MERGEABLE (main is 0 behind — clean merge)
  - 17 commits · 112 files · +8,358 / −46

  The 5 commits from this session (review hardening, S1.4 T4–T6, S1.5 T1–T7) were pushed, and the PR spans the whole Midas Phase 1 arc (Stages 1.1–1.5). The body documents what's included per stage, the review
  fixes, test coverage, and — called out explicitly so a reviewer sees them — the deliberate scoping decisions and follow-ups:

  - loader_runs is in-memory (DB-backed swap is the next step)
  - fixtures are synthetic (swap in real broker exports + genFixtures)
  - MCP refresh is synchronous (async deferred to v1.x)
  - JWKS verification + RBAC are the platform-wide deferred posture
  - the live cluster smoke is unrun (manifests build; smoke.sh syntax-checked)

  You can pick up from the main worktree at /Users/bora/Dev/kantheon. When you want to continue, the natural next moves are: run the live smoke on bp-dsk, swap the in-memory loader store for the DB-backed one, or
  start Stage 1.6 / the next arc — just say the word.


  IRIS:

  ⏺ PR is up: https://github.com/BoraPerusic/kantheon/pull/44

    #44 — feat(iris): P1 S1.4 + P2 S2.1–S2.2 — iris-bff hardening, FE extraction, envelope/v1 + BFF re-point, targeting main.

    The body is organized by stage, calls out the subtree import (so the +27k line count isn't alarming), lists the intentional Phase-3/4 degradations, records the verification (163 tests green, lint/build/type-check
    clean, bp-dsk smoke), and flags the one open item — live turn validation pending a /v2 golem behind the BFF.

    One thing to watch on review: this PR's frontend-iris CI job runs for the first time here (CI only triggers on PRs to main), so it's the real test of the envelope-ts codegen fix from commit 100ef24. If anything
    in that job is going to surface, it'll show on this PR — worth a glance at the checks before merging. Want me to keep an eye on the CI run and report back if it goes red?
