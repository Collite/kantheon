# Layer-1 routing-weights tuning log (Phase 3 Stage 3.5, T5)

> Plan reference: [`tasks-p3-s3.5-eval-ci.md`](../../../docs/implementation/v1/themis/tasks-p3-s3.5-eval-ci.md) T5.
> DONE gate: Layer-1 hit-rate ≥ 60% on the populated routing corpus.

The four-layer cascade's Layer 1 scores each candidate agent by rule weights
(`RouteToAgentNode` / `LayerWeights`, set in Stage 3.3 T4): `+0.5 / +0.4 / +0.3 /
+0.2`. This log records each tuning pass against the eval corpus so the chosen
weights are reproducible and the trade-offs are visible.

## How to run a pass

```sh
just eval-themis-routing            # against a deployed Themis (nightly/local)
# or, harness logic only (CI, no cluster):
just eval-themis-routing-selftest
```

Read the Markdown report's **Layer-1 hit-rate (non-ambiguous)** line and the
**Per bucket** table; agents that should hit Layer 1 but fall through to Layer 2
indicate an under-weighted rule signal.

## Passes

| # | Date | Weights (`exact/intent/tag/entity`) | Layer-1 hit-rate | Routing acc | Notes |
|---|---|---|---|---|---|
| 0 | 2026-06-21 | 0.5 / 0.4 / 0.3 / 0.2 (Stage 3.3 hand-tuned) | _pending corpus_ | _pending_ | Baseline weights as shipped in Stage 3.3. No live run yet: the routing corpus is the 6-line skeleton; the ≥60% gate is measured once Bora's ~180-question fill lands and the first nightly `themis-routing` run baselines actuals. |

## Status

**Open.** Awaiting (a) Bora's corpus content fill and (b) the first live
`themis-routing` nightly run. Until then `thresholds.yaml` holds the plan floors
(70 / 60 / 85) and the harness self-test guards the scoring/aggregation logic in
PR CI. When actuals land, append a pass row here, retune weights if Layer-1 < 60%,
and tighten `thresholds.yaml` to current-minus-5pp.
