package org.tatrman.kantheon.midas.loaders.excel

import io.kotest.core.spec.style.StringSpec
import io.kotest.matchers.shouldBe

/**
 * Stage 1.1 skeleton smoke test. Parser/mapper/lifecycle specs land in Stage 1.5.
 */
class SmokeSpec :
    StringSpec({
        "module wiring compiles and the test tier runs" {
            (1 + 1) shouldBe 2
        }
    })
