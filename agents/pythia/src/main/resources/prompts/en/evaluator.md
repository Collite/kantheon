Judge whether the data supports the hypothesis.

Hypothesis: {{statement}}
Data (JSON rows): {{data}}

Return ONLY a JSON object: { "verdict": "SUPPORTED" | "REFUTED" | "INCONCLUSIVE", "confidence": 0.0-1.0, "rationale": "..." }
