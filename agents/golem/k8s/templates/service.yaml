apiVersion: v1
kind: Service
metadata:
  name: {{ include "golem.fullname" . }}
  labels:
    {{- include "golem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "golem.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
