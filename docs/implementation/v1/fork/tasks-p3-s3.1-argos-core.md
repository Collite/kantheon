# Fork — Stage 3.1: Argos I — validator core + bearer roles

> Branch: `feat/fork-p3-s3.1-argos-core`. Pre-flight: Phase 1; Stage 2.1 (Ariadne — the validator reads the model graph). Plan: [`plan.md`](./plan.md) Stage 3.1. Tracker: [`tasks.md`](./tasks.md). Security context: [`../../../architecture/fork/contracts.md`](../../../architecture/fork/contracts.md) §3, [`../../../architecture/kantheon-security.md`](../../../architecture/kantheon-security.md).
>
> Source: `services/validator` (Kotlin/Ktor; RLS predicate injection, TopN enforcement, column rules, LLM-as-judge guard; calls sql-security for policy evaluation — that call becomes in-process in Stage 3.2). Proto forks here: `cz.dfpartner.validator.v1` → `org.tatrman.argos.v1`, `ValidatorService` → `ArgosService`. Ports: 7285/7286.
>
> **Pre-flight re-validation (2026-06-14, Bora) — Stage 3.1 is a MECHANICAL fork; the bearer-roles change reslots to Stage 3.2.** The task list (written ahead of execution) put "the one deliberate behavior change (bearer roles) lives in this stage / delete the whois client" here. Verified against `ai-platform@kantheon-fork-point` (validator unchanged since): **`services/validator` has zero whois references** — no whois client, no `whois-common` dep, no JWT parsing. Roles already arrive in `PipelineContext.auth_roles` and the validator **trusts** them (in-cluster trust model; zero-trust hop re-validation deferred — fork contracts §3, `kantheon-v1.1.md` §1). The JWT→`realm_access.roles` parse lives at the MCP edge (`tools/query-mcp/identity/IdentityResolver.kt` → forks to **theseus-mcp, Stage 3.5**); the actual **whois client lives in `infra/sql-security` (`WhoisClient.kt`)**, which folds into Argos in **Stage 3.2**. So the whois-deletion + bearer-roles policy rework **moves to Stage 3.2**; Stage 3.1 forks the validator mechanically (like Proteus 2.4) with roles sourced from context exactly as today. Argos is **not** to parse/verify the JWT itself at v1 (contracts §3).

- [x] **T1 — Proto fork (test first).**
  Extend `ForkedProtoDescriptorSpec` with `argos/v1` (red): package; imports only `org.tatrman.{plan,ariadne}.v1` + common (security/v1 shapes arrive in 3.2). Copy `cz/dfpartner/validator/v1/*.proto` → `org/tatrman/argos/v1/`; rename. `just proto`; green.
  *Done 2026-06-14: `argos.proto` → `org.tatrman.argos.v1` (`ArgosService`, RPC `Validate` kept; plan/context → `org.tatrman.plan.v1`; Rule-6 messages=99 → `org.tatrman.kantheon.common.v1.ResponseMessage`; `java_outer_classname = "ArgosProto"`). **Deviation (pre-flight):** also forked `security.proto` → `org.tatrman.security.v1` — the validator's `SecurityClient`/`SecurityApplier` import `cz.dfpartner.security.v1.*` (EvaluatePolicies*, TablePredicate, ColumnRule, SecurityServiceGrpcKt), a hard compile dep, so T2's "suite green first" can't hold without it; its metadata refs (DependencyStatus/OverallStatus) → `org.tatrman.ariadne.v1`, ResponseMessage → common. The security *service* still folds in-process at 3.2 (legacy SQL-fragment kept verbatim for now, stripped in 3.2). `ForkedProtoDescriptorSpec` + `verify-forked-proto-layout.sh` extended with both; `just proto` + descriptor spec + verify green.*

- [x] **T2 — Fork the module, suite lands first.**
  `services/validator` → `services/argos`; include; provenance; deps re-pointed (`shared/proto`, `query-translator`, in-repo libs). Package sweep (generated → `org.tatrman.argos.v1`, binding → `ArgosServiceGrpcKt`, module → `org.tatrman.kantheon.argos.*`). **The forked suite and its policy fixtures must run green before any behavior work starts** — sql-security stays mocked exactly as the original suite has them. This is the safety net for T3/T4.
  *Done 2026-06-14: copied 11 main + 8 test sources; package sweep `services.validator` → `org.tatrman.kantheon.argos` (Kotlin root per the Stage-2.1 convention lock, **not** the doc's older `org.tatrman.argos.*`); generated-symbol renames `ValidatorServiceGrpcKt`→`ArgosServiceGrpcKt`, `MetadataServiceGrpcKt`→`AriadneServiceGrpcKt` (metadata→Ariadne; message/RPC names preserved so the mapping holds), `ValidatorServiceImpl`→`ArgosServiceImpl`. Type refs: plan→`org.tatrman.plan.v1`, security→`org.tatrman.security.v1`, ResponseMessage/Severity→common, metadata domain→ariadne. `build.gradle.kts` on the proteus template + ktor-client + wiremock; `:services:argos` included. **Full suite green: 8 specs, 69 tests, 0 failures** (no whois/Testcontainers in the original; sql-security mocked via SAM lambdas, llm-gateway via WireMock). Config namespace swept `validator`→`argos`, `metadata`→`ariadne` (env `VALIDATOR_*`→`ARGOS_*`, `METADATA_*`→`ARIADNE_*`); ports HTTP 7285 / gRPC 7286.*

- [x] **T3 — Confirm bearer-only roles (no whois to delete here; reslotted to 3.2).**
  *Done 2026-06-14: repo grep confirms `services/argos` carries no `whois`/`whois-common`/JWT-parse code or config — the only `whois`/`realm_access` hits are in comments documenting the upstream trust model. Admin-bypass (DF-V02) reads `incomingContext.authRolesList`; role-from-context trust model intact (Argos does not re-verify the bearer). Whois-deletion + bearer-roles policy rework reslotted to Stage 3.2 (docs updated).*
  *Reframed 2026-06-14 (see header):* the validator already sources roles from `PipelineContext.auth_roles` (populated upstream from the JWT's `realm_access.roles` at the MCP edge) and has no whois client. So: (a) confirm via repo grep that the forked `services/argos` carries **no** `whois`/`whois-common`/JWT-parse code or config; (b) keep the role-from-context trust model intact (Argos does not re-verify the bearer — contracts §3); (c) the admin-bypass (DF-V02) check keys on `context.auth_roles` — covered in T4. **The whois-deletion + bearer-roles policy rework happens in Stage 3.2** when `infra/sql-security` (which holds `WhoisClient.kt`) folds into Argos and the policy lookup is re-pointed to the bearer roles by default (contracts §3 `roleSource = bearer`).

- [x] **T4 — RLS predicate tests green under bearer roles.**
  *Done 2026-06-14 — adjusted to the trust model (Argos reads roles from `PipelineContext.auth_roles`, not a raw JWT, so no signing-key fixtures are built; that matches contracts §3 and is the correct coverage). RLS predicate injection green (`SecurityApplierSpec` — per-table predicate wrap + AND-merge). **Admin-bypass DF-V02 covered explicitly** in `ArgosServiceImplSpec`: admin role in `auth_roles` → bypass honored (0 security applied); absent or wrong role → `security_bypass_denied` + security forced on. All green within the 69.*

- [x] **T5 — Remaining guards unchanged.**
  *Done 2026-06-14: `RuleEnforcerSpec` (TopN cap + DF-V01 column deny/mask), `LlmGuardSpec` (verdict parse + FAIL_CLOSED/FAIL_OPEN via WireMock), `StrictCoercionCheckerSpec`, `PlanWalkerSpec`, `ArgosERPassSpec`, `DependencyMonitorSpec` — all assertions untouched, green. Live judge via Prometheus deferred to the integration-test suite.*

- [x] **T6 — k8s + deploy + stage exit.**
  `k8s/{base,overlays/local}`; Jib `argos:dev`; probes green (sql-security still external-mocked — the pod's readiness must not require it before 3.2). `just test-all && just lint-all`. Check Stage 3.1 in [`tasks.md`](./tasks.md). (Tag waits for 3.2 — Argos ships whole.)
  *Done 2026-06-14: `k8s/base` (Deployment + Service, ports 7285/7286, readiness `/ready` on 7285) + `overlays/local` (`imagePullPolicy: Never`, `ARGOS_USE_FIXTURE_MODEL=true` so it's Ready standalone). **Readiness nuance (T6) implemented:** added `security.required` (default **false**) — `buildDependencyMonitor` excludes the policy-engine probe from `/ready` until the 3.2 in-process fold, so the pod doesn't block on a non-existent sql-security; Ariadne stays a hard dep. `jib to.image = argos:dev` (matches k8s base, per the Phase-2 review tag-alignment fix). Provenance README written. **Deferred-with-evidence (no live cluster/Docker):** `kubectl kustomize` base + local render clean; live deploy belongs to the integration track. `just lint-all` ✓, `just test-all` ✓ (no OOM), proto-verify ✓. Tag waits for 3.2.*

**DONE means:** Argos validates fixture plans with JWT-derived roles, fails closed without a bearer, no whois anywhere; original guard suite untouched and green.
