# Fork — Stage 4.1: Constellation wiring end-state

> Branch: `feat/fork-p4-s4.1-wiring-endstate`. Pre-flight: Phase 3 done. Plan: [`plan.md`](./plan.md) Stage 4.1. Tracker: [`tasks.md`](./tasks.md).
>
> Goal: the forked platform behaves as one constellation — registry complete, observability on, local bring-up one command, independence provable.

- [x] **T1 — Registry completeness.** *(done 2026-06-17)*
  capabilities-mcp search returns the full forked toolset: `run_query` (theseus-mcp), the four Ariadne tools, `Match` (echo-mcp), `Analyze` (kadmos-mcp). Source-controlled fixture YAMLs in `capabilities-mcp/manifests/` match the runtime registrations (diff them — fixtures cover bootstrap, runtime supersedes; both must agree on names/schemas). Add a mocked registry component test (heartbeats simulated by mocked clients) asserting the expected tool inventory. Per the testing policy (planning-conventions.md §4): mocked unit tests only; integration suite is separate — true all-pods-heartbeat verification deferred to the separate integration-test suite.
  **Done:** added **10** bootstrap tool fixtures under `tools/capabilities-mcp/src/main/resources/manifests/tools/` — `theseus.{query,compile}:v1`, `ariadne.{get_model,get_object,get_prompts,list_objects,list_queries,search}:v1`, `echo.match:v1`, `kadmos.analyze:v1` (the "four Ariadne tools" is **six** — fixtures must mirror exactly what ariadne-mcp registers). Each is a provenance-headed mirror of the owning wrapper's runtime manifest. New `ForkedToolRegistrySpec` (3 tests): (a) bootstrap fixtures cold-start the full inventory; (b) **drift guard** — each fixture parses to a `ToolCapability` proto-equal to its runtime source manifest (read directly from the sibling module, so a source edit fails until the fixture is re-synced); (c) a runtime heartbeat supersedes the fixture (same `registration_id`, now heartbeat-backed) without losing inventory. Full module suite green.

- [x] **T2 — Decommission the PoC heartbeat expectation.** *(done 2026-06-17)*
  Confirm nothing in kantheon awaits or special-cases the old ai-platform `query-mcp` heartbeat: grep capabilities-mcp fixtures/config/tests for ai-platform tool names and hostnames; remove stale fixtures. **ai-platform itself is not touched** — its heartbeat simply stops being meaningful (note in capabilities-mcp README).
  **Done:** capabilities-mcp is free of ai-platform tool ids + hostnames. Production agent fixture `golem-erp.yaml` capability_refs/preferred_capabilities `query.named:v1`/`query.compile:v1` → `theseus.query:v1`/`theseus.compile:v1` (+ `metadata-mcp`/`ai-platform metadata` comments → Ariadne). Test tree: `query.named:v1` → `theseus.query:v1`, category `query.*` → `theseus.*`, PoC hostname `query-mcp.ai-platform.svc.cluster.local:7201` → `theseus-mcp.kantheon.svc.cluster.local:7307`; PoC tool fixture `query-named-test.yaml` → `theseus-query-test.yaml`. README: added a **"Fork note"** documenting the decommissioned PoC heartbeat (ai-platform untouched, heartbeat no longer meaningful) + the bootstrap-fixture inventory; quickstart now lists tool ids. `pythia.yaml`'s `model.fit.*`/`move.materialize.*` refs are forward-looking Pythia-arc placeholders (not query-mcp names) — left to that arc. Residual `query-mcp` **persona-name drift** in *other* modules' comments + `theseus-mcp/docs/catalog-info.yaml` carried to Stage 4.2 T1/T6 (recorded there). Module suite green.

- [x] **T3 — Observability client config.** *(done 2026-06-17; depth = "orchestration spans", Bora)*
  Every forked module wires the in-repo `otel-config` (`createOpenTelemetrySdk()` — pattern: `ai-platform/EXAMPLES.md` §8) / Python `otel-config` for Kadmos + Steropes; service names follow the persona (`kantheon-theseus`, `kantheon-argos`, …). Verify span emission via a mocked component test exercising a `run_query` chain with mocked downstream clients (one trace, spans nesting theseus-mcp → Theseus → Proteus/Argos/Kyklop → Brontes); per the testing policy (planning-conventions.md §4) the live "traces land in the fabric-infra stack" e2e confirmation is deferred to the separate integration-test suite. Dashboard/alert *definitions* are fabric-infra-owned — file the list of wanted panels (per-service RED metrics + `run_query` latency) as a fabric-infra note, don't add server-side manifests here (inherited decision #8).
  **Done:** **client config** confirmed across all forked modules — Ktor/Python lanes wire the in-repo `otel-config`; **Prometheus** (Spring Boot) wires the equivalent `opentelemetry-spring-boot-starter` (OTLP + auto-instrumentation + persona `service.name`) — forcing the Ktor lib into Spring would double-init the SDK (documented as deliberate). **Service-name convention:** all modules landed with **bare persona** names (`theseus`, not `kantheon-theseus`), uniform in code + k8s — kept (unambiguous; pre-write's `kantheon-` prefix not adopted; noted so it isn't read as a defect). **Found the chain had zero manual spans** (only metrics + log bridge); per Bora's call added **orchestration spans**: a shared coroutine-safe `withSpan` + `Flow.tracedFlow` helper in `otel-config` (`asContextElement` / `flowOn` so the Flow invariant holds), a `mcp.tool.<name>` root span in theseus-mcp `InstrumentedTool`, and `theseus.run` + per-stage decorator spans (`theseus.detect_schema/parse/translate/validate/dispatch`) in `TheseusServiceImpl` (tracing decorators around the injected clients — branchy run/compile bodies untouched). **Leaf services** (Argos/Kyklop/Brontes) keep gRPC auto-instrumentation; cross-pod nesting → integration suite. New `RunQueryTracingComponentSpec` asserts one trace, root → `theseus.run` → the four stage spans via an in-memory exporter sharing one SDK across the in-process chain (+ `TracingSpec` unit test for the helper). **Fabric-infra panel/alert wishlist** + the full wiring map filed in new [`docs/architecture/fork/observability.md`](../../../architecture/fork/observability.md) (no server-side manifests in-repo). otel-config / theseus / theseus-mcp `test`+`ktlintCheck` green.

- [x] **T4 — Throughput smoke + cost hints.** *(done 2026-06-17)*
  Reference dataset (1e5 rows fixture) through Brontes and Steropes paths; record rows/s + p50/p95 into each capability manifest's `cost_hints` (the Charon/Metis idiom) and the module READMEs. Not a benchmark suite — one recorded baseline per path.
  **Done:** one recorded read-out baseline per worker (1e5-row fixture, warm-up + 20 reps, single-host). **Steropes** (`bench/bench_throughput.py`, `uv run`): Polars transform → `to_arrow` → the worker's own `_split_table`/`_serialize_record_batch` → **p50 0.5 ms / p95 0.7 ms / ~188 M rows/s**. **Brontes** (`src/bench/kotlin/.../bench/ThroughputBench.kt`, `:workers:brontes:benchThroughput` JavaExec on a `bench` source set — out of the CI gate): `ArrowIpcSerializer.serializeBatch` of a 100k-row root (2.24 MB IPC) → **p50 1.2 ms / p95 2.9 ms / ~84 M rows/s**. Both recorded in the worker READMEs with methodology + caveat. **`cost_hints` placement decision:** workers carry **no** capability manifest (they're not MCP tools), so the manifest-`cost_hints` half of the idiom has no home at the worker level; the baselines prove read-out is **not** the bottleneck (sub-3 ms/100k rows both), so end-to-end `run_query` cost hints stay on `theseus.query:v1` (which already reflects the DB-fetch-dominated 4 s) and are left unchanged — the README is the worker baseline's home. DB/source fetch excluded (integration-suite territory). Benches lint-clean (ktlint + ruff).

- [x] **T5 — One-command local bring-up.** *(done 2026-06-17)*
  From a clean K3s: `just local-infra-up` (now owns: MSSQL, WireMock-LLM, any seeds) + a new `just deploy-fork` recipe (deploys the ten forked modules + capabilities-mcp in dependency order). Document the bring-up in `deployment/local/README.md`. Acceptance: fresh K3s → both commands → T1's registry test green and the 3.6 acceptance script passes.
  **Done:** `just local-infra-up` (ns + `kubectl apply -k deployment/local` = MSSQL + seeds; waits on the `mssql` rollout) and `just deploy-fork` (builds + applies all **15** modules — capabilities-mcp → leaf services → proteus → workers → argos → kyklop → theseus → 4 MCP edges; Kotlin via `deploy-kt`, Kadmos/Steropes via `deploy-py`). **Fixed a fork gap:** `_resolve` + `deploy-kt`'s k8s find searched only `agents/tools/frontends/shared` — added `services/ workers/` so the forked Kotlin services/workers resolve by bare name (verified `theseus → :services:theseus`, `brontes → :workers:brontes`, etc.). `deployment/local/README.md` documents both commands + the dependency rationale + acceptance; stale WireMock comment in `deployment/local/kustomization.yaml` corrected (WireMock-LLM is integration-track; local runs on placeholder keys). All **15** forked `k8s/overlays/local` render; recipes parse (`just --list`). **Acceptance scoping:** the unit/component gate (T1 `ForkedToolRegistrySpec` + the 3.6 acceptance specs) is green under `just test-all`; the live on-K3s bring-up + `run_query` e2e is integration-track (planning-conventions §4) — there is no standalone "3.6 acceptance *script*" (3.6 shipped component specs, not an on-cluster script), noted in the README.

- [x] **T6 — Independence assertion + stage exit.** *(done 2026-06-17)*
  Prove zero egress to ai-platform: grep all `application.conf`/`.yaml`/k8s manifests for ai-platform hostnames (none); the full kantheon mocked unit/component suite passes with no ai-platform clients wired in (all external dependencies mocked). Per the testing policy (planning-conventions.md §4): mocked unit tests only; integration suite is separate — the "ai-platform stack down/unreachable, full e2e suite still passes" confirmation is deferred to the separate integration-test suite. Record the assertion in `docs/architecture/fork/architecture.md` §9 (flip the section's framing from end-state-future to achieved, with date). `just test-all && just lint-all`. Check Stage 4.1 in [`tasks.md`](./tasks.md).
  **Done:** **zero ai-platform egress** — the conf/yaml/k8s grep for ai-platform service hosts returns none; the 3 residual `ai-platform`/`llm-gateway` strings are provenance/persona-name **comments** (not endpoints) carried to Stage 4.2. `just test-all` **green** (full mocked suite, no ai-platform client wired) + `just lint-all` **green** + steropes `ruff` clean. **Independence assertion recorded** in `docs/architecture/fork/architecture.md` §9 — Phase 1–4 pipeline independence flipped to **ACHIEVED (2026-06-17)** with the evidence (build cut, zero egress, mocked-suite green); total independence still gated on Phase 5 (technical wave). Stage 4.1 ticked in `tasks.md`.

**DONE means:** complete registry, traced pipeline, one-command local stack, and a demonstrated cold shoulder to ai-platform. — **all met; Stage 4.1 complete.**
