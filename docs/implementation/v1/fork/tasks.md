# The Platform Fork — Task Tracker

> **Status:** opened 2026-06-12; all 14 mini-lists written 2026-06-12. Umbrella tracker for [`plan.md`](./plan.md). Per-stage mini-lists live next to this file as `tasks-p<n>-s<n.m>-<short>.md`; each holds 6–8 tasks. Phase 2–4 lists were written ahead of execution — **re-validate a stage's pre-flight block before starting it**; landed predecessors may have shifted details (the lists flag these spots explicitly, e.g. "verify, don't duplicate").
>
> **Instructions for the coding agent (read before every session):**
> 1. Work **one stage at a time**, top to bottom. Do not start a task whose predecessors are unchecked.
> 2. **Check each checkbox immediately after the task is done** — in the mini-list *and* the stage row here. Never batch checkbox updates.
> 3. TDD: where a task says "test first", the test must exist and fail before the implementation lands.
> 4. **"Tests green" ≠ done**: run `just lint-all` before claiming any task complete (ktlint failures pass tests but fail the DoD — inherited ai-platform rule).
> 5. Branch per stage: `feat/fork-p<n>-s<n.m>-<short>`. Never merge branches yourself.
> 6. Every forked module gets the provenance header in its README: `forked-from: ai-platform@<sha> (<original path>)` (template from Stage 1.1 T2).
> 7. Conventions authority: kantheon `CLAUDE.md` + `AGENTS.md` + `EXAMPLES.md`; for code patterns born in ai-platform, `ai-platform/EXAMPLES.md` (§ references are given per task).

## Phase 1 — Foundation

| Stage | Mini-list | Status |
|---|---|---|
| 1.1 Fork point & conventions | [`tasks-p1-s1.1-conventions.md`](./tasks-p1-s1.1-conventions.md) | [x] |
| 1.2 Pipeline protos | [`tasks-p1-s1.2-pipeline-protos.md`](./tasks-p1-s1.2-pipeline-protos.md) | [x] |
| 1.3 Shared libs & Maven cut | [`tasks-p1-s1.3-shared-libs.md`](./tasks-p1-s1.3-shared-libs.md) | [x] |

**Phase 1 DONE:** 7 Kotlin libs + 1 Python lib forked in-repo under `shared/libs/{kotlin,python}/<name>/` with provenance headers. `just test-all` green. `rg "cz\.dfpartner" gradle/ settings.gradle.kts build.gradle.kts` shows **only** the documented Themis-residual `cz.dfpartner:shared-proto:0.9.0` (Themis's `nlp.v1` import — deleted in Stage 2.6). Clean-clone build needs `gpr.user`/`gpr.token` **only** for that one residual artifact; the four other cz.dfpartner:.* coords (otel-config, fuzzy-common, logging-config, ktor-configurator) are no longer pulled from Maven. The contracts §6 / plan §1.3 list of "ttr-parser / ttr-writer" was aspirational — those modules do not exist as in-repo libs at `ai-platform@kantheon-fork-point`; they're now published as `org.tatrman:ttr-{parser,writer,semantics}:0.4.0` from a separate `Collite/modeler` repo and consumed as Maven coords (work belongs in Stage 2.4 Proteus). *Pre-flight re-validation, fork Day 1.3, 2026-06-13.* `just lint-all` is **green repo-wide** (review-002, 2026-06-13): the earlier note here mis-described the lint state as "inherited pre-existing ktlint failures in `tools/capabilities-mcp` (git `af5ed38`)" — in fact the failures were a repo-wide, auto-fixable `ktlint_official` formatting debt across themis, capabilities-mcp, capabilities-client, the smoke modules, and shared:proto (Kotest fluent-chain / class-signature rules), and `af5ed38` is a themis-arc commit, not capabilities-mcp. Resolved by `ktlintFormat` across all modules; tests unaffected.

## Phase 2 — Off-data-path services

| Stage | Mini-list | Status |
|---|---|---|
| 2.1 Ariadne + ariadne-mcp | [`tasks-p2-s2.1-ariadne.md`](./tasks-p2-s2.1-ariadne.md) | [x] |
| 2.2 Echo + echo-mcp | [`tasks-p2-s2.2-echo.md`](./tasks-p2-s2.2-echo.md) | [x] |
| 2.3 Kadmos + kadmos-mcp | [`tasks-p2-s2.3-kadmos.md`](./tasks-p2-s2.3-kadmos.md) | [x] |
| 2.4 Proteus | [`tasks-p2-s2.4-proteus.md`](./tasks-p2-s2.4-proteus.md) | [x] |
| 2.5 Prometheus | [`tasks-p2-s2.5-prometheus.md`](./tasks-p2-s2.5-prometheus.md) | [x] |
| 2.6 Themis switch-over (eval-gated) | [`tasks-p2-s2.6-themis-switchover.md`](./tasks-p2-s2.6-themis-switchover.md) | [x]¹ |

Stages 2.1–2.5 are mutually independent (parallelizable); 2.6 needs all five.

¹ Stage 2.6 **code parts done 2026-06-14** (proto swap, endpoint config, Maven cut, docs, repo-wide cz.dfpartner-free, `test-all`/`lint-all` green). Its **eval no-regression gate (T1/T6) is deferred to the separate integration track** per the unit-tests-only policy (Bora 2026-06-14) — **Phase 2 code exit is reached; the eval gate closes M0 from the integration side.**

**Phase 2 review pass + cleanup (2026-06-14).** A full review of the merged Phase 2 (Kadmos/Proteus/Prometheus forks + Themis switch-over) ran; findings fixed on `fix/fork-p2-deploy-cleanup`. Two MAJORs: (1) jib `<svc>:latest` vs k8s-base `<svc>:dev` tag mismatch (echo/proteus/prometheus) → aligned to `:dev`; (2) Themis k8s **base** still defaulted to `ai-platform.svc` hosts (masked locally by the overlay's full-env replace) → retargeted to `kadmos`/`echo`/`prometheus` `.kantheon.svc`. MINORs: Prometheus OTLP protocol dead branch, Kadmos `config.py` defaults realigned to `config.yaml` (incl. `morphodita.enabled`), Proteus poll-interval fallback. NIT sweep: `LLM_GTW_*`→`PROMETHEUS_*`, `llm-gateway`→`prometheus`, Kadmos persona naming/metrics, stale doc/README. **Flag closed:** root `gradle.properties` (`-Xmx4g`) added — default heap OOMed `test-all` once the Spring-inclusive Prometheus fork joined the build. Left for Bora: catalog `owner: df-platform` (repo-wide, team decision) and the capabilities-mcp `query-mcp` test fixtures (Fork Phase 4 / §7.2 scope). Verified green: `lint-all`, `test-all` (no OOM), kadmos pytest 29, proto-layout verify, kustomize renders.

## Phase 3 — The query path

| Stage | Mini-list | Status |
|---|---|---|
| 3.1 Argos I: validator core + bearer roles | [`tasks-p3-s3.1-argos-core.md`](./tasks-p3-s3.1-argos-core.md) | [x]² |
| 3.2 Argos II: the policy fold | [`tasks-p3-s3.2-argos-fold.md`](./tasks-p3-s3.2-argos-fold.md) | [x]³ |
| 3.3 Kyklop + Brontes | [`tasks-p3-s3.3-kyklop-brontes.md`](./tasks-p3-s3.3-kyklop-brontes.md) | [x]⁴ |
| 3.4 Steropes + Charon re-point | [`tasks-p3-s3.4-steropes.md`](./tasks-p3-s3.4-steropes.md) | [x]⁵ |
| 3.5 Theseus + theseus-mcp | [`tasks-p3-s3.5-theseus.md`](./tasks-p3-s3.5-theseus.md) | [x]⁶ |
| 3.6 Security review + acceptance | [`tasks-p3-s3.6-security-review.md`](./tasks-p3-s3.6-security-review.md) | [x]⁷ |

3.3 and 3.4 run in parallel after 3.2; 3.5 needs 3.1–3.4 + 2.4; 3.6 needs 3.5.

**Phase 3 closed — sign-off 2026-06-17 (Bora).** The forked query path is in-repo and reviewed: identity at the theseus-mcp edge (OBO, fail-closed), Argos RLS (per-tenant + DF-V02 admin bypass), clean typed errors with no partial results. All Phase-3 tags applied at close: `argos/v0.1.0`, `kyklop/v0.1.0`, `brontes/v0.1.0`, `steropes/v0.1.0`, `theseus/v0.1.0`, `theseus-mcp/v0.1.0`.

⁷ Stage 3.6 **done 2026-06-17** (T1–T5 on `feat/fork-p3-s3.6-security-review`, merged PR #20; T6 sign-off on `feat/fork-p3-s3.6-signoff`). **Security doc** (`kantheon-security.md`) rewritten to the in-repo owners (theseus-mcp edge + Argos RLS; fork-notice → "fork executed"). **RLS acceptance matrix** (T2, `RlsAcceptanceMatrixSpec`, 4): disjoint per-tenant row sets; DF-V02 admin bypass unfiltered; non-admin bypass refused (`security_bypass_denied`) + forced scoped. **OBO discipline** (T3, `OboDisciplineComponentSpec`, 3): valid bearer → roles reach Argos; no-token / service-identity token → fail-closed, Argos never called; no credential leak. **Token expiry** (T4, `TokenExpiryComponentSpec`, 2): trust-boundary pinned (edge does not verify `exp` — ingress's job) + clean typed error with no partial results — **found + fixed a real fail-closed defect**: `QueryTool` ignored Theseus's `errorBatch` (ERROR-severity Rule-6 message), surfacing internally-caught pipeline failures as `ok=true` with partial/zero rows and a dropped error; now fails closed on any ERROR-severity batch message. **Audit-derivability** (T5): derivability table in `iris/contracts.md` §3.1; one bounded gap (query tool echoes neither executed SQL nor exact total rows — `compile` does) filed non-blocking in `kantheon-v1.1.md` §11. Also cleaned pre-existing Metis-arc ktlint debt surfaced by `lint-all`. **+9 component tests**; `test-all`/`lint-all` green. **Phase 3 exit.**

⁶ Stage 3.5 **done 2026-06-15** (branch `feat/fork-p3-s3.5-theseus`, off `main` after the Steropes merge). **Theseus** (query orchestrator + plan cache) forked from `services/query-runner` → `services/theseus` (proto `org.tatrman.theseus.v1`/`TheseusService`; downstream config + grpc stubs re-pointed to Proteus 7276 / Argos 7286 / Kyklop 7291; service ports 7305/7306; Caffeine LRU cache). **theseus-mcp** (agent edge) forked from `tools/query-mcp` → `tools/theseus-mcp` **(from ai-platform HEAD, for the proto-compatible pattern-params improvement)**; root `org.tatrman.kantheon.theseus.mcp`; port 7307; `query`/`compile` tools (vocabulary forks as-is). **IdentityResolver forked unchanged**; the OBO fail-closed gate extracted to a unit-tested pure `IdentityGate` (kantheon-security §2/§2.1): valid bearer → `auth_roles` from `realm_access.roles`; no identity → `missing_user_identity`; **service-account token (no user claim) → rejected, never service identity**; token/arg conflict → `identity_conflict`. **Plan-cache replay** strengthened past the call-counting trap (T3): captures the dispatched plan and proves a replay serves the right entry. **Full-chain component smoke** (T6): `run_query` → real in-process Theseus → mocked Proteus/Argos/Kyklop → real Arrow → JSON rows, with OBO roles verified reaching Argos end-to-end. **Capabilities**: `theseus.query:v1`/`theseus.compile:v1` manifests + capabilities-client registration (warn-and-continue). k8s for both. **+127 tests** (Theseus 50, theseus-mcp 77). `test-all`/`lint-all`/proto-verify/kustomize all green. **Tags `theseus/v0.1.0` + `theseus-mcp/v0.1.0` ready but NOT pushed**; **not merged** (tracker rule).

⁵ Stage 3.4 **done 2026-06-15** (branch `feat/fork-p3-s3.4-steropes`, off `main` after the Charon P1 + 3.3 merges). **Steropes** (the Polars worker, the second Kyklops) forked from `workers/polars` → `workers/steropes` (Python; package `workers_steropes`; HOCON `workers-steropes`; ports HTTP 7300 / gRPC 7301; proto imports → `org.tatrman.{worker,plan}.v1` + ariadne/common; implements the generic `org.tatrman.worker.v1.WorkerService`). **Cross-engine fingerprint unified (Bora decision: full unify)** on Charon's canonical, implementation-independent algorithm — Steropes (`fingerprint.py`) AND Brontes (`ArrowIpcSerializer.fingerprintFor`, migrated off raw IPC bytes) now produce digests byte-identical to Charon's `Integrity.kt`; the shared pin `shared/testdata/fingerprints/` (reference/scalars/list/map IPC + `fingerprints.json`, `reference.arrow` = Charon's `69779ea6…` anchor) is recomputed by Steropes pytest + Brontes `SchemaFingerprintCrossEngineSpec`, incl. the entries-wrapped **map** form (the Stream B note). **Kyklop** Steropes worker slot enabled (`steropes:7301`); `KyklopSteropesDispatchComponentSpec` exercises stateful workspace routing + sticky pinning. **Charon re-pointed** (docs `architecture.md`/`contracts.md` + two accurate comments; WorkerEndpoint still stubbed → no channel-config code to swap, flagged for Charon Stage 3.1). **Local infra:** Steropes k8s base+overlay. **+108 tests** (Steropes 50, Brontes +5, Kyklop +2, + the carried suites). `test-all`/`lint-all`/`test-py steropes`/proto-verify/kustomize all green. Also fixed a main merge artifact (dangling `:workers:_smoke-worker` include re-added by the Charon merge). **Follow-up (Stream B):** Charon's `fixtures/integrity/regenerate.py` `children(map)` should adopt the entries-wrapped form (latent; logged in `kantheon-v1.1.md`). **Tag `steropes/v0.1.0` ready but NOT pushed**; **not merged** (tracker rule).

⁴ Stage 3.3 **done 2026-06-15** (branch `feat/fork-p3-s3.3-kyklop-brontes`, off `main` after the 3.1/3.2 merge). **Kyklop** (worker dispatcher) forked from `services/dispatcher` → `services/kyklop` (Kotlin root `org.tatrman.kantheon.kyklop`; `DispatcherService` → `KyklopService` / `KyklopServiceImpl`; config+env `dispatcher`/`DISPATCHER_*` → `kyklop`/`KYKLOP_*`; ports 7290/7291). **Brontes** (MSSQL worker — the first real Kyklops) forked from `workers/mssql` → `workers/brontes` (Kotlin root `org.tatrman.kantheon.brontes`; implements the generic `org.tatrman.worker.v1.WorkerService`; `translator.v1`→`proteus.v1`, `metadata.v1`→`common`/`ariadne`; ports 7295/7296; credential-free named-connection config). **Proto:** `dispatcher.proto` → `org.tatrman.kyklop.v1`/`KyklopService` (`KyklopProto`); added to `ForkedProtoDescriptorSpec` + the layout verify script. **`_smoke-worker` placeholder retired** (Phase 1 Stage 1.1 T3) — first real `workers/` occupant. **Local infra started** (kantheon-architecture §7.1): `deployment/local/mssql/` (Deployment+Service+init-SQL ConfigMap+init Job, local-only SA secret) + a new top-level `deployment/local/kustomization.yaml`. **Tests:** forked suites carried (mocked JDBC/translator — source never used Testcontainers) + `ArrowIpcFormatterContractSpec` (worker→data-formatter Arrow IPC round-trip, T5) + `KyklopBrontesDispatchComponentSpec` (Kyklop↔in-process-Brontes: Arrow pass-through fidelity + Rule-6 `no_worker_for_connection` fail-closed, T6); **+106 tests** (Kyklop 56, Brontes 50). `test-all`/`lint-all`/proto-verify/kustomize-render (kyklop+brontes overlays + deployment/local) all green. **Tags `kyklop/v0.1.0` + `brontes/v0.1.0` ready but NOT pushed** (coordinated at merge — tracker rule). **Not merged** (tracker rule).

³ Stage 3.2 **done 2026-06-14** (branch `feat/fork-p3-s3.2-argos-fold`, stacked on 3.1). **The policy fold:** `infra/sql-security`'s v1 RelNode policy engine folded **in-process** into `services/argos/.../policy/` (PolicyRegistry, PolicyConfigLoader, PolicyPredicate/Policy, PolicyToExpression/ResolvedIdentity, PolicyMetadataClient) behind a new `PolicyEngine.evaluatePolicies(req)` lifted from `SecurityServiceImpl`. **Bearer roles:** `resolveIdentity` now reads roles from `context.auth_roles` (joined into the identity's `roles` attr) — **whois lookup removed** (default `roleSource = bearer`, contracts §3). **Dropped:** the gRPC `SecurityService` layer, the legacy SQL-fragment path (`PolicyToSqlFragment`, routes, `EvaluatePoliciesLegacy`), OPA, and whois — none copied. `GrpcSecurityClient` replaced by in-process `LocalPolicyClient` (SecurityApplier/ArgosServiceImpl untouched); the readiness gate drops the sql-security probe (Ariadne-only). `security.proto` trimmed to the in-process **data contract** (service/legacy/GetStatus removed). **HOCON policy store** (T4): `resources/policies/policies.conf` (`argos.policies`, default tenant_isolation) + `ARGOS_POLICIES_FILE` external override. **Latency** (T5): `PolicyLatencySpec` asserts in-process p50 < 2 ms. **98 tests green** (4 ported policy specs + bearer-roles spec + in-process component spec + carried validator suite); `lint-all`/`test-all`/proto-verify/kustomize-render green. **Tag `argos/v0.1.0` ready but NOT pushed** (coordinated at merge — tracker rule). Phase-3 §1 contracts (in-process eval) satisfied.

² Stage 3.1 **done 2026-06-14** (branch `feat/fork-p3-s3.1-argos-core`). **Mechanical fork** — pre-flight re-validation found the bearer-roles change does not live here: the validator has no whois and trusts `PipelineContext.auth_roles` (the whois client is in `infra/sql-security`, folds at 3.2; the JWT parse is at the theseus-mcp edge, Stage 3.5). Protos forked: `org.tatrman.argos.v1` (`ArgosService`) **and** `org.tatrman.security.v1` (the policy-eval contract — a hard compile dep of Argos's `SecurityClient`; the *service* still folds in-process at 3.2). Module `services/argos` (Kotlin root `org.tatrman.kantheon.argos`, per the Stage-2.1 convention lock, not the doc's older `org.tatrman.argos.*`); **69 tests green** (full forked suite incl. RLS injection, admin-bypass DF-V02, TopN, column rules, LLM-judge), `lint-all`/`test-all`/proto-verify/kustomize-render all green. Readiness: `security.required=false` keeps the (not-yet-existing) policy engine out of the gate (T6). **Tag waits for 3.2** (Argos ships whole). Doc reslot of the whois/bearer-roles work into 3.1/3.2 task files done. **Not merged** (tracker rule).

## Phase 4 — Independence & sweep

| Stage | Mini-list | Status |
|---|---|---|
| 4.1 Constellation wiring end-state | [`tasks-p4-s4.1-wiring-endstate.md`](./tasks-p4-s4.1-wiring-endstate.md) | [x]⁸ |
| 4.2 Docs & ledger sweep | [`tasks-p4-s4.2-docs-sweep.md`](./tasks-p4-s4.2-docs-sweep.md) | [x]⁹ |

**Phase 4 complete (2026-06-17) — the fork pipeline (Phases 1–4) is done.** Zero ai-platform coupling on the query pipeline (build + runtime, both directions); docs flipped to dissolved/ACHIEVED; the constellation arcs are unblocked (mergepoint **M1** crossed → Iris Stage 1.1 execution starts). Only **Phase 5** (technical wave, below) remains of the fork — off the critical path.

⁸ Stage 4.1 **done 2026-06-17** (branch `feat/fork-p4-s4.1-wiring-endstate`, stacked on the 3.6 sign-off). **T1 registry completeness** — 10 bootstrap tool fixtures in capabilities-mcp mirror the runtime manifests (theseus×2, ariadne×6, echo, kadmos) + `ForkedToolRegistrySpec` (cold-start inventory, drift guard, heartbeat-supersede). **T2 PoC-heartbeat decommission** — capabilities-mcp free of ai-platform tool ids/hostnames (`query.named`→`theseus.query`, PoC fixture/host retired, README fork note); `pythia.yaml` placeholders left to that arc. **T3 observability** (depth = "orchestration spans", Bora) — coroutine-safe `withSpan`/`tracedFlow` in otel-config; `mcp.tool.<name>` root span + `theseus.run` + per-stage decorator spans; `RunQueryTracingComponentSpec` asserts one nested trace via in-memory exporter; Prometheus on the Spring OTel starter (equivalent); bare-persona service-name convention + fabric-infra wishlist in `docs/architecture/fork/observability.md`. **T4 throughput** — read-out baselines (Steropes ~188M rows/s, Brontes ~84M rows/s on 100k rows) via on-demand benches, recorded in worker READMEs; workers carry no manifest so cost_hints stay on `theseus.query`. **T5 one-command bring-up** — `just local-infra-up` + `just deploy-fork` (15 modules, dependency-ordered) + `deployment/local/README.md`; fixed `_resolve`/`deploy-kt` to find services/workers; all 15 overlays render. **T6 independence** — zero ai-platform egress (conf/yaml/k8s grep clean; residuals are naming comments → 4.2); `test-all`+`lint-all` green with no ai-platform client wired; §9 independence assertion flipped to ACHIEVED for Phases 1–4. **Carry-ins to Stage 4.2** (persona-name drift `query-mcp`→`theseus-mcp` + `query_mcp_*` metrics + `llm-gateway`→Prometheus comment + `query.named:v1` in capabilities-client tests) recorded in the 4.2 mini-list. **Not merged** (tracker rule).

⁹ Stage 4.2 **done 2026-06-17** (branch `feat/fork-p4-s4.2-docs-sweep`). **T1** final `cz.dfpartner` sweep — zero live refs (all survivors are provenance/guard-script/rename-map/historical); carry-in `query.named:v1`→`theseus.query:v1` in capabilities-client tests. **T2** CLAUDE §7 + AGENTS + EXAMPLES truth pass — §7 flipped to dissolved/ACHIEVED-for-pipeline; AGENTS recipes (py lane + deploy-fork) + stale `cz.dfpartner` Maven coords → in-repo libs / ColliteModeler; EXAMPLES §1/§2/§7/§8 rewritten to real shapes (`mcp-server-base` myth corrected, `shared.*` roots, withSpan/tracedFlow). **T3** architecture docs — kantheon-arch §10 + fork §11 risks (status column) + contracts §7 ports (Prometheus gRPC reconciled to deployed 9090) + security verified post-3.6. **T4** ledger §1 strike intact + §5 Prometheus + new §12 (Stage 4.1 observability deferrals); arc pre-flights verified (golem/pythia closed, iris no platform tools, themis switchover done) + pythia body de-staled. **T5** docs/README "what changed 2026-06-17"; master-plan (next-steps is archived) flipped Fork→P1–P4 done + M0/M1 crossed + frontier→Iris Stage 1.1; memory `kantheon_platform_fork.md` created; all ten module READMEs carry provenance+ports+run (created echo README; added Run to kyklop/ariadne/theseus/argos). **T6** carry-ins fixed (`query_mcp_*`→`theseus_mcp_*` metrics, `query-mcp`→`theseus-mcp` comments + catalog-info, argos `llm-gateway`→Prometheus); new `scripts/fork-docs-check.sh` (name/package/port + dissolved-coupling gate) green; `test-all`+`lint-all`+ruff green. **Not merged** (tracker rule).

## Phase 5 — Technical services & total independence

> Added 2026-06-13: the four "technical" services fork too, so ai-platform can be switched off. **Off the critical path** — needs only Phase 1 (and Stage 3.1 for the additive 5.3 Argos hook); does not gate Iris.

| Stage | Mini-list | Status |
|---|---|---|
| 5.0 `infra/` tree + auth/identity libs | [`tasks-p5-s5.0-infra-auth-libs.md`](./tasks-p5-s5.0-infra-auth-libs.md) | [x]¹⁰ |
| 5.1 whois service | [`tasks-p5-s5.1-whois.md`](./tasks-p5-s5.1-whois.md) | [x]¹¹ |
| 5.2 health service | [`tasks-p5-s5.2-health.md`](./tasks-p5-s5.2-health.md) | [x]¹² |
| 5.3 Argos optional `whois` role source (additive) | [`tasks-p5-s5.3-argos-role-source.md`](./tasks-p5-s5.3-argos-role-source.md) | [x]¹³ |
| 5.4 landing frontend | [`tasks-p5-s5.4-landing.md`](./tasks-p5-s5.4-landing.md) | [x]¹⁴ |
| 5.5 backstage developer portal | [`tasks-p5-s5.5-backstage.md`](./tasks-p5-s5.5-backstage.md) | [x]¹⁵ |
| 5.6 Total-independence sweep | [`tasks-p5-s5.6-independence-sweep.md`](./tasks-p5-s5.6-independence-sweep.md) | [x]¹⁶ |

After 5.0: 5.1 / 5.2 / 5.5 are mutually independent; 5.3 needs 5.1; 5.4 needs 5.2; 5.6 needs all.

**Phase 5 complete (2026-06-24) — THE FORK IS DONE.** All six stages shipped on branch `fork-5` (Stream-B worktree). The technical wave (whois / health / landing / backstage) is forked in-repo, Argos gained the additive `whois` role source, and the total-independence sweep confirmed zero live ai-platform coupling root survives. **ai-platform can be switched off without breaking a single kantheon path — One Kantheon.** Tags to cut on merge: `whois`/`health`/`backstage`/v0.1.0 + `argos`/v0.2.0.

¹⁶ Stage 5.6 **done 2026-06-24** (branch `fork-5`). **T1** zero live coupling roots (`com.platform`/`infra.{whois,health}`/`infra.erp.sql.common`) — remaining broad matches are all provenance/guard-test/rename-map/historical; whois fixture emails de-branded (18 specs still green). **T2–T3** CLAUDE §7/§7.4 + kantheon-architecture coupling section flipped to "fork complete / total independence". **T4** `deployment/local/README.md` technical-wave section (Helm/ArgoCD bring-up; per-service charts/images/ports). **T5** fork/architecture §9 independence assertion extended to TOTAL (pipeline + technical wave). **T6** plan.md Phase 5 sign-off + memory + master-plan flipped. **Not merged** (tracker rule; pushed on `fork-5`).

¹⁵ Stage 5.5 **done 2026-06-24** (branch `fork-5`). backstage forked `infra/backstage` → kantheon (Backstage / Node + yarn-berry; a **rebrand + catalog re-point**, app source unchanged). `app-config.yaml` `organization.name`/`app.title` → Kantheon; `app-config.local.yaml` host, `docs/catalog-info.yaml`, the agent-starter template owner, and the backend OIDC comment swept. New `examples/kantheon-catalog.yaml` = the constellation as a Backstage catalog (a `kantheon` System + Group + **22 Components** for the pantheon — agents/services/workers/MCP/technical), wired as a `catalog.locations` entry replacing the upstream example catalog. Full `yarn install`+`yarn build` is the **CI-gated Node-toolchain exception** (like Prometheus's Spring Boot); local gate = YAML/catalog well-formedness (all configs + the 24-doc catalog parse) + `rg df-partner|ai-platform` clean bar provenance. **Tag `backstage/v0.1.0` on merge.** **Not merged** (tracker rule; pushed on `fork-5`).

¹⁴ Stage 5.4 **done 2026-06-24** (branch `fork-5`). landing forked `frontends/landing` → kantheon (Vue 3 + Vite + vue-i18n; a **rebrand**, build shape unchanged). `header.title` → **Kantheon** in all five locales (+ `index.html` `<title>`, config example host, requirements doc, README). `public/services.json` rewritten to the kantheon estate — `tech` keys match the Stage-5.2 health `technologies.*` keys; legacy ai-platform/erp-sql tiles dropped. `.env`/`.env.example` realm → `kantheon`, added the `VITE_HEALTH_URL` var the runtime template referenced. `npm run build` (vue-tsc + vite) + `npm run lint` (oxlint+eslint) + vitest (`rebrand.spec.ts`, 3 tests: branding/locale, dispatcher-no-legacy, config-from-APP_CONFIG) all green. `rg df-partner|ai-platform` clean bar provenance + the guard test's own patterns. Image `kantheon/landing` at deploy. **Not merged** (tracker rule; pushed on `fork-5`).

¹³ Stage 5.3 **done 2026-06-24** (branch `fork-5`). **Additive** whois role enrichment for Argos. New `roles/` package: `RoleSource` (interface) + `BearerRoleSource` (default, extracts the Phase-3 behaviour) + `WhoisRoleSource` (bearer floor + `WhoisRoleLookup`, caffeine TTL cache) + `KtorWhoisRoleLookup` (CIO client over `GET /whois?user_id=&user_id_type=KEYCLOAK`, `whois-common` shapes). `ArgosServiceImpl` resolves the effective roles before the admin gate + SecurityApplier and writes them back into `auth_roles`; fails closed (`role_source_unavailable` Rule-6 error, no plan) when the source is down. Config `argos.roleSource = bearer | whois` + `argos.whois.{baseUrl,cacheTtlSeconds}`. **Default unchanged → the full existing argos suite stays green (115 tests, 0 failures)**; new `RoleSourceSpec` + `KtorWhoisRoleLookupSpec` + 3 `ArgosServiceImplSpec` cases (whois-grants-admin bypass, bearer denies, fail-closed). `kantheon-security.md` §3.6 marked implemented. **Argos minor bump `argos/v0.2.0` on merge.** **Not merged** (tracker rule; pushed on `fork-5`).

¹² Stage 5.2 **done 2026-06-24** (branch `fork-5`). health forked `infra/health` → kantheon, package sweep `com.platform.health.*`→`org.tatrman.health.*`. Image `health:dev`, port 7000, engine CIO, deps on otel-config + ktor-configurator. **T3 re-point:** `application.conf` dropped the legacy ai-platform/erp-sql targets and now checks the kantheon constellation (agents/services/workers/MCP wrappers/technical-wave, bare in-namespace names) + the retained fabric-infra (namespace-qualified); `platform-postgres`→`kantheon-postgres`; a new `ConfigLoaderTest` case guards that no `sql-*`/legacy key survives. Argos-mirrored Helm chart (probe `/health/all?threshold=0`, since installKtorServerBase adds no self-health route). **9 tests** (`PrometheusHealthCheckerTest` forked unchanged + `ConfigLoaderTest` re-pointed + new `HealthRollupComponentSpec`) + ktlint green. **Tag `health/v0.1.0` on merge.** **Not merged** (tracker rule; pushed on `fork-5`).

¹¹ Stage 5.1 **done 2026-06-24** (branch `fork-5`). whois forked `infra/whois` → kantheon, package sweep `infra.whois.*`→`org.tatrman.whois.*` + `infra.erp.sql.common.auth`→`org.tatrman.keycloak.auth`; `infra/_smoke` retired (real module landed). Image `whois:dev`, port 7110, engine CIO, deps on the Stage-5.0 libs + db-common. Flyway V1–V5 forked unchanged; `json`/`db` repository modes. Argos-mirrored Helm chart (`helm template` clean); `_resolve` gains `infra/`; new catalog alias `ktor-client-auth`. **18 mocked unit/component tests** (routes / bundle / Keycloak-WireMock / sync-orchestration / deep-merge) + ktlint green. ai-platform ships no whois unit tests, so the suite is new (T2 N/A). **Tag `whois/v0.1.0` on merge.** **Not merged** (tracker rule; pushed on `fork-5`).

¹⁰ Stage 5.0 **done 2026-06-24** (branch `fork-5` — the single Phase-5 branch Bora set for all the technical-wave forks, in the Stream-B worktree). **T1** top-level `infra/` introduced in `settings.gradle.kts` with an `infra/_smoke` placeholder (`org.tatrman.infra.smoke`; retired when whois/health/backstage land). **T2** `whois-common` forked → `org.tatrman.whois.domain` (3 records; `UserRecordTest` replaces the upstream `DummyTest` with real extension + JSON-round-trip coverage). **T3** `keycloak-auth` **extracted** off `erp-sql-common.auth` (4 files) → `org.tatrman.keycloak.auth`; `NoErpSqlCommonImportSpec` guards the zero-coupling promise; the two forked unit specs came along. **T4** both libs registered + deps wired (whois-common: kotlinx-ser; keycloak-auth: ktor-client core/apache/content-neg + ser + caffeine + typesafe-config + slf4j + coroutines). **T5** provenance headers + contracts §6 cross-check. **T6** `test ktlintCheck` green across the build; `rg infra.erp.sql.common` clean. **Not merged** (tracker rule; pushed on `fork-5`).

---

*Owner: Bora. Update stage rows as mini-lists complete; plan.md stays the authority on stage content.*
