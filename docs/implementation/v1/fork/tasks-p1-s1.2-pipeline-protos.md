# Fork — Stage 1.2: Pipeline protos

> Branch: `feat/fork-p1-s1.2-pipeline-protos`. Pre-flight: Stage 1.1 done (fork-point tag exists). Plan: [`plan.md`](./plan.md) Phase 1. Tracker: [`tasks.md`](./tasks.md). Rename authority: [`../../../architecture/fork/contracts.md`](../../../architecture/fork/contracts.md) §1.
>
> Goal: the cross-service pipeline protos live in kantheon under `org.tatrman.*`, code-gen in Kotlin and Python, Rule-6 retargeted. **Discovered constraint:** `worker.proto` imports `cz/dfpartner/metadata/v1/metadata.proto`, so the **Ariadne proto types fork here too** (types only — the Ariadne *service* lands in Stage 2.1).
>
> Source files at `ai-platform@kantheon-fork-point`:
> `shared/proto/src/main/proto/cz/dfpartner/plan/v1/{plan,context,parameters}.proto`, `worker/v1/worker.proto`, `transdsl/v1/transdsl.proto`, `dfdsl/v1/dfdsl.proto`, `metadata/v1/metadata.proto`.

- [x] **T1 — Tests first: descriptor spec.**
  In `shared/proto` (or, if the module has no test source set, add one mirroring `tools/capabilities-mcp`'s test setup — Kotest StringSpec), write `ForkedProtoDescriptorSpec` asserting, for each forked file: (a) the descriptor's package is the `org.tatrman.*` target (`plan.v1`, `worker.v1`, `transdsl.v1`, `dfdsl.v1`, `ariadne.v1`); (b) **no** dependency of any forked descriptor starts with `cz/dfpartner/`; (c) every message that declares field number 99 types it as `org.tatrman.kantheon.common.v1.ResponseMessage` (walk `Descriptors.FileDescriptor.messageTypes`, check `findFieldByNumber(99)?.messageType?.fullName`). Spec must fail (files absent) before T2.
  *Done 2026-06-13: all three invariants (a), (b), (c) are enforced by `ForkedProtoDescriptorSpec` (Kotest, `shared/proto/src/test/kotlin/org/tatrman/kantheon/fork/`) — a `FileDescriptor` walk over the seven forked outer classes (Plan/Context/Parameters, Worker, Transdsl, Dfdsl, Ariadne). 22 cases; verified live via a negative run (breaking the field-99 expectation fails exactly on worker + ariadne, the two files carrying field 99). `scripts/verify-forked-proto-layout.sh` (T6) is retained as a cheap textual pre-check.*
  *Correction 2026-06-13: the earlier note claimed the in-module Kotlin/Java source-set ordering made a descriptor walk inside `:shared:proto` "brittle" — that was wrong. The symptom (test Kotlin failing to resolve main-output proto classes despite them being on the classpath) was a stale Kotlin incremental-compile cache / stale main jar, cleared by `:shared:proto:clean`. Confirmed: a full clean build compiles and runs `ForkedProtoDescriptorSpec` against the Java-generated `org.tatrman.*` types with no source-set workarounds (no `dependsOn("jar")`, no manual `sourceSets.main.output` wiring). The protobuf `kotlin` builtin emits DSL files into the main source set; it does not alter test→main classpath wiring.*

- [x] **T2 — Fork + rename the six pipeline files.**
  Copy the seven source files into `shared/proto/src/main/proto/org/tatrman/{plan,worker,transdsl,dfdsl,ariadne}/v1/` (metadata.proto → `ariadne/v1/ariadne.proto`). In each file rewrite: `package cz.dfpartner.X.v1` → `org.tatrman.X.v1` (metadata → ariadne); all `import "cz/dfpartner/..."` paths to the new locations; **all options** — check and rewrite `option java_package`, `option java_multiple_files`, `option java_outer_classname`, and any `py_generic_services`/language options present in the originals (read them; do not assume). gRPC service rename in ariadne.proto: `MetadataService` → `AriadneService` (RPC names unchanged) per contracts §1.
  *Done 2026-06-13: seven files copied from `/tmp/aip-fork` (ai-platform worktree at the `kantheon-fork-point` tag), packages + imports + `option java_package` rewritten, `MetadataService` → `AriadneService` rename. The `metadata.proto` → `ariadne.proto` filename change required a manual fix on `worker.proto`'s import path (the rule-based rewrite preserved the old filename).*

- [x] **T3 — Rule-6 retarget.**
  `grep -n "= 99" shared/proto/src/main/proto/org/tatrman/{plan,worker,transdsl,dfdsl,ariadne}/v1/*.proto`. For every hit, replace the field's type `cz.dfpartner.metadata.v1.ResponseMessage` (or package-local equivalent) with `org.tatrman.kantheon.common.v1.ResponseMessage` and add the matching `import "org/tatrman/kantheon/common/v1/response_message.proto";`. If `ariadne.proto` *defines* a local `ResponseMessage`, delete the definition (contracts §4 — the kantheon stand-in is canon) and fix all references. T1's spec (c) goes green here.
  *Done 2026-06-13: ariadne's local `message ResponseMessage` + `enum Severity` deleted; matching `import "org/tatrman/kantheon/common/v1/response_message.proto"` added. All 16 `repeated ResponseMessage messages = 99;` references in ariadne + 3 in worker retargeted to the kantheon stand-in. Also caught a stray `repeated ResponseMessage issues = 1;` in `ValidateModelResponse` (a non-99 field, but same rule).*

- [x] **T4 — Codegen wiring, both languages.**
  `just proto` must emit Kotlin + Python for the new packages (kantheon's `:shared:proto:assemble` does KT; check the justfile `proto` recipe body for the PY leg — extend it following `ai-platform/justfile`'s `proto-py` recipe if kantheon's doesn't have one yet). Verify generated Kotlin compiles (`./gradlew :shared:proto:build`) and Python imports resolve (`python -c "from org.tatrman.plan.v1 import plan_pb2"` inside the generated package env, or the repo's equivalent generated-package smoke).
  *Done 2026-06-13: shared/proto/build.gradle.kts gains the `id("python")` builtin and a `PreparePythonPackage` task mirroring the ai-platform pattern. `:shared:proto:assemble` produces both `build/classes/{java,kotlin}/main` and `build/python-package/src/{org,cz}/...`. Python import structure verified (`from org.tatrman.plan.v1 import plan_pb2` resolves to the generated file; runtime needs `protobuf>=4.0.0` per pyproject.toml).*

- [x] **T5 — Descriptor spec green + full build.**
  `./gradlew :shared:proto:test` green (T1 spec). `just test-all && just lint-all` green — nothing else may regress (Themis still consumes `cz.dfpartner.nlp.v1` from Maven; that is expected and stays until Stage 2.6).
  *Done 2026-06-13: `scripts/verify-forked-proto-layout.sh` (T6) is the executable form of the T1 assertions; `./gradlew test` green (capabilities-mcp, smoke-worker, themis unaffected). `just lint-all` still fails on the inherited themis baseline (pre-existing; not caused by this stage — see AGENTS.md §12).*

- [x] **T6 — Charon contracts pointer edit.**
  In `docs/architecture/charon/contracts.md` and `charon/architecture.md` §2: the `cz.dfpartner.worker.v1.WorkerService` references gain the note "→ `org.tatrman.worker.v1` (in-repo) since fork Stage 1.2; behavior re-point to Steropes happens in fork Stage 3.4". No code change (Charon isn't built yet).
  *Done 2026-06-13: `docs/architecture/charon/architecture.md` §2 row "Worker access" gets the pointer note (`org.tatrman.worker.v1.WorkerService` in-repo; `WorkerEndpoint` re-point to Steropes in fork Phase 3 Stage 3.4). `docs/architecture/charon/contracts.md` had no `cz.dfpartner.worker.v1` ref to annotate; nothing to change there. The shell script `scripts/verify-forked-proto-layout.sh` is also written in this commit (it backs the T1 (a)+(b) assertions).*

- [x] **T7 — Ledger verification + tracker update.**
  Verify `docs/implementation/kantheon-v1.1.md` §1 already shows the ResponseMessage item struck with the fork pointer (done in the 2026-06-12 docs session — confirm, don't duplicate). Check the Stage 1.2 row in [`tasks.md`](./tasks.md).
  *Done 2026-06-13: `kantheon-v1.1.md` §1 already shows the ResponseMessage item struck ("Closed 2026-06-12 by the platform fork…") — no duplication. Stage 1.2 row in `tasks.md` to be checked.*

**DONE means:** forked descriptors carry only `org.tatrman.*` packages and imports; field-99 types are the kantheon stand-in everywhere; KT+PY codegen green; repo build green; the only remaining `cz.dfpartner` proto reference in kantheon is Themis's Maven-consumed `nlp.v1` (falls in Stage 2.6).
