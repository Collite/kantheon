# Fork — Stage 2.6: Themis switch-over

> Branch: `feat/fork-p2-s2.6-themis-switchover`. Pre-flight: Stages 2.1–2.5 deployed on local K3s. Plan: [`plan.md`](./plan.md) Stage 2.6. Tracker: [`tasks.md`](./tasks.md).
>
> **The one deliberate touch on a locked arc.** Themis (mid-Stage 2.4 of its own plan) moves from ai-platform runtime dependencies to the forked stack. Gate: **eval no-regression** (`agents/themis/eval/run_eval.py`, corpus in `agents/themis/eval/corpus/`). Phase 2 exits here.

- [~] **T1 — Record the eval baseline.** *(integration-deferred)*
  Run the Themis eval against the *current* configuration (ai-platform nlp-mcp/fuzzy-mcp endpoints, if still reachable; otherwise use the last recorded green run — find it in the Stage 2.4 task docs / CI history). Record score + date in `agents/themis/eval/BASELINE.md`. No-regression in T6 is measured against this number.
  *Deferred 2026-06-14: the eval needs the deployed forked stack + reachable endpoints — integration, moved to the separate integration track per the unit-tests-only policy (Bora 2026-06-14). Baseline + no-regression run there together with T6.*

- [x] **T2 — Proto import swap.**
  `shared/proto/.../kantheon/themis/v1/themis.proto`: `import "cz/dfpartner/nlp/v1/..."` → `import "org/tatrman/kadmos/v1/..."`; referenced type names follow (`cz.dfpartner.nlp.v1.AnalyzeResponse` → `org.tatrman.kadmos.v1.AnalyzeResponse`).
  - **Rule-6 retarget (logged for traceability by review-001 M3, 2026-06-13):** `themis.proto:130` still declares `repeated cz.dfpartner.metadata.v1.ResponseMessage messages = 99;`. Retarget to `org.tatrman.kantheon.common.v1.ResponseMessage` in the same commit as the nlp.v1 swap, so post-2.6 every in-repo proto shares one Rule-6 type. After this, extend `ForkedProtoDescriptorSpec`: **no descriptor in the entire kantheon proto set depends on `cz/dfpartner/` — repo-wide now**, not just forked files. The themis allowlist in `scripts/verify-forked-proto-layout.sh` can also be removed.
  - `just proto`; Kotlin sweep in `agents/themis` for the renamed generated imports; suite green.
  *Done 2026-06-14: themis.proto imports swapped (nlp.v1→kadmos.v1, metadata→common response_message); type refs `AnalyzeResponse`→kadmos.v1, `ResponseMessage`→common.v1. Main.kt swept (NerEntity/Span/Token/AnalyzeResponse→kadmos.v1; ResponseMessage/Severity→common.v1) — **no fuzzy.v1 refs** in Themis (fuzzy is HTTP/DTO). `ForkedProtoDescriptorSpec` extended with `Themis.getDescriptor()` (repo-wide cz/dfpartner-free assertion); verify-script themis allowlist removed → repo-wide check. `just proto` + spec + verify green; Themis compiles.*

- [x] **T3 — Endpoint swap.**
  Themis config: nlp-mcp URL → kadmos-mcp (7272), fuzzy-mcp → echo-mcp (7267) (gateway already flipped in 2.5 T6). All three in `application.conf` with env overrides — grep for any hardcoded ai-platform hostnames and eliminate.
  *Done 2026-06-14 — **with a correction to the task's stated endpoints.** Themis's clients call the **engine REST APIs directly**, not the MCP wrappers: `NlpClient` POSTs `/v1/analyze` (served by the **Kadmos engine on 7270**, not kadmos-mcp:7272 which speaks MCP), and `FuzzyServiceClient` POSTs `/match` (served by the **Echo service on 7265**, not echo-mcp:7267 — echo-mcp has no REST `/match`). So `application.conf`: nlp host `nlp-service`→`kadmos`:7270, fuzzy host `fuzzy-mcp`→`echo`:7265. Themis k8s local overlay swapped off the three `*.ai-platform.svc.cluster.local` hosts → `kadmos`/`echo`/`prometheus` `.kantheon.svc`. `NLP_*`/`FUZZY_*` env names kept (stable HOCON contract). No hardcoded ai-platform hostnames remain in Themis.*

- [x] **T4 — The Maven cut, completed.**
  Delete from kantheon: the `ai-platform-proto` entry + `version.ref` in `gradle/libs.versions.toml`; the entire `AiPlatformPackages` maven block in `settings.gradle.kts`; the residual PAT paragraph in `AGENTS.md`; any `GITHUB_TOKEN`-for-packages step in `ci.yml`. Clean-clone build (no `gpr.*` anywhere) fully green — this completes Stage 1.3 T5's residual.
  *Done 2026-06-14: removed `ai-platform-proto` [versions]+[libraries] from `libs.versions.toml`, the `AiPlatformPackages` (DFPartner/ai-platform) maven block from `settings.gradle.kts`, `libs.ai.platform.proto` from `agents/themis` **and** `:shared:proto` build files. **Side-effect fixed:** the removed `api(ai-platform-proto)` had been leaking `kotlinx-coroutines-core` onto `:shared:proto`'s compile classpath (generated grpc-kotlin `Flow`) — added `api(libs.kotlinx.coroutines.core)` directly. **§7.3 deviation from the task's "no gpr.* anywhere":** the `Collite/modeler` repo + the `gpr.*`/`GITHUB_TOKEN` CI auth STAY — they serve `org.tatrman:ttr-*` (permanent third-party dep, not ai-platform). AGENTS.md PAT section + ci.yml comment updated to say the PAT is now modeler-only (ai-platform coupling gone). No `cz.dfpartner` Maven coordinate remains.*

- [x] **T5 — Contracts + docs edit.**
  `docs/architecture/themis/contracts.md`: nlp.v1 → kadmos.v1 type references; nlp-mcp/fuzzy-mcp → kadmos-mcp/echo-mcp endpoints (fork contracts §5 row 1). Note the switch-over in the Themis plan (`docs/implementation/v1/themis/plan.md`) as an inserted stage with a pointer here.
  *Done 2026-06-14: `themis/contracts.md` `ResolveResponse.parse` type `cz.dfpartner.nlp.v1.AnalyzeResponse`→`org.tatrman.kadmos.v1.AnalyzeResponse`; `themis/plan.md` §9 gains an inserted "fork switch-over (fork Stage 2.6)" note under Stage 2.4 pointing here (records the kadmos/echo/prometheus endpoint reality + the eval-gate-in-integration split). Remaining `cz.dfpartner` mentions in these docs are historical/provenance prose.*

- [~] **T6 — Eval gate.** *(integration-deferred)*
  Full corpus run against the forked stack on local K3s (Kadmos + Echo + Prometheus live). **Gate: score ≥ T1 baseline.** On regression: diff per-case failures; expected causes are Kadmos engine-version drift (check eval-kadmos baseline from 2.3 T5) or fixture catalog deltas in Echo — fix the service config, never the corpus. Record the new run in `BASELINE.md`.
  *Deferred 2026-06-14: the live eval gate requires the deployed forked stack — integration, run in the separate integration track (with T1). Note for that track: watch for Kadmos engine-version drift and the **lingua langid bugfix** from Stage 2.3 (cs/en detection now correct vs the original's silent default-to-Czech) as a possible expected per-case delta.*

- [x] **T7 — Component suite + stage exit.**
  Per the testing policy (planning-conventions.md §4): mocked unit tests only; integration suite is separate. Themis mocked component suite (Wiremock where applicable — Wiremock is a mock and stays; the former Testcontainers paths are rewritten to mocked clients/fakes at unit level, with real-dependency verification moved to the separate integration-test suite) green; `rg "cz\.dfpartner" --type kotlin --type proto -g '!*build*'` across the repo returns nothing outside Phase-3 source material notes. `just test-all && just lint-all`. Check Stage 2.6 in [`tasks.md`](./tasks.md) — **Phase 2 exit**.
  *Done 2026-06-14: Themis mocked unit/component suite green (no Testcontainers rework needed — the carried-over Themis tests are already mocked/Wiremock). Repo-wide check: **no live `cz.dfpartner` import or Maven coordinate** anywhere in `.kt`/`.proto`/build files — only provenance/historical comments + the descriptor-spec's own assertion strings. `just lint-all` ✓; `just test-all` ✓ (**note:** required `-Xmx4g` — the default Gradle heap OOMs now that the Spring-inclusive build is heavier; flagged to Bora to add `org.gradle.jvmargs` to `gradle.properties`). **Phase 2 exit** (code side); the eval gate (T1/T6) closes in the integration track. Stage 2.6 ticked in `tasks.md`.*

**Review cleanup (2026-06-14, Phase 2 review pass — `fix/fork-p2-deploy-cleanup`):** the review caught that the T3 sweep fixed the Themis k8s **local overlay** but not the **base** — `k8s/base/deployment.yaml` still defaulted `NLP_MCP_*`/`FUZZY_MCP_*`/`LLM_GATEWAY_*` to `*.ai-platform.svc.cluster.local` (masked locally by the overlay's full-env `op: replace`, but wrong as the canonical manifest). MAJOR fixed: base env retargeted to `kadmos`:7270 / `echo`:7265 / `prometheus`:7280 `.kantheon.svc`; comment updated. kustomize render confirms base + overlay both emit kantheon hosts, zero ai-platform in any shippable manifest. **The T7 heap flag is now closed:** root `gradle.properties` with `org.gradle.jvmargs=-Xmx4g` added — `just test-all` runs without OOM.

**DONE means:** Themis runs entirely on kantheon-internal services, eval-proven; the last Maven thread to ai-platform is cut.
