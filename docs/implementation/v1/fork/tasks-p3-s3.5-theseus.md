# Fork — Stage 3.5: Theseus (+ theseus-mcp)

> Branch: `feat/fork-p3-s3.5-theseus`. Pre-flight: Stages 3.1–3.4 + 2.4 (Proteus) deployed. Plan: [`plan.md`](./plan.md) Stage 3.5. Tracker: [`tasks.md`](./tasks.md). Identity contract: [`../../../architecture/fork/contracts.md`](../../../architecture/fork/contracts.md) §3.
>
> Sources: `services/query-runner` (Kotlin/Ktor; the pipeline orchestrator Translator→Validator→Dispatcher with a compiled-RelNode LRU plan cache) → `services/theseus`; `tools/query-mcp` (the agent-facing edge: `run_query` tool + **IdentityResolver** — Keycloak JWT → `PipelineContext.user_id`/`auth_roles`) → `tools/theseus-mcp`. Proto forks here: `cz.dfpartner.runner.v1` → `org.tatrman.theseus.v1`, `QueryRunnerService` → `TheseusService`. Ports: Theseus 7305/7306, theseus-mcp 7307.

- [x] **T1 — Proto fork (test first).**
  `ForkedProtoDescriptorSpec` + `theseus/v1` (red): package; imports `org.tatrman.{plan,proteus,kyklop,worker,ariadne}.v1` + common. Copy `cz/dfpartner/runner/v1/*.proto` → `org/tatrman/theseus/v1/`; rename. `just proto`; green.
  *Done 2026-06-15: forked `query_runner.proto` → `theseus/v1/theseus.proto` — package `org.tatrman.theseus.v1`, `QueryRunnerService` → `TheseusService` (RPCs Run/Compile/Translate/GetStatus kept), `java_outer_classname = "TheseusProto"`. Imports map: `translator.v1` → `proteus.v1` (both `proteus.proto` for TranslateRequest/Response + `translator.proto` for Language), `worker.v1`/`plan.v1` unchanged, `metadata.v1.{ResponseMessage→common, OverallStatus/DependencyStatus→ariadne}`. Added `TheseusProto` to `ForkedProtoDescriptorSpec` + theseus dir/file to `verify-forked-proto-layout.sh`; `just proto` ✓, descriptor spec + layout verify green. (Note: the runner proto imports only plan/proteus/worker/ariadne+common — it does NOT import kyklop/argos; those are Kotlin gRPC-client concerns, not wire types on Theseus's surface.)*

- [x] **T2 — Fork Theseus.**
  `services/query-runner` → `services/theseus`; include; provenance; deps re-pointed. Package sweep; downstream client config renames: translator→`proteus:7276`, validator→`argos:7286`, dispatcher→`kyklop:7291` (config keys, env-overridable; grep out every old service hostname). Forked suite green with downstream stubs as the original has them.
  *Done 2026-06-15: forked `services/query-runner` → `services/theseus`; Kotlin root `org.tatrman.kantheon.theseus`; `QueryRunnerServiceImpl` → `TheseusServiceImpl`, grpc binding → `TheseusServiceGrpcKt`; downstream grpc stubs re-pointed (`TranslatorServiceGrpcKt`→`ProteusServiceGrpcKt`, `ValidatorServiceGrpcKt`→`ArgosServiceGrpcKt`, `DispatcherServiceGrpcKt`→`KyklopServiceGrpcKt`). Config namespaces `translator`/`validator`/`dispatcher`/`runner` → `proteus`(7276)/`argos`(7286)/`kyklop`(7291)/`theseus`; service ports HTTP 7305 / gRPC 7306; env `*_SERVER` per persona; error code `query_runner_failed`→`theseus_failed`, `setSourceService("theseus")`. `build.gradle.kts` re-pointed to kantheon catalog (`grpc.kotlin.stub`, `typesafe.config`, `ktor.serialization.kotlinx.json`, `caffeine`, jib plugin + mac socket, `theseus:dev`). `:services:theseus` added to settings; README provenance. Forked suite green (50 tests incl. e2e two-pass chain, retries, fingerprint).*

- [x] **T3 — Plan-cache data-flow tests.**
  Inherited trap (ai-platform CLAUDE.md): *call-counting mocks miss cache-replay bugs.* Verify the forked suite asserts on **captured arguments and the second call** (cache hit replays the correct compiled RelNode for the correct args, not just "dispatcher called once"). If the forked suite only counts calls, strengthen it here: two distinct queries → two cache entries; repeat of query 1 → cache hit serving query 1's plan (assert on the plan payload reaching Kyklop's stub).
  *Done 2026-06-15: the forked `CompiledPlanCacheSpec` asserts on the cached plan object (right fingerprint replayed), but `TheseusServiceImplSpec`'s cache test only counted hits — so **strengthened**: new `TheseusServiceImplSpec` test "cache replay dispatches the right compiled plan for the args (not a sibling entry)" uses a per-source parse stub (table name = source) + a capturing `DispatcherClient`. Two distinct queries → 2 cache entries; replaying query A → `cache_hit` AND the plan reaching the dispatcher carries A's table name, not B's — captured at Kyklop's edge. Guards against wrong-entry / shared-mutable-plan / key-collision replay bugs.*

- [x] **T4 — Fork theseus-mcp, IdentityResolver included (TDD on OBO).**
  `tools/query-mcp` → `tools/theseus-mcp`; sweep; channel → `theseus:7306`. IdentityResolver forks **unchanged** (JWT validation → `PipelineContext`). New tests first, then config: (a) request with a valid user bearer → `user_id` + `auth_roles` populated from `realm_access.roles`; (b) request with no token → rejected, fail-closed, Rule-6 message (kantheon-security §2.1); (c) a service-account-shaped token (no user claim) → rejected — **agents must call with the user's OBO token, never service identity** (kantheon-security §2).
  *Done 2026-06-15: forked `tools/query-mcp` → `tools/theseus-mcp` (Kotlin root `org.tatrman.kantheon.theseus.mcp`). **Forked from ai-platform HEAD** (e6ee38c7), not the fork-point tag — query-mcp had a post-fork-point, proto-compatible pattern-params improvement worth carrying (no `plan` proto drift between tag and HEAD; documented in README provenance). Proto sweep: runner→theseus, translator→proteus, validator→argos, metadata→ariadne(model types)/common(ResponseMessage,Severity), `MetadataServiceGrpcKt`→`AriadneServiceGrpcKt`. Config root `query-mcp`→`theseus-mcp`; upstream keys `query-runner/translator/validator/metadata`→`theseus`(7306)/`proteus`/`argos`/`ariadne` (+ `QueryMcpConfig.load` paths); port 7307; env `THESEUS_MCP_*`. **IdentityResolver forked unchanged** (preferred_username→sub, `realm_access.roles`). **OBO gate extracted** to a pure `identity/IdentityGate.decide(...)` (so the security-critical fail-closed decision is unit-testable) + wired into `McpTransport`. `IdentityGateSpec` (5): (a) valid bearer → id + realm roles; (b) no identity + require → `missing_user_identity`; (c) service-account token (no user claim) → `missing_user_identity` (never service identity); + spoof `identity_conflict` + dev-network allow-null. `build.gradle.kts` re-pointed (drop shadowJar, add jib plugin, kantheon aliases, `theseus-mcp:dev`, port 7307); `:tools:theseus-mcp` in settings; README provenance.*

- [x] **T5 — k8s + capability manifest.**
  `k8s/{base,overlays/local}` both; Jib `kantheon/theseus`, `kantheon/theseus-mcp`; deploy. `run_query` ToolCapability YAML (name unchanged — contracts §2: tool vocabulary forks as-is) + capabilities-client heartbeat (warn-and-continue test). Registration visible.
  *Done 2026-06-15: `k8s/{base,overlays/local}` for both — Theseus (`theseus:dev`, HTTP 7305/gRPC 7306, downstream `PROTEUS_SERVER`/`ARGOS_SERVER`/`KYKLOP_SERVER`, local overlay `THESEUS_USE_FIXTURE=true`) and theseus-mcp (`theseus-mcp:dev`, HTTP 7307, `THESEUS_SERVER` upstream, `/ready`+`/health` probes, local overlay `THESEUS_MCP_REQUIRE_IDENTITY=false`). All overlays render via `kubectl kustomize`. **Capability manifests** (tool vocabulary forks as-is, contracts §2): `resources/manifests/tools/{query,compile}.yaml` (`theseus.query:v1`, `theseus.compile:v1`); `Capabilities.kt` (`ManifestLoader` + `registerWithCapabilities`, warn-and-continue, opt-in `CAPABILITIES_MCP_URL`) wired in Application; `capabilities-client` + jackson-yaml deps added. `CapabilitiesRegistrationSpec` (3) asserts both capabilities load with their ids/endpoint/search-tags. Live deploy → integration track.*

- [x] **T6 — component smoke.**
  Per the testing policy (planning-conventions.md §4): mocked unit tests only; integration suite is separate. Mocked component test wiring the full chain with mocked Proteus/Argos/Kyklop/worker clients: `run_query` via theseus-mcp with a test-user bearer → Theseus → Proteus (parse) → Argos (validate, roles from the same bearer) → Kyklop → Brontes (mocked MSSQL fixture data) → Arrow IPC → JSON rows back through the MCP response. One DataFrame-path variant through Steropes. (True on-K3s full-chain e2e smoke deferred to the separate integration-test suite.)
  *Done 2026-06-15: `RunQueryFullChainComponentSpec` (theseus-mcp, `testImplementation(project(":services:theseus"))`) wires `run_query` via the real `QueryTool` → a real in-process `TheseusServiceImpl` (mocked Proteus parse / Argos validate / Kyklop dispatch; the worker emits **real Arrow IPC**) → decoded to JSON rows. Asserts (a) rowCount=2 JSON rows return through the MCP response, and (b) the OBO identity's roles travel as `PipelineContext.auth_roles` all the way to Argos (captured at the validator stub) — the bearer-roles contract end-to-end. DataFrame-path variant: a `session_id` run walks the same chain. (The Steropes stateful dispatch itself is unit-pinned by 3.4's `KyklopSteropesDispatchComponentSpec`.) On-K3s e2e → integration track.*

- [x] **T7 — Stage exit.**
  `just test-all && just lint-all`. Tags `theseus/v0.1.0`, `theseus-mcp/v0.1.0`. Check Stage 3.5 in [`tasks.md`](./tasks.md).
  *Done 2026-06-15: `just test-all` ✓, `just lint-all` ✓ (ktlintFormat applied), proto-verify ✓, both overlays render. New tests: Theseus 50 (+1 cache-replay), theseus-mcp 77 (+IdentityGate 5, +CapabilitiesRegistration 3, +RunQueryFullChain 2, + carried query-mcp incl. pattern-params). DONE-grep `rg -i "cz\.dfpartner|services\.runner|tools\.querymcp" services/theseus/src tools/theseus-mcp/src` → clean. **Tags `theseus/v0.1.0` + `theseus-mcp/v0.1.0` ready but NOT pushed** (coordinated at merge). Stage 3.5 ticked in `tasks.md`. **Not merged** (tracker rule).*

**DONE means:** the labyrinth is walkable in-repo: theseus-mcp serves `run_query` through the full forked chain under an OBO bearer, verified by mocked unit/component tests (true end-to-end acceptance deferred to the separate integration-test suite).
