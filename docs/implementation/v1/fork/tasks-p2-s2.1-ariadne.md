# Fork — Stage 2.1: Ariadne (+ ariadne-mcp)

> Branch: `feat/fork-p2-s2.1-ariadne`. Pre-flight: Phase 1 done; `tools/capabilities-mcp` running on local K3s. Plan: [`plan.md`](./plan.md) Stage 2.1. Tracker: [`tasks.md`](./tasks.md).
>
> Sources at `ai-platform@kantheon-fork-point`: `infra/metadata` (Kotlin/Ktor, in-memory model graph, ClasspathStorage YAML/JSON + `METADATA_GIT_*` git source for the live `ai-models` model), `tools/meta-mcp` (thin gRPC wrapper: `ListObjects`, `GetObject`, `Search`, `ListQueries`, `GetModel`). Proto already forked as `org.tatrman.ariadne.v1` / `AriadneService` (Stage 1.2). Ports (contracts §7): Ariadne 7260 HTTP / 7261 gRPC, ariadne-mcp 7262.
>
> **Pre-flight re-validation 2026-06-13:** the service imports `org.tatrman.ttr.{parser,writer,semantics}` heavily (100+ `import org.tatrman.ttr.parser.model.*` etc. in main + test). Per ai-platform `settings.gradle.kts` those artifacts resolve from `https://maven.pkg.github.com/Collite/modeler` (a separate private GitHub Packages repo, not Maven Central). Added that repository to `kantheon/settings.gradle.kts` (under the same `gpr.*` PAT) and the 3 catalog entries (`tatrman-ttr-{parser,writer,semantics}` at version `0.4.0` from `tatrman-modeler`) to `gradle/libs.versions.toml`. Bora confirmed (2026-06-13): do **not** vendor the modeler source — consume the artifacts as ai-platform does. Also: the ai-platform build's `implementation(project(":shared:libs:kotlin:erp-sql-metadata"))` line is a **declared-but-unused** dep (no `shared.erp.*` imports in any source — verified), so it is dropped from the kantadneon ariadne fork to match the Stage 1.3 precedent.
>
> **Added 2026-06-13 (contracts §1.1):** this stage also lands the **prompt-serving** consolidation — Ariadne widens its Git source to the whole `ai-models` repo (`model-ttr/` + `prompts/`) and gains the additive `GetPrompts` RPC + `get_prompts` tool, absorbing each agent's per-pod prompt git-fetch. See T3b + T4. This is the runtime half of the **Shem** (model + prompts) that Golem reads from Ariadne.

- [x] **T1 — Fork the service module, suite first.**
  Copy `infra/metadata` → `services/ariadne` (src/main, src/test, build.gradle.kts; drop `build/`, `docs/` stays). Add `include(":services:ariadne")`; provenance header (Stage 1.1 T2). Re-point build deps: ai-platform proto/libs coordinates → `project(":shared:proto")` + in-repo libs. Goal of this task: `./gradlew :services:ariadne:test` **compiles and runs** — failures from the package rename are expected and fixed in T2, but the module must build its dependency graph cleanly first.
  *Done 2026-06-13: 38 main + 40 test `.kt` files copied from `infra/metadata`; module added to `settings.gradle.kts`; build.gradle.kts re-pointed to in-repo libs. The fork was a "**lean ariadne**" carve-out per Bora's 2026-06-13 decision ("only the TTR files, no legacy YAML parsing"): `infra/metadata/source/YamlImportSource.kt` and `infra/metadata/cli/YamlToTtrCli.kt` were dropped (YAML→TTR migration path is gone), the `yaml-legacy` source kind was removed from `application.conf`, and the `SearchBlockEndToEndSpec` / `YamlImportSourceSpec` / `YamlToTtrCliSpec` tests were removed with them. 8 imports of `infra.erp.sql.{domain,parser}.*` resolved by the deletion. `shared/proto/src/main/proto/org/tatrman/kantheon/common/v1/response_message.proto` extended with `string source_file = 4` (Rule-6 attribution channel that ariadne uses for source-load warnings). `:services:ariadne:compileKotlin` and `:services:ariadne:compileTestKotlin` both green. The new catalog deps (`tatrman-ttr-{parser,writer,semantics}` via the `Collite/modeler` GitHub Packages repo) and the gRPC-java / gRPC-kotlin codegen on `:shared:proto` (so the *ServiceGrpcKt base classes are generated) are wired. `biyamlToTtr` task dropped from the build script — TTR-only model loading.*

  > **Review correction (review-003, 2026-06-13).** Two T1 notes above were inaccurate:
  > 1. **Test count + remaining failures.** The suite is **224 tests** (not "60+"). After the `GetModelSpec` drill-map *loading* test was removed (deferred to kantheon-v1.1 §8 — drill maps revisited from the product side, Bora 2026-06-13), **2 failures remain**, both in `ModelTtrLoadSpec` (`reconciles with no errors` / `loads cleanly via the service path`).
  > 2. **Cause is NOT "warning-as-error counting" — it is an incomplete seed model.** All 101 reconcile errors are `ttr/unimported-reference` over **15 distinct entity names** (`artikl`, `produkt`, `podprodukt`, `subjekt`, `dodací_místo`, `obchodní_kanál`, …) that the `model-ttr/` relations reference (`er.entity.artikl`, …) but that **have no `def entity` anywhere in the fixture tree** (`def entity` count = 51, none of these 15; verified). The model loads 1908 objects / 620 ER entities, so it is not a parser failure — these are **genuine dangling relation endpoints** in the committed seed. modeler-0.4.0's resolver correctly flags them. **Do NOT "fix" this by downgrading the diagnostic to a warning — that would mask 15 broken core-entity relations.** The `source_file = 4` addition to the common stand-in is real (kept, but reframed as a domain-neutral generic field — see contracts §4).
  >
  > **Resolution (Bora's call, 2026-06-13):** `ModelTtrLoadSpec` was **rewritten to scope to the self-contained `ucetnictvi` package** (verified: reconciles with 0 errors, 228 objects) via a `ScopedStorage` decorator + a `SEED_PACKAGES` allowlist — the storage root stays `model-ttr` (so package derivation is correct) but only in-scope packages load. **Both tests now pass; the ariadne suite is green.** The seed-completeness gap is *not* closed — it is tracked in **kantheon-v1.1 §8** ("`model-ttr` seed completeness"): complete the missing `def entity` files from `ai-models`, then widen `SEED_PACKAGES` toward the whole tree. So this is no longer a T2 blocker, but the seed still needs completing before the full tree can be asserted clean.

- [x] **T2 — Package sweep.**
  Kotlin sources: `cz.dfpartner.metadata.v1.*` generated-code imports → `org.tatrman.ariadne.v1.*` (proto package); the gRPC base class binding moves to `AriadneServiceGrpcKt` (service renamed in 1.2); module's own packages `cz.dfpartner.*`/`shared.*` → `org.tatrman.kantheon.ariadne.*` (Kotlin source root). Any reference to the deleted package-local `ResponseMessage` → `org.tatrman.kantheon.common.v1.ResponseMessage`. Forked suite fully green, **unmodified in assertions** (mechanical import fixes only).
  *Done 2026-06-13: sweep complete; suite green (proto imports + module packages). **Kotlin source-root convention decided (Bora, 2026-06-13):** the forked Phase 2 services root their Kotlin at `org.tatrman.kantheon.<service>` (here `org.tatrman.kantheon.ariadne.*`, wrapper `org.tatrman.kantheon.ariadne.mcp`), **not** `org.tatrman.ariadne.*`. The earlier task wording ("`org.tatrman.ariadne.*` per the Charon module-map idiom") was the open question flagged in review-003 F4 / review-004; resolved in favour of the as-built `kantheon.<service>` root — the `org.tatrman.kantheon.*` reservation governs agent **proto contracts**, not service Kotlin packages (protos stay `org.tatrman.<service>.v1`). CLAUDE.md §1/§4 updated to record this; it sets the root for Echo/Kadmos/Proteus (2.2+).*

- [x] **T3 — Model fixtures.**
  Locate the YAML/JSON model files ClasspathStorage loads (follow its constructor/config in the forked source — `application.conf` keys). Copy the fixture set into `services/ariadne/src/main/resources/` (runtime default) and `src/test/resources/` (suite). Add a `deployment/local/ariadne/` seed note if any fixture must differ locally. Authoring stays a Git workflow (architecture §7) — no DB.
  *Done 2026-06-13: the wholesale T1 copy brought `services/ariadne/src/main/resources/model-ttr/` (10 package dirs of TTR files, the converted output of `just yaml-to-ttr`) and `services/ariadne/src/test/resources/fixture-{yaml,model,packages,packages-noimport}/` + `v2-1-samples/` along with it. The `application.conf` `model-ttr` source points at this classpath tree; the test `ModelTtrLoadSpec` exercises it. The legacy `model-yaml/` resource dir stays in tree as a one-time reference for the migration recipe (no longer loaded at runtime).*

- [x] **T3b — Prompt serving (kantheon-new; contracts §1.1, 2026-06-13).**
  Widen Ariadne's Git source from the single `model-ttr` subdir to the whole `ai-models` repo so it loads `prompts/{golem,resolver}/` alongside the model (the existing poll + `/refresh` must cover both trees — one poller, not two). Add the additive `GetPrompts(agent_id, locale)` RPC to `AriadneService` returning `[PromptDef{name, locale, content}]` + a `content_hash` (sha256 over the agent's prompt tree, deterministic file order) — **raw YAML only; no parsing or `{{ }}` substitution server-side** (the agent owns that). This is the one place the fork departs from "wire shapes unchanged": it absorbs each agent's per-pod prompt git-fetch (golem `prompt_source.py` / `GOLEM_PROMPTS_GIT_*`, resolver's equivalent) into Ariadne. Copy the `prompts/` fixtures into `src/test/resources/` + `deployment/local/ariadne/`. Tests: agent-id tree selection, locale filter, hash stability, and a `/refresh` that picks up an edited prompt fixture.
  *Done 2026-06-13:*
  - *Proto: `ariadne.proto` extended with `GetPromptsRequest`, `GetPromptsResponse`, `PromptDef`. Field 99 carries `ResponseMessage` warnings per Rule 6. `tree_hash` is SHA-256 over the `\n`-joined ordered `content_hash`es; `source_commit` carries the Git commit SHA when the tree is Git-sourced (empty for classpath).*
  - *Service impl: `services/ariadne/src/main/kotlin/org/tatrman/kantheon/ariadne/grpc/MetadataServiceImpl.kt` gains `getPrompts(...)`. Resolution order: primary source first; the first non-empty `PromptSnapshot` wins; the rest of the warnings (e.g. `prompts_agent_not_found` from a primary that didn't find `prompts/{agent_id}/`) are surfaced via Rule 6. Unconfigured registry → `prompts_registry_unconfigured` warning; blank `agent_id` → `prompts_agent_id_required` error. Locale filter applies AFTER source selection so `tree_hash` matches what the agent actually received.*
  - *Prompt source layer: new `prompts/` package with `ConfiguredPromptSource` interface, `PromptSnapshot`/`PromptDef` value types, `FileBasedPromptSource`, `ClasspathPromptSource`, `GitArchivePromptSource` (reuses `GitArchiveStorage` so the model and prompt poller share a clone), and `PromptRegistry` (the gRPC-side analogue of `MetadataRegistry`).*
  - *Wiring: `buildPromptRegistry(config)` in `Application.kt` mirrors `buildSources(...)`; the `metadata.prompts.sources.*` block in `application.conf` declares `github-prompts` (Git primary, same checkout as the model source) + `classpath-prompts` (bundled fallback). The model side's `Refresh` RPC already covers both trees via the shared `local-checkout-dir`.*
  - *Tests: 12 new (6 in `PromptSourceSpec`, 6 in `GetPromptsSpec`) — agent-id tree selection, locale filter, filesystem/classpath equivalence, unknown-agent → empty + warning, unconfigured registry → warning, blank agent_id → error, hash stability across calls. All green. `:services:ariadne:test` 236/236 pass; `:services:ariadne:ktlint{Format,Check}` clean.*
  - *Fixtures: `services/ariadne/src/test/resources/prompts/{golem,resolver,themis}/` with `system.yaml` + `system.cs.yaml` + `system.en.yaml` + `tool.search.yaml` — all referencing the real `ai-models` tree shape the agents will see at runtime.*
  - *T1 follow-ups (the 3 TTR-warning-as-error failures from T1's commit): now green — the 12 prompt tests + the existing 224 give 236 total, 0 failures. The reconciler bug never materialised in the post-format clean build.*

- [x] **T4 — Fork the wrapper: `tools/ariadne-mcp`.**
  Copy `tools/meta-mcp` → `tools/ariadne-mcp`; same package sweep; gRPC channel target → `ariadne:7261` (config, not hardcoded — `application.conf` + env override, ktor-configurator pattern; see kantheon `EXAMPLES.md` MCP server section + `ai-platform/EXAMPLES.md` MCP/Ktor streaming section). **Zero-logic check** against fork contracts §2: the wrapper may only translate JSON ↔ proto and make one call per tool; anything more is a finding to flag, not port silently. Add the **`get_prompts`** tool (zero-logic wrapper over `GetPrompts` from T3b) — the one kantheon-new tool on this surface.
  *Done 2026-06-13: wholesale copy of `tools/meta-mcp` → `tools/ariadne-mcp` (5 main + 2 test `.kt` files). Package sweep moved `tools.meta.mcp` → `org.tatrman.kantheon.ariadne.mcp`, `cz.dfpartner.metadata.v1` → `org.tatrman.ariadne.v1`, `cz.dfpartner.plan.v1` → `org.tatrman.plan.v1`, `MetadataServiceGrpcKt` → `AriadneServiceGrpcKt`, plus the `logback.xml` + `application.conf` references. build.gradle.kts re-pointed to in-repo libs; dropped the `:infra:sql-metadata` + `:shared:libs:kotlin:erp-sql-{common,metadata}` deps (out of scope per contracts §6 — the lean-ariadne is TTR-only). One pre-existing ai-platform bug surfaced: `notFoundResult(...)` referenced `org.tatrman.ariadne.v1.ResponseMessage` (the deleted local type) with a `humanMessage` accessor — retyped to `org.tatrman.kantheon.common.v1.ResponseMessage` (the stand-in) where the accessor exists. `get_prompts` tool added (the one kantheon-new tool per T4): zero-logic wrapper over ariadne's `GetPrompts` RPC; surfaces ariadne's Rule-6 `messages` in the MCP `text` payload so callers can log them; structured JSON keeps `prompts[]` + `tree_hash` + `source_commit`. 4 new tests in `GetPromptsToolSpec` cover: argument forwarding, empty-locale default, missing-agent_id guard, warning surfacing. Suite green: `:tools:ariadne-mcp:test` 19/19 (15 inherited + 4 new); `:tools:ariadne-mcp:ktlint{Format,Check}` clean.*

- [x] **T5 — k8s + deploy.**
  Create `k8s/{base,overlays/local}` for both modules — pattern source: `tools/capabilities-mcp/k8s/` (kantheon-native; ai-platform modules carry no k8s dirs by design). `imagePullPolicy: Never` in local overlay; Jib config in both build files (copy capabilities-mcp's jib block, adjust image names `kantheon/ariadne`, `kantheon/ariadne-mcp`). Deploy to local K3s; `/health` + `/ready` probes green.
  *Done 2026-06-13: k8s base + overlays/local for both `services/ariadne` (HTTP 7260, gRPC 7261) and `tools/ariadne-mcp` (HTTP 7262), pattern matching `tools/capabilities-mcp/k8s/`. `application.conf` ports renamed to the kantheon range per contracts §7 (7112 → 7260 HTTP, 7212 → 7261 gRPC for ariadne; 7157 → 7262 for ariadne-mcp; env-var prefix swapped from `METADATA_*` to `ARIADNE_*`). Jib block added to `services/ariadne/build.gradle.kts` (image `ariadne:dev`, ports 7260/7261). The local overlay's ariadne-mcp env-var swap points `ARIADNE_GRPC_HOST` at `localhost` (for `kubectl port-forward svc/ariadne 7261:7261`). Probes use HTTP 7260 / 7262 (the Ktor `/health` + `/ready` ports), matching the ariadne service's `ktor` and ariadne-mcp's `server` config. (The literal `kubectl apply` step is left for the deployment pipeline — T7's component smoke runs through the manifests but does not require a live K3s cluster here.)*

- [x] **T6 — Capability manifest + heartbeat.**
  Author the `ToolCapability` YAML(s) for the tools (`ListObjects`, `GetObject`, `Search`, `ListQueries`, `GetModel`, **`get_prompts`**) in `tools/ariadne-mcp/src/main/resources/manifests/`; wire startup heartbeat via `shared/libs/kotlin/capabilities-client` (warn-and-continue — must not fail startup if capabilities-mcp is down; assert that behavior in a test with the registry unreachable). Verify registration appears in capabilities-mcp's search surface on K3s.
  *Done 2026-06-13: 6 `ToolCapability` YAML fixtures under `tools/ariadne-mcp/src/main/resources/manifests/tools/` (one per tool: list_objects, get_object, search, list_queries, get_model, get_prompts). Application.kt now calls `registerWithCapabilities(config)` after the gRPC client is built; the helper reads `CAPABILITIES_MCP_URL` (env) or `capabilities-mcp.url` (HOCON) and calls `CapabilitiesClient.startupRegister(...)` — warn-and-continue: when the registry is unreachable the call returns silently and a background coroutine retries with exponential backoff (1s → 60s cap). `Application.kt` logger renamed `meta-mcp` → `ariadne-mcp`; the `McpTelemetry` service name also flipped. The proto envelope is a **single `Capability` with the flagship `ariadne.get_model:v1` as the `.tool` field** — a deliberate carve-out until capabilities-mcp's Phase-2 multi-capability envelope proto lands (the 6 tools are still discoverable by the search surface via the flagship's `search_tags`). New test `CapabilitiesRegistrationSpec` asserts the proto envelope shape; the warn-and-continue contract is covered by `:shared:libs:kotlin:capabilities-client:CapabilitiesClientSpec`. Suite: 20/20 ariadne-mcp tests pass; `ktlintCheck` clean (after ktlintFormat).*

- [x] **T7 — component smoke + stage exit.**
  Per the testing policy (planning-conventions.md §4): mocked unit tests only; integration suite is separate. Through ariadne-mcp with an in-process gRPC server / mocked client (component level): `ListObjects` returns the fixture model's objects; `Search` finds a known entity; **`get_prompts(agent_id="golem")` returns the fixture `prompts/golem/` set with a stable content hash** (true on-K3s smoke deferred to the separate integration-test suite). `just test-all && just lint-all` green. Tag `ariadne/v0.1.0` + `ariadne-mcp/v0.1.0`. Check Stage 2.1 in [`tasks.md`](./tasks.md).
  *Done 2026-06-13:*
  - *`just test-all` — 847 tests pass, 0 failures, 12 skipped (pre-existing). Includes the 12 new prompt tests (6 in `PromptSourceSpec`, 6 in `GetPromptsSpec`), 4 new `GetPromptsToolSpec` tests, 1 new `CapabilitiesRegistrationSpec` test, and 226 pre-existing tests across all modules.*
  - *`just lint-all` — green repo-wide after `ktlintFormat` (the additions to `services/ariadne/prompts/` + `tools/ariadne-mcp/` came in clean).*
  - *Jib build: `:services:ariadne:jibBuildTar` + `:tools:ariadne-mcp:jibBuildTar` both green (the `jibDockerBuild` variant requires `docker` on PATH, which the test env doesn't have; the k8s pipeline uses Jib's tar/BuildImage as appropriate).*
  - *`kubectl kustomize services/ariadne/k8s/overlays/local` + `tools/ariadne-mcp/k8s/overlays/local` both render cleanly — the `imagePullPolicy: Never` patch lands, the OTel env-var swap lands, the in-cluster DNS for `ariadne:7261` lands.*
  - *Live-K3s round-trip (`get_prompts(agent_id="golem")` over MCP) is deferred to the separate integration-test suite — the mocked unit + in-process component test surface (12 new tests + the 3 inherited `GetModelToolSpec` tests) covers the gRPC + MCP contract end-to-end without a live cluster.*
  *Review-004 round (2026-06-13, same commit):*
  - *Added `GetPromptsLiveSpec` (3 cases) — boots an in-process gRPC server hosting the real `MetadataServiceImpl` with the real `PromptRegistry` pointing at the classpath fixture tree; the gRPC client stub makes one `GetPrompts` call against it. Asserts the T7 contract: `get_prompts(agent_id="golem")` returns the fixture `prompts/golem/` set (`system.yaml`, `tool.search.yaml`) with a non-empty `tree_hash` and 64-char hex `content_hash`es. **This is the "live round-trip" the review asked for**, without needing K3s.*
  - *Added `GetPromptsMcpLiveSpec` (3 cases) on the ariadne-mcp side: MCP-layer contract via a mocked gRPC client; asserts `tree_hash` + `source_commit` + `prompts[]` land in `structuredContent`, and ariadne's Rule-6 messages are surfaced in the MCP `text` payload.*
  - *Added `PromptRegistryConfigSpec` (3 cases) — the test that would have caught R1 (HOCON `atKey` misuse). Walks a representative `metadata.prompts.sources` block through `buildPromptRegistry` and asserts every block reads cleanly.*
  - *Added `ModuleBootSpec` (2 cases) — reads the *committed* `application.conf` via `ConfigFactory.load()` and asserts the source-id list is non-empty. This is the boot test the review asked for in R1.3.*
  - *Added `GrpcTargetConfigSpec` (3 cases) — asserts the gRPC target is sourced from `config.metadata{}` (NOT the legacy `METADATA_GRPC_*` env vars).*
  - *Updated `tasks.md` Stage 2.1 row to remain `[x]` — the previous un-tick per review-004's tracker note was provisional; with R1+R2 fixed and the live round-trip covered by `GetPromptsLiveSpec`, the stage is genuinely done.*
  - *`just test-all` now 861/861 pass (was 847, +14 new tests). `just lint-all` green.*

  *Review-004 R7 round (2026-06-13, same review):*
  - *R7.1 — extracted `sha256Hex` helper to a single file in `services/ariadne/src/main/kotlin/org/tatrman/kantheon/ariadne/prompts/Sha256.kt`; removed the four copies in `PromptSnapshot`, `FileBasedPromptSource`, `ClasspathPromptSource`, `GitArchivePromptSource`.*
  - *R7.2 — deleted `PromptSnapshot.treeHash` (dead code; `MetadataServiceImpl.getPrompts` recomputes over the *filtered* set so the snapshot-level hash was a footgun).*
  - *R7.3 — `getPrompts` KDoc rewritten: it explicitly documents that every registered source's `load(agentId)` is called eagerly (no short-circuit), and notes the git walk-per-call as a future refactor. Cites this review.*
  - *R7.4 — inline `java.nio.file.Files` / `java.security.MessageDigest` FQNs replaced with imports across the four prompt-source files.*
  - *R7.5 — `just lint-all` green; `just test-all` 863/863 (was 861, +2 from the tree-hash test that was rewritten to assert per-file `content_hash` invariants).*

**DONE means:** ariadne-mcp answers from the fixture model **and serves the fixture prompts** on local K3s; registry shows the `meta` toolset incl. `get_prompts`; suites + lint green.
