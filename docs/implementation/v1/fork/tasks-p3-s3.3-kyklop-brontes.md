# Fork — Stage 3.3: Kyklop + Brontes

> Branch: `feat/fork-p3-s3.3-kyklop-brontes`. Pre-flight: Stage 1.2 (`worker/v1` + `ariadne/v1` protos). Parallel with 3.4 after 3.2. Plan: [`plan.md`](./plan.md) Stage 3.3. Tracker: [`tasks.md`](./tasks.md).
>
> Sources: `services/dispatcher` (Kotlin/Ktor; routes validated PlanNodes to capable workers via HOCON worker-capability config; streams Arrow IPC back) → `services/kyklop`; `workers/mssql` (Kotlin; `WorkerService` executor over JDBC, Arrow IPC streaming, stateless) → `workers/brontes` — the first real `workers/` module (placeholder from 1.1 T3 retires here). Proto forks here: `cz.dfpartner.dispatcher.v1` → `org.tatrman.kyklop.v1`, `DispatcherService` → `KyklopService`; Brontes implements `org.tatrman.worker.v1.WorkerService` (forked 1.2). Ports: Kyklop 7290/7291, Brontes 7295/7296.

- [x] **T1 — Proto fork (test first).**
  `ForkedProtoDescriptorSpec` + `kyklop/v1` (red): package; imports `org.tatrman.{plan,worker,ariadne}.v1` + common. Copy `cz/dfpartner/dispatcher/v1/*.proto` → `org/tatrman/kyklop/v1/`; rename. `just proto`; green.
  *Done 2026-06-15: forked `dispatcher.proto` → `shared/proto/.../org/tatrman/kyklop/v1/kyklop.proto` — package `org.tatrman.kyklop.v1`, `DispatcherService` → `KyklopService` (RPCs `Dispatch`/`ListWorkers`/`GetStatus` kept), `java_outer_classname = "KyklopProto"`. Mapped `cz.dfpartner.{plan,worker}.v1` → `org.tatrman.{plan,worker}.v1`, `metadata.v1.ResponseMessage` → `common.v1.ResponseMessage` (Rule-6 = 99), `metadata.v1.OverallStatus` → `ariadne.v1.OverallStatus`. Added `KyklopProto` to `ForkedProtoDescriptorSpec` + kyklop dir/file to `verify-forked-proto-layout.sh`. `just proto` ✓; descriptor spec + layout verify green.*

- [x] **T2 — Fork Kyklop.**
  `services/dispatcher` → `services/kyklop`; include; provenance; deps re-pointed. Package sweep (binding → `KyklopServiceGrpcKt`; module → `org.tatrman.kyklop.*`). Worker-capability HOCON: rename worker entries to the Kyklops (`brontes` capability block; `steropes` placeholder commented for 3.4). Forked suite green (worker stubbed/in-process as the original does).
  *Done 2026-06-15: forked `services/dispatcher` → `services/kyklop`; Kotlin source root `org.tatrman.kantheon.kyklop` (Stage-2.1 lock), `DispatcherServiceImpl` → `KyklopServiceImpl`, grpc binding → `KyklopServiceGrpcKt.KyklopServiceCoroutineImplBase`. Config namespace + env `dispatcher`/`DISPATCHER_*` → `kyklop`/`KYKLOP_*`; ports HTTP 7290 / gRPC 7291; `setSourceService("kyklop")`. `kyklop.workers` HOCON: Brontes slot active (`KYKLOP_WORKER_BRONTES_ENDPOINT`), Steropes/Polars slot commented for 3.4. `build.gradle.kts` re-pointed to kantheon catalog (`grpc.kotlin.stub`, `typesafe.config`, `ktor.serialization.kotlinx.json`, jib plugin + mac docker socket, `kyklop:dev`). `:services:kyklop` added to settings. README provenance written. Forked suite (in-process `StubClient` worker) green — 56 tests.*

- [x] **T3 — Fork Brontes.**
  `workers/mssql` → `workers/brontes`; include (`:workers:brontes`); delete `workers/_smoke-worker` placeholder + its settings line; provenance; sweep (implements `org.tatrman.worker.v1.WorkerService` — generated imports only; `supports_stateful_sessions=false` semantics unchanged). Connection config via the named-connection/sealed-secret idiom (`charon/architecture.md` §6 pattern); no credentials in requests.
  *Done 2026-06-15: forked `workers/mssql` → `workers/brontes`; Kotlin source root `org.tatrman.kantheon.brontes`; `WorkerServiceImpl` kept (implements the generic `org.tatrman.worker.v1.WorkerService`). Proto sweep: `translator.v1` → `proteus.v1` (`TranslatorServiceGrpcKt` → `ProteusServiceGrpcKt`), `metadata.v1.{ResponseMessage,Severity}` → `common.v1.*`, `metadata.v1.{OverallStatus,DependencyStatus}` → `ariadne.v1.*`. Config namespace + env `mssql-worker`/`MSSQL_WORKER_*` → `brontes`/`BRONTES_*`; `translator {}` block → `proteus {}` (host `proteus`, gRPC 7276); ports HTTP 7295 / gRPC 7296. `connections {}` stripped of the ai-platform `DFServ7`/`DFP_Dolphin` defaults → env-only commented template (named-connection idiom; no creds in-repo). `build.gradle.kts` re-pointed (`mssql.jdbc`, `arrow.vector`/`memory.netty`, dropped unused `arrow.jdbc`, jib `brontes:dev`). `:workers:brontes` added + `_smoke-worker` placeholder and its settings line removed. README provenance written. Suite green — 49 tests (T5's contract spec adds the 50th).*

- [x] **T4 — MSSQL local infra + mocked driver tests.**
  Per the testing policy (planning-conventions.md §4): mocked unit tests only; integration suite is separate. Rewrite the forked Brontes suite to run against a mocked JDBC driver / in-memory fake at unit level, preserving the behavioural intent (the original ran Testcontainers MSSQL); real-MSSQL verification moves to the separate integration-test suite. For K3s: add an MSSQL manifest under `deployment/local/mssql/` — port from ai-platform's `deployment/local` (CLAUDE.md there lists MSSQL in local-infra; locate it under its `config`/`init`/`kustomization` tree) and wire into a kantheon `deployment/local/kustomization.yaml` (first occupant alongside the Stage 2.5 WireMock LLM fixtures — this *starts* the owned local-infra stage flagged in kantheon-architecture §7.1).
  *Done 2026-06-15: **mocked already** — the ai-platform source never used Testcontainers (`ConnectionPoolManagerSpec` uses `recordProbeResult`, `WorkerServiceImplSpec` seeds probes, `ExecutePipelineSpec` mocks the translator), so the forked suite is already at the mocked-driver level the policy wants; no rewrite needed (verified: zero `testcontainers`/`GenericContainer` refs). **Local infra authored** (ai-platform had no real MSSQL manifests — they were commented out): `deployment/local/mssql/` = Deployment (`mcr.microsoft.com/mssql/server:2022-latest`, emptyDir, Recreate, TCP probes) + Service (1433) + init-SQL ConfigMap (seeds `kantheon_local.dbo.sample_orders`) + init Job (`mssql-tools` sqlcmd, waits then applies) + kustomization with a local-only `secretGenerator` (SA password). New top-level `deployment/local/kustomization.yaml` references `mssql` (first occupant — the Stage 2.5 WireMock fixtures actually live in the Prometheus overlay, noted in-file for later consolidation). `kubectl kustomize deployment/local` renders 5 objects clean. This starts the owned local-infra stage (kantheon-architecture §7.1).*

- [x] **T5 — Arrow IPC path vs data-formatter.**
  Mocked component test (Brontes over the mocked JDBC driver): a fixture plan executed by Brontes streams Arrow IPC that `shared/libs/kotlin/data-formatter` decodes round-trip (schema + row count + a value probe). This pins the worker ↔ formatter contract before Theseus arrives. (Real-MSSQL confirmation deferred to the separate integration-test suite.)
  *Done 2026-06-15: `ArrowIpcFormatterContractSpec` drives Brontes's real Arrow path against a mocked JDBC `ResultSet`/`ResultSetMetaData` (mockk, 2 cols id INT + name VARCHAR, 2 rows): `ResultSetToArrow.convert` → `ArrowIpcSerializer.serializeBatch` produces the exact `ResultBatch.arrow_ipc` bytes; `DataFormatter.fromArrow(bytes, CSV)` round-trips them — asserts rowCount=2, columnCount=2, column names `[id, name]`, and value probes (`Brontes-alpha`/`Brontes-beta`). Added `:shared:libs:kotlin:data-formatter` as a brontes testImplementation. Green.*

- [x] **T6 — k8s + dispatch component test.**
  `k8s/{base,overlays/local}` both; Jib `kantheon/kyklop`, `kantheon/brontes`; deploy with local MSSQL up. Mocked component test (Kyklop with an in-process/mocked Brontes worker): Kyklop receives a validated fixture plan → routes to Brontes (capability match) → Arrow IPC streams back through Kyklop. Unknown-capability plan → typed Rule-6 error, not a hang. (True on-K3s dispatch integration deferred to the separate integration-test suite.)
  *Done 2026-06-15: `k8s/{base,overlays/local}` authored for both — Kyklop (HTTP 7290/gRPC 7291, `kyklop:dev`, `KYKLOP_WORKER_BRONTES_ENDPOINT=brontes:7296`, local overlay `KYKLOP_USE_FIXTURE=true`) and Brontes (HTTP 7295/gRPC 7296, `brontes:dev`, `PROTEUS_SERVER`, local overlay `BRONTES_USE_FIXTURE=true`); both local overlays `imagePullPolicy: Never`, OTEL off. All three overlays (kyklop, brontes, deployment/local) render clean via `kubectl kustomize` (live deploy → integration track; no cluster/Docker). `KyklopBrontesDispatchComponentSpec`: an in-process Brontes (engine "mssql", serving df-test, streaming two real Arrow IPC frames) — Kyklop routes a validated `db.dbo.ORDERS` scan to it and the two `arrow_ipc` frames + `schema_fingerprint` arrive byte-identical, in order, with the `routing_decision` warning Kyklop adds; an unroutable `db.dbo.CUSTOMERS` scan (default-connection no worker serves) terminates with Rule-6 `no_worker_for_connection` — fails closed, no hang.*

- [x] **T7 — Stage exit.**
  `just test-all && just lint-all`. Tags `kyklop/v0.1.0`, `brontes/v0.1.0`. Check Stage 3.3 in [`tasks.md`](./tasks.md).
  *Done 2026-06-15: `just test-all` ✓ (no OOM; +106 tests — Kyklop 56, Brontes 50), `just lint-all` ✓ (ktlintFormat applied to both new modules), proto-verify ✓, all kustomize renders ✓. DONE-grep `rg -i "cz\.dfpartner|services\.dispatcher|workers\.mssql" services/kyklop/src workers/brontes/src` → clean. **Tags `kyklop/v0.1.0` + `brontes/v0.1.0` ready but NOT pushed** (coordinated with Bora at merge — tracker rule). Stage 3.3 ticked in `tasks.md`.*

**DONE means:** Kyklop routes a fixture plan to Brontes and streams results back, verified by mocked unit/component tests (real-MSSQL / on-K3s confirmation deferred to the separate integration-test suite); the genus dispatches, the kinsman forges.
