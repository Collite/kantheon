# Fork — Stage 4.2: Docs & ledger sweep

> Branch: `feat/fork-p4-s4.2-docs-sweep`. Pre-flight: Stage 4.1. Plan: [`plan.md`](./plan.md) Stage 4.2. Tracker: [`tasks.md`](./tasks.md).
>
> Goal: the repo's words match the repo's reality; the constellation arcs are unblocked. Much of the doc surface was pre-written on 2026-06-12 (fork decision day) — this stage *verifies and finishes* rather than re-writes.

- [x] **T1 — The final `cz.dfpartner` sweep.** *(done 2026-06-17)*
  `rg "cz\.dfpartner" --hidden -g '!.git' -g '!*build*' .` across code, resources, HOCON, k8s manifests, justfile, CI, and docs. Allowed survivors: provenance headers, fork contracts §1 (the rename map), historical/handover docs explicitly marked historical. Everything else is a defect — fix it. Record the final clean output in this file.
  > **Carry-in from 4.1 T2 (2026-06-17):** incidental ai-platform tool id `query.named:v1` still used as a throwaway fixture id in `shared/libs/kotlin/capabilities-client/src/test/.../CapabilitiesClientSpec.kt` (2×) — rename to `theseus.query:v1` during this sweep (a *different* module from capabilities-mcp, which was cleaned in 4.1 T2).
  **Done:** **zero live `cz.dfpartner` refs.** Code/config/proto surface verified clean — every hit is an allowed survivor:
  - **Code/build/config** (`.github/workflows/ci.yml`, `gradle/libs.versions.toml`, `settings.gradle.kts`, `services/echo/.../MetadataServiceClient.kt`): all are **provenance comments** documenting the Stage-2.6 removal / rename map (e.g. "cz.dfpartner coupling was fully removed in fork Stage 2.6").
  - **Protos** (`argos/v1/argos.proto`, `kadmos/v1/kadmos.proto`, `common/v1/{handoff,response_message}.proto`, `proteus/v1/proteus.proto`, `security/v1/security.proto`): **rename-map provenance headers** only — no live `import`/type ref. `themis/v1/themis.proto` no longer appears (the Rule-6 `cz.dfpartner.metadata.v1.ResponseMessage` field flagged in review-001 M3 was swapped to the kantheon stand-in in Stage 2.6 — confirmed absent from the sweep).
  - **`scripts/verify-forked-proto-layout.sh`**: the **guard script itself** (it greps *for* `cz.dfpartner.*` to fail the build if any returns).
  - **READMEs** (argos, kyklop, proteus, theseus, query-translator): "`cz.dfpartner.X` is gone" provenance notes.
  - **Docs**: `fork/contracts.md` §1 (the rename map — explicitly allowed), historical `_archive/*`, `review-00*.md`/`tasks-review-*.md` review artifacts, and completed fork/themis/charon task files (records of work done). All historical/provenance.
  - **Resource grep** (`.conf`/`.yaml`/`.yml`/k8s, excluding docs): the only non-build hit is the `ci.yml` comment above — **no live config coupling.**
  
  **Fixed:** the carry-in — `query.named:v1` → `theseus.query:v1` (2×) in `CapabilitiesClientSpec.kt`. Module suite re-run green.

- [x] **T2 — CLAUDE.md + AGENTS.md truth pass.** *(done 2026-06-17)*
  Verify the 2026-06-12 pre-writes now hold and finish the residue: §7 "being dissolved" framing → dissolved (tense flip, date); AGENTS.md has no PAT/bootstrap remnant (2.6 T4), has the Python lane + forked-modules sections (1.1), build commands cover py + deploy-fork recipes (4.1 T5). EXAMPLES.md: add or pointer-in the forked-stack examples if any pattern materially changed during the fork (MCP heartbeat, OBO test fixtures) — pointers over duplication.
  **Done:**
  - **CLAUDE.md §7** — tense flipped to **dissolved/ACHIEVED for the pipeline (Phases 1–4, 2026-06-17)**, with the honest caveat that the **technical wave (§7.4, Phase 5) is the one remaining tie** (not yet executed) and §7.3 modeler is permanent (not coupling). §7.1 ("removed — Phase 1, done") + §7.2 ("removed — Phase 4, done") sub-headers flipped to past tense with the evidence pointers (§9 assertion, capabilities-mcp README fork note).
  - **AGENTS.md** — PAT bootstrap already correctly scoped to the modeler `org.tatrman` group (no ai-platform remnant); Python lane (§4.1) + forked-modules (§12.1) present. **Finished residue:** §3 recipes gained the **Python lane** (`build-py`/`test-py`/`lint-py`/`deploy-py <path>`) + **`local-infra-up` / `deploy-fork`** (4.1 T5); §1.1 table swapped stale `cz.dfpartner:{ktor-configurator,logging-config}` Maven coords → in-repo `shared/libs/kotlin/*` and the proto cell → "no `cz.dfpartner` Maven proto remains"; §5 catalog excerpt + §5.1 `settings.gradle.kts` rewritten from the **AiPlatformPackages/`cz.dfpartner`** fiction to the real **ColliteModeler/`org.tatrman`** repo + in-repo `project(":shared:...")` deps; §6.1 Rule-6 example → kantheon stand-in `org.tatrman.kantheon.common.v1.ResponseMessage`; §6.2 example cap id `query.run:v1` → `theseus.query:v1`; §1.3/§7 OTel cells de-stale'd (`shared` otel-config + Prometheus-TSDB vs `services/prometheus` name-clash note + fork observability-doc pointer); §12 Koog gotcha flipped (hedge retired, Koog 0.8.x); two broken `_archive/` links to `aip-v1-gap-closure-plan.md` fixed.
  - **EXAMPLES.md** — a fuller truth-pass than a pointer (the snippets had drifted past just package names): §1a/§1b/§2 rewritten to the **real** `KtorConfigFactory.fromConfig` + `createOpenTelemetrySdk(OtelEndpointConfig)` + `mcpStreamableHttp`/`addTool`/`guarded`/`structured` shapes (the cited **`mcp-server-base` artifact never existed** — corrected, with a note); all `cz.dfpartner.*` import roots → `shared.*` / `org.tatrman.kadmos.v1` / `org.tatrman.kantheon.common.v1`; §7 reframed "calling an ai-platform tool" → **in-repo Kadmos/Echo/Theseus/Ariadne MCPs**; §8 → in-repo `shared.otel` real signature **+ the new `withSpan`/`tracedFlow` coroutine-safe span helpers** (Stage 4.1 T3) with the observability-doc pointer. Index §7 title updated. Two deliberate `cz.dfpartner` mentions remain (explanatory "use `shared.*` not `cz.dfpartner`" notes).

- [x] **T3 — Architecture docs truth pass.** *(done 2026-06-17)*
  `kantheon-architecture.md`: §10 tense flip (coupling dissolved, date); §2/§3 tables list the forked services as present, not planned. `fork/architecture.md`: §1 SHA recorded (1.1), §9 flipped (4.1 T6), risks table — mark mitigated/realized. `fork/contracts.md` §7: ports table matches deployed manifests. `kantheon-security.md`: 3.6 rewrite landed, fork notice removed.
  **Done:**
  - **`kantheon-architecture.md`** — §10 header + body **tense-flipped to ACHIEVED for the pipeline (Phases 1–4, 2026-06-17)**; the closing "until the fork phases land…" paragraph rewritten ("pre-fork couplings on the pipeline are gone; the one remaining tie is the Phase 5 technical wave"). §2 tables already list forked services as **present** with persona + old names — refined §2.b framing to "forked … Phases 1–4 complete 2026-06-17" and fixed the stale `query.*` capability family in the capabilities-mcp row → `theseus.*`/`ariadne.*`. §3 layout is already the post-fork target.
  - **`fork/architecture.md`** — §1 SHA + §9 assertion already landed (1.1 / 4.1 T6); **§11 risks table gained a Status column** marking the six risks **Mitigated / Realized-&-accepted / Ongoing-accepted** as of 2026-06-17.
  - **`fork/contracts.md` §7** — ports table cross-checked against every forked `k8s/base` manifest: all match **except Prometheus**, whose deployed gRPC port is ai-platform's verbatim **9090**, not the reserved **7281** (the as-is fork kept it). Reconciled the table to **9090** + noted 7281 stays free (and in the block-strategy reserved list). All other rows verified (theseus 7305/06 + mcp 7307; ariadne 7260/61 + mcp 7262; echo 7265/66 + mcp 7267; kadmos 7270 + mcp 7272; proteus 7275/76; argos 7285/86; kyklop 7290/91; brontes 7295/96; steropes 7300/01).
  - **`kantheon-security.md`** — verified the **Stage 3.6 rewrite already landed** (line-5 "Fork executed (Stage 3.6, 2026-06-15)" record; §1/§2/§3.4 rewritten to in-repo owners; §3.6 Argos role-source added). The remaining fork blockquotes are *accurate* (the "fork executed" provenance record + the still-pending Phase-5 whois addendum) — not stale "pending-fork" banners, so kept. The sole `query-mcp` mention is the `query-mcp → theseus-mcp` provenance note. No edit needed.

- [x] **T4 — Ledger + arc pre-flight pass.** *(done 2026-06-17)*
  `kantheon-v1.1.md`: §1 strike intact; §5 Prometheus items present; add anything Phase 2–4 consciously deferred (collect from stage files' findings notes). Arc plans: `iris/plan.md`, `golem/plan.md`, `pythia/plan.md` pre-flights point at theseus-mcp/ariadne-mcp and the fork tags (3.6 T6 did pythia/golem — verify, and do iris if it references platform tools). `themis/plan.md` shows the switch-over stage as done.
  **Done:**
  - **Ledger `kantheon-v1.1.md`** — §1 ResponseMessage strike (line 18) **intact** (closed/graduated to fork Stage 1.2); §5 has all three **Prometheus** items present (cost-attribution, budget ceilings, tier-routing). Verified §8 Ariadne (2.1), §9 Charon (3.4), §10 Metis, §11 theseus-mcp (3.6) Phase 2–4 deferrals already recorded. **Added §12 — Fork observability / fabric-infra (Stage 4.1):** the fabric-infra dashboards/alerts wishlist (decision #8, not in-repo), cross-pod trace-nesting verification (integration-suite), and worker `cost_hints` (no manifest home) — the three conscious Stage-4.1 deferrals that had no ledger entry.
  - **Arc pre-flights** — `golem/plan.md` (ariadne-mcp/theseus-mcp, **closed 2026-06-17**) + `pythia/plan.md` (theseus-mcp/steropes, Fork Phase 3 **closed 2026-06-17**) verified done from 3.6 T6. **`iris/plan.md` references no platform tools** (iris-bff routes via Themis + new-golem `/v2`, never query-mcp/ariadne-mcp) — nothing to repoint. Fixed residual stale **body** refs in `pythia/plan.md` (not pre-flights): Phase 2 deliverable "QueryNode via query-mcp" → **theseus-mcp**; §8 retitled "In-repo coordination" with the now-moot ai-platform cross-repo track struck (gateway → in-repo Prometheus backlog §5; worker read-out verified Stage 4.1 T4); the two stale Phase-4 pre-flight rows (35–36) on the never-written `aip-v1-gateway-worker-plan.md` struck as moot/superseded.
  - **`themis/plan.md`** — the **fork switch-over (fork Stage 2.6) is shown as done** (line 398: "Code parts done 2026-06-14"; proto `cz.dfpartner.nlp.v1`→`org.tatrman.kadmos.v1`, endpoints nlp→Kadmos/fuzzy→Echo/gateway→Prometheus, last Maven dep cut; eval gate on the integration track). No edit needed.

- [x] **T5 — README + memory + next-steps.** *(done 2026-06-17)*
  `docs/README.md`: fork entries verified, "what changed" gains the completion date. `next-steps.md`: top pointer flips to **Iris Stage 1.1 execution starts** (the fork-first gate is satisfied). Memory mirror (`kantheon_platform_fork.md` + index) updated to "fork executed" state. Module READMEs: every forked module has provenance + ports + run instructions (spot-check all ten).
  **Done:**
  - **`docs/README.md`** — added a **"What changed 2026-06-17"** entry (fork Phases 1–4 complete; zero coupling; §9 assertion; M1 satisfied → Iris unblocked; Phase 5 the remaining tie). Flipped the stale "Maven coupling is removed by Phase 1" line on the ai-platform repo entry to past tense + pipeline-coupling-gone.
  - **next-steps → master-plan** — `next-steps.md` is **archived** (replaced by `master-plan.md`, the live resumption pointer). Updated master-plan to reality: arc-inventory Fork row → **P1–P4 done (2026-06-17)**; dependency-graph Fork marks → ✓; **M0 + M1 marked CROSSED**; status-board Fork → done + Iris → **"Unblocked — M1 crossed → Iris Stage 1.1 execution starts"**; **§6 frontier rewritten** to "the Fork is complete (Phases 1–4) — M1 crossed; Iris is unblocked … Stream A's next move is Iris Stage 1.1 execution." (the fork-first-gate flip.)
  - **Memory** — created `kantheon_platform_fork.md` (project memory, "fork executed" state: Phases 1–4 done / Phase 5 pending, per-phase summary, the two permanent caveats, Iris unblocked; links [[pythia_framework_choice]] + [[proto-kotlin-stale-cache]]) + added the index line to `MEMORY.md`.
  - **Module READMEs (all ten spot-checked)** — found + fixed two gaps: **`services/echo` had no README at all** (created: provenance header + surface + loader sources + Run + ports `7265/7266`, echo-mcp `7267`); **`services/kyklop`, `services/ariadne`, `services/theseus`, `services/argos` lacked a Run section** (added `just build/test/deploy-kt` blocks + relevant config notes). All ten now carry provenance + ports + run instructions.

- [x] **T6 — Final verification + arc handoff.** *(done 2026-06-17)*
  Cross-doc consistency run: every persona name, package root, port, and tag referenced in fork docs greps consistently across the repo (script it: a small `scripts/fork-docs-check.sh` with the name/package/port table — commit it; CI-optional). `just test-all && just lint-all` one last time. Check Stage 4.2 in [`tasks.md`](./tasks.md) — **Phase 4 exit, fork complete; the constellation arcs resume.**
  **Done:**
  - **Carry-ins fixed.** `query_mcp_*` → **`theseus_mcp_*`** metric prefix in theseus-mcp `InstrumentedTool.kt` + `Application.kt` (7 names; no test referenced the old names). `query-mcp` → **`theseus-mcp`** persona-name comments in `context.proto`, `converter.py`, `worker-steropes.md`, `DataFormatter.kt`, `Format.kt`, argos `SecurityApplier.kt` + `ArgosServiceImpl.kt` (settings.gradle.kts provenance left as the allowed survivor). `tools/theseus-mcp/docs/catalog-info.yaml` → `name: theseus-mcp` + `system: kantheon` + Theseus description. argos `application.conf` `llm-gateway` → **Prometheus** (comments + example URLs; no live endpoint).
  - **Consistency gate.** New **`scripts/fork-docs-check.sh`** (executable, CI-optional) with the persona/dir/package/port table: asserts each forked module's README provenance + HTTP port in k8s base; guards **no live `cz.dfpartner`**, **no `query-mcp` persona name**, **no `query_mcp_` metric prefix** in code/config (docs/archives excepted — they carry legit historical refs); verifies Kotlin source roots (`org.tatrman.kantheon.<svc>`, with the Python lanes + Spring-Boot Prometheus `org.tatrman.prometheus` as documented exceptions). **Green.**
  - **Gates.** `just test-all` **green** (51s; theseus-mcp metric rename compiles + passes), `just lint-all` **green**, steropes `ruff` clean.
  - **Phase 4 exit.** Stage 4.2 ticked in [`tasks.md`](./tasks.md) with the Phase-4-complete callout; **the fork pipeline (Phases 1–4) is done** — mergepoint M1 crossed, Iris Stage 1.1 execution unblocked; only Phase 5 (technical wave) remains, off the critical path.

**DONE means:** docs match deployed reality; ledgers reconciled; next-steps says "Iris"; the new platform stands on its own and says so truthfully. — **all met; Stage 4.2 complete; Phase 4 (the fork) closed.**
  > **Carry-in from 4.1 T2 (2026-06-17):** persona-name drift `query-mcp` → `theseus-mcp` to fix as part of this consistency pass: `tools/theseus-mcp/docs/catalog-info.yaml` (`name: query-mcp` — the Backstage descriptor still claims the pre-fork name) and explanatory **code comments** referencing `query-mcp`'s role (now Theseus/theseus-mcp): `services/argos/.../ArgosServiceImpl.kt` + `.../SecurityApplier.kt`, `shared/proto/.../plan/v1/context.proto`, `shared/libs/kotlin/data-formatter/.../Format.kt` + `.../DataFormatter.kt`, `workers/steropes/.../converter.py`, `settings.gradle.kts` (the "forked from tools/query-mcp" provenance comment is an allowed survivor). None await the decommissioned heartbeat — pure naming.
  > **Carry-in from 4.1 T6 (2026-06-17):** one stale persona-name comment — `services/argos/src/main/resources/application.conf` references "a `models.yaml` entry in **llm-gateway**" (now **Prometheus**). Not an endpoint (no egress); fix in the persona-name pass.
  > **Carry-in from 4.1 T3 (2026-06-17):** theseus-mcp's Micrometer metric names still carry the pre-fork `query_mcp_*` prefix (`query_mcp_tool_calls_total`, `query_mcp_tool_call_duration_seconds`, `query_mcp_tool_errors_total`, `query_mcp_active_requests` in `InstrumentedTool.kt` + `Application.kt`) — rename to `theseus_mcp_*` during the consistency sweep (left untouched in T3 to keep the span change isolated and avoid silently breaking any existing dashboard scrape).

**DONE means:** docs match deployed reality; ledgers reconciled; next-steps says "Iris"; the new platform stands on its own and says so truthfully.
