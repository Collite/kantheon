# Fork — Stage 2.4: Proteus

> Branch: `feat/fork-p2-s2.4-proteus`. Pre-flight: Stage 1.3 (`query-translator`, `ttr-parser`, `ttr-writer` in-repo). Plan: [`plan.md`](./plan.md) Stage 2.4. Tracker: [`tasks.md`](./tasks.md).
>
> Source: `services/translator` (Kotlin/Ktor; thin gRPC wrapper around the `query-translator` Calcite/RelNode engine; RPCs `ParseToRelNode`, `UnparseFromRelNode`, `Translate`, `Explain`). Proto forks here: `cz.dfpartner.translator.v1` → `org.tatrman.proteus.v1`, `TranslatorService` → `ProteusService`, **RPC names kept** (contracts §1). No MCP wrapper exists or is added. Ports: 7275/7276.
>
> ⚠ Inherited warning (ai-platform CLAUDE.md): **ktlint failures recur in `services/translator`** — tests pass, lint fails. Budget for it.

- [x] **T1 — Proto fork (test first).**
  Extend `ForkedProtoDescriptorSpec` with `proteus/v1` (red): package, RPC names unchanged (`ParseToRelNode`, `UnparseFromRelNode`, `Translate`, `Explain`), imports only `org.tatrman.{plan,ariadne}.v1` + common. Copy `cz/dfpartner/translator/v1/*.proto` → `org/tatrman/proteus/v1/`, rename per contracts §1. `just proto`; green.
  *Done 2026-06-14: the service surface forked to `org/tatrman/proteus/v1/proteus.proto` — package `org.tatrman.proteus.v1`, `service ProteusService` (RPCs `ParseToRelNode`/`UnparseFromRelNode`/`Translate`/`Explain`/`DetectSourceSchema` unchanged), `java_outer_classname = "ProteusProto"`. **Key reconciliation:** `org/tatrman/proteus/v1/translator.proto` already existed (Stage 1.3 library-only stub holding the `Language`/`SqlDialect` enums consumed by `query-translator`), so the service proto **imports** it rather than redefining the enums (proto3 forbids duplicate defs in a package — that was the initial codegen collision). Refs: plan/context → `org.tatrman.plan.v1`, ResponseMessage → `org.tatrman.kantheon.common.v1` (Rule 6). No `ariadne` import actually needed (only plan + common referenced). `ForkedProtoDescriptorSpec` + verify script extended with `proteus/v1`; `just proto` (KT+PY) + spec + verify all green.*

- [x] **T2 — Fork the service module.**
  `services/translator` → `services/proteus`; include; provenance; deps → `project(":shared:proto")` + `project(":shared:libs:kotlin:query-translator")` (+ `ttr-parser`/`ttr-writer` if the build pulls them). Package sweep: generated imports → `org.tatrman.proteus.v1`, binding → `ProteusServiceGrpcKt`, module packages → `org.tatrman.proteus.*`.
  *Done 2026-06-14: tree copied (excl. build/.gradle); Kotlin root `services.translator` → **`org.tatrman.kantheon.proteus`** (the locked Stage-2.1 convention — `org.tatrman.kantheon.<service>`, as echo did; the task's "org.tatrman.proteus.*" wording is the loose form, the `.v1` reservation governs protos). Generated-import sweep: translator→`org.tatrman.proteus.v1` + `ProteusServiceGrpcKt`/`ProteusServiceCoroutine*`; **metadata→Ariadne** (`org.tatrman.ariadne.v1.*` + `AriadneServiceGrpcKt`/`AriadneServiceCoroutineStub` — incl. ModelSnapshot, ObjectDescriptor, GetQuery/GetSnapshot, Er2Db* mapping types, all present in ariadne.proto); ResponseMessage + Severity → `org.tatrman.kantheon.common.v1`; plan/context → `org.tatrman.plan.v1`. Service identity rebranded: config sections `translator`→`proteus`, `metadata`→`ariadne`; env `TRANSLATOR_*`→`PROTEUS_*`, `METADATA_*`→`ARIADNE_GRPC_*` (host `ariadne`, gRPC `7261`); ports **7275** (HTTP) / **7276** (gRPC); serviceName/OTel/logback/status-endpoint all `proteus`. Internal impl class `TranslatorServiceImpl` kept (internal, tests reference it; persona canon is satisfied by the proto `ProteusService` + service identity). build.gradle.kts: alias fixes (`grpc.kotlin`→`grpc.kotlin.stub`, `hocon.config`→`typesafe.config`, `ktor.serialization.json`→`ktor.serialization.kotlinx.json`), jib plugin added + rancher socket, image `proteus:latest`. `ttr-*` come transitively via `query-translator` (not added directly — build resolves). README provenance header + catalog-info rebrand. `:services:proteus:compileKotlin` green.*

- [x] **T3 — Calcite golden suite green.**
  The forked test suite — especially round-trip goldens (language → RelNode → SQL) — green with assertions unmodified. On Calcite API mismatches, consult `~/Dev/view-only/calcite` (graphify-out available) and `ai-platform/EXAMPLES.md` §Apache Calcite before changing any pin; the Stage 1.3 T2 version decision is the authority.
  *Done 2026-06-14: `:services:proteus:test` green — **29 tests, 0 failures** across all 6 specs (TranslatorServiceImplSpec 14, TranslatorQueryRefSpec, DetectSourceSchemaSpec, SnapshotModelHandleSpec, MetadataServiceModelHandleProviderSpec, WarmUpSpec). Golden assertions unmodified; no Calcite pin changes needed (the in-repo `query-translator` lib keeps `shared.translator.*` and the Stage-1.3 Calcite pins). The round-trip goldens (parse→RelNode→SQL) pass as-is.*

- [x] **T4 — ktlint pass.**
  `./gradlew :services:proteus:ktlintCheck` — expect failures (inherited); `ktlintFormat` + manual fixes until clean. Do this as its own task so it isn't skipped under "tests green".
  *Done 2026-06-14: `:services:proteus:ktlintCheck` **clean on first run** — the inherited ai-platform ktlint debt didn't carry over (it was an `ktlint_official` formatting-rule delta resolved repo-wide in Phase 1.3 review-002; the forked source was already format-clean under kantheon's config). No `ktlintFormat` needed.*

- [x] **T5 — k8s + deploy.**
  `k8s/{base,overlays/local}` (capabilities-mcp pattern); Jib image `kantheon/proteus`; deploy local K3s; probes green. No capability manifest — Proteus is an internal pipeline service called by Theseus, not an agent-facing tool (contracts §2 lists only the four wrappers).
  *Done 2026-06-14: `services/proteus/k8s/{base,overlays/local}` — Deployment+Service (HTTP 7275 + gRPC 7276), image `proteus:dev`, probes `/ready`+`/health`, env `ARIADNE_GRPC_{HOST,PORT}` (ariadne:7261). Local overlay: `imagePullPolicy: Never` + `PROTEUS_USE_FIXTURE_MODEL=true` (boots Ready without Ariadne) + OTel off. No capability manifest (internal pipeline service). `kubectl kustomize overlays/local` renders cleanly; `:services:proteus:jibBuildTar` → `build/jib-image.tar` (~178 MB). Live K3s deploy deferred (no daemon — Ariadne/Echo precedent); `just deploy-kt proteus` is the bring-up.*

- [x] **T6 — component smoke + stage exit.**
  Per the testing policy (planning-conventions.md §4): mocked unit tests only; integration suite is separate. A mocked Kotest component spec (service constructed over the boot fixture): `ParseToRelNode` of a fixture query returns a RelNode that `UnparseFromRelNode` round-trips to the golden SQL; `Explain` returns the debug shape (true grpcurl-on-K3s e2e smoke deferred to the separate integration-test suite). `just test-all && just lint-all`. Tag `proteus/v0.1.0`. Check Stage 2.4 in [`tasks.md`](./tasks.md).
  *Done 2026-06-14: the ParseToRelNode→UnparseFromRelNode round-trip-to-golden-SQL is covered by the passing mocked Kotest component specs (TranslatorServiceImplSpec / TranslatorQueryRefSpec / WarmUpSpec construct the service over the boot fixture and assert the golden SQL) — the task accepts a mocked Kotest component spec; live grpcurl-on-K3s e2e deferred to the separate integration-test suite (no cluster, precedent). Gates green: `just test-all` (95 tasks) ✓, `just lint-all` ✓. **Tag NOT pushed** (no `*/v0.1.0` service tags exist yet; coordinated with Bora at merge — tracker rule). Stage 2.4 ticked in `tasks.md`.*

**Review cleanup (2026-06-14, Phase 2 review pass — `fix/fork-p2-deploy-cleanup`):** jib image `proteus:latest`→`proteus:dev` (MAJOR — k8s base referenced `:dev`, so the local deploy would `ErrImageNeverPull`); `ariadne.poll-interval-seconds` in-code fallback `60`→`600` (match conf); `BootFixtureModel` KDoc key `translator.use-fixture-model`→`proteus.use-fixture-model`; `SnapshotModelHandle` comment `infra/metadata`/`services/translator`→Ariadne/Proteus personas; dead `telemetry.otlp` HOCON block removed (endpoint comes from the env var via `OtelEndpointConfig`). kustomize render confirms `proteus:dev` + `imagePullPolicy: Never`.

**DONE means:** Parse/Unparse round-trip green under mocked unit/component tests (deployed on local K3s; e2e verification deferred to the separate integration-test suite); goldens untouched; lint actually clean.
