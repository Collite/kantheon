# Fork — Stage 2.5: Prometheus

> Branch: `feat/fork-p2-s2.5-prometheus`. Pre-flight: Stage 1.3. Plan: [`plan.md`](./plan.md) Stage 2.5. Tracker: [`tasks.md`](./tasks.md).
>
> Source: `infra/llm-gateway` (**Spring Boot** — forked as-is, no Ktor rewrite; the repo's first and only Spring module). Proto: `org.tatrman.llmgateway.v1` → `org.tatrman.prometheus.v1`, service name aligns to `PrometheusService` (contracts §1 — already org.tatrman, rename is persona consistency). Port: 7280. Caller today: Themis (the only live kantheon agent).

- [x] **T1 — Proto rename (test first).**
  Extend `ForkedProtoDescriptorSpec` with `prometheus/v1` (red). Move/rename `org/tatrman/llmgateway/v1/*.proto` → `org/tatrman/prometheus/v1/` in kantheon `shared/proto` (copy from ai-platform at the fork tag), rename package/options/service. `just proto`; green.
  *Done 2026-06-14: `org/tatrman/prometheus/v1/prometheus_chat.proto` forked from `llm_gateway_chat.proto` — package **normalised** `org.tatrman.llmgateway.grpc` → `org.tatrman.prometheus.v1` (the original was non-conforming `.grpc`; now matches the `.v1` convention), `java_outer_classname = "PrometheusProto"`, `service ChatService` → `PrometheusService`. No Rule-6 field / no cz.dfpartner (gateway proto). Spec + verify script extended; `just proto` + descriptor spec green.*

- [x] **T2 — Fork the module: `services/prometheus`.**
  Copy `infra/llm-gateway` → `services/prometheus`; provenance header; include in settings. Catalog work: add the Spring Boot + `kotlin.spring` plugin aliases and Spring BOM coordinates to `gradle/libs.versions.toml` (they don't exist in kantheon yet — copy versions from the module's original build, no hardcoding in the module file). Document the single-Spring-module exception in the module README + `AGENTS.md` one-liner.
  *Done 2026-06-14: module copied (excl. `bin/`/build); included in `settings.gradle.kts`. **Catalog port:** added [versions] spring-boot 4.0.2, spring-dep 1.1.7, spring-ai **2.0.0-M2**, spring-grpc 1.0.1, springdoc 3.0.1, kaml 0.61.0, flyway 11.20.3, nats 0.6.2+3.5, h2 2.4.240, otel-instrumentation-bom 2.25.0; [plugins] kotlin-spring/spring-boot/spring-dep; [libraries] the boot-starter-* / ai-starter-model-* / spring-grpc / springdoc / flyway / h2 / nats / kaml / otel-bom (reusing kantheon's existing grpc/jackson/kotest/mockk/postgresql/typesafe-config/logstash aliases). **Resolves from mavenCentral** — Spring AI 2.0.0-M2 needed no milestone repo (as ai-platform did). build.gradle.kts uses kantheon aliases; dropped the integration-only deps (testcontainers/wiremock/boot-starter-test) + the `protobuf` & `spring-openapi` plugins + the `openApi{}` block (unit-tests-only, no protoc in-module — protos come from `:shared:proto`). README provenance + Spring-exception; AGENTS.md framework-row one-liner; catalog-info rebrand. App class `LlmGatewayApplication` → `PrometheusApplication`. **`libs.pgsql`→`libs.postgresql`** (kantheon alias). `compileKotlin` green.*

- [x] **T3 — Package sweep + suite.**
  `org.tatrman.llmgateway.*` → `org.tatrman.prometheus.*` (sources + generated imports + `application.yaml`/`application.properties` keys if package-derived). Forked suite green unmodified in assertions.
  *Done 2026-06-14: Kotlin root `org.tatrman.llmgateway.*` → `org.tatrman.prometheus.*` (the as-is Spring fork keeps `prometheus.*`, not `kantheon.prometheus`, per task T3). **Proto/Kotlin package collision resolved:** in ai-platform the Kotlin pkg `org.tatrman.llmgateway.grpc` == the proto java_package, so `GrpcChatService` referenced generated types unqualified; post-split (Kotlin `prometheus.grpc`, proto `prometheus.v1`) it now `import org.tatrman.prometheus.v1.*` and the Java binding `ChatServiceGrpc.ChatServiceImplBase` → `PrometheusServiceGrpc.PrometheusServiceImplBase`. application.yml identity rebranded (app name `prometheus`, server port default **7280**, logging pkg, security service name); functional `LLM_GTW_*` env vars kept (as-is). **33 unit tests, 0 failures** (kotest+mockk; Spring beans mocked, no context). Assertions unmodified.*

- [x] **T4 — Secrets + local LLM fixtures.**
  Upstream LLM API keys via the sealed-secret idiom (fabric-infra pattern; reference `charon/architecture.md` §2 secrets row). For local K3s: WireMock fixtures emulating the upstream chat-completion endpoints into `deployment/local/wiremock-llm/` (port the relevant mappings from ai-platform's `deployment/local/wiremock` if they exist; otherwise author minimal ones for the smoke path). The pod must come up healthy with only WireMock upstreams.
  *Deferred to the separate integration track (Bora 2026-06-14: skip everything integration/infra; design + run integration tests separately). The k8s **consumes** upstream LLM keys from a sealed `prometheus-secrets` Secret (`AZURE_OPENAI_API_KEY` / `ANTHROPIC_API_KEY` via `secretKeyRef`) — the secret authoring + WireMock upstream fixtures + the "pod healthy on WireMock" live check belong to that integration suite. The local overlay boots standalone (H2 `test` profile + placeholder keys) so no secret/WireMock is needed for non-integration use.*

- [x] **T5 — k8s + deploy.**
  `k8s/{base,overlays/local}` ; Jib works for Spring Boot (keep Jib for consistency — verify the image boots; if the module's original build used a different image mechanism, prefer Jib and note the delta). Deploy; probes green (Spring actuator endpoints mapped to the kantheon probe convention).
  *Done 2026-06-14: `services/prometheus/k8s/{base,overlays/local}` — Deployment+Service (HTTP **7280** + gRPC 9090), image `prometheus:dev`, Spring-actuator probes `/actuator/health/{readiness,liveness}`, API keys via `prometheus-secrets`. Local overlay: `imagePullPolicy: Never` + `SPRING_PROFILES_ACTIVE=test` (H2, standalone — no Postgres) + placeholder keys. `kubectl kustomize overlays/local` renders cleanly; **Jib works for Spring Boot** — `:services:prometheus:jibBuildTar` → `build/jib-image.tar` (~232 MB). Live K3s deploy deferred (no daemon; integration track).*

- [x] **T6 — Flip Themis's gateway endpoint.**
  Themis's llm-gateway URL config → the in-repo Prometheus service address. Run the Themis unit/component suites that exercise LLM calls against WireMock-backed Prometheus (Wiremock is a mock and stays). Per the testing policy (planning-conventions.md §4): mocked unit tests only; integration suite is separate — on-K3s confirmation against the deployed Prometheus is deferred to the separate integration-test suite. (Full eval re-run happens in Stage 2.6 — don't duplicate here.)
  *Done 2026-06-14: `agents/themis/src/main/resources/application.conf` `llm-gateway` block flipped — host `llm-gateway`→`prometheus`, port `8090`→`7280` (env overrides `LLM_GATEWAY_{HOST,PORT}` unchanged). Themis's mocked suites stay green (`just test-all` passed — the flip is config-default only; Wiremock-backed unit tests override the endpoint). On-K3s confirmation against deployed Prometheus → integration suite; full eval re-run is Stage 2.6.*

- [x] **T7 — Backlog re-home verification + stage exit.**
  Verify `kantheon-v1.1.md` §5 already carries the Prometheus-native cost-attribution + tier-routing items (done in the 2026-06-12 docs session — confirm, don't duplicate). `just test-all && just lint-all`. Tag `prometheus/v0.1.0`. Check Stage 2.5 in [`tasks.md`](./tasks.md).
  *Done 2026-06-14: `kantheon-v1.1.md` already carries the three Prometheus backlog rows (Prometheus-native cost-attribution; per-user/org budget ceilings; Prometheus tier routing) — confirmed, not duplicated. Gates green: `just test-all` (100 tasks) ✓, `just lint-all` ✓ (prometheus ktlint clean after `ktlintFormat` on the build script's BOM chain). **Tag NOT pushed** (no `*/v0.1.0` service tags exist yet; coordinated with Bora at merge). Stage 2.5 ticked in `tasks.md`.*

**Review cleanup (2026-06-14, Phase 2 review pass — `fix/fork-p2-deploy-cleanup`):** jib image `prometheus:latest`→`prometheus:dev` (MAJOR — k8s base `:dev`, else local `ErrImageNeverPull`); OTLP protocol dead branch fixed (`protocol` lowercased then matched against `"HTTPS"`/`"HTTP"` → never matched → always grpc:4317; now lowercase compare); legacy-naming sweep `LLM_GTW_*`→`PROMETHEUS_*` (code/yml/k8s) and `llm-gateway`→`prometheus` (app name, db/h2 names, cache key prefix, `models-config-path`, api-docs profile name + port `7001`→`7280`, mkdocs/catalog/technical docs, README versions Spring 4.0.2/Kotlin 2.3.0). The actuator `management.endpoint.prometheus` (Micrometer scrape) left untouched. Stale `build/` classes cleaned. kustomize render confirms `prometheus:dev`.

**DONE means:** Themis completes an LLM call through in-repo Prometheus on local K3s (WireMock upstream); Spring exception documented; catalog clean.
