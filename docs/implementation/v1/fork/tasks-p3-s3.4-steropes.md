# Fork — Stage 3.4: Steropes (+ Charon re-point)

> Branch: `feat/fork-p3-s3.4-steropes`. Pre-flight: Stage 1.1 T4 (Python lane); parallel with 3.3 after 3.2. Charon interplay: if Charon Phase 1 hasn't landed yet, T5 becomes a contracts-only edit (see task). Plan: [`plan.md`](./plan.md) Stage 3.4. Tracker: [`tasks.md`](./tasks.md).
>
> Source: `workers/polars` (**Python**; in-memory DataFrame executor implementing `worker/v1`; session workspace — `workspace.py` with idle-TTL + caps — the pattern Metis ports; `conf/`, `Dockerfile`, `uv.lock`). Target: `workers/steropes`. Ports: 7300/7301.

- [x] **T1 — Fork the module.**
  `workers/polars` → `workers/steropes` (src, tests, `conf/`, `pyproject.toml`, `Dockerfile`; `uv sync` regenerates the lock). Provenance header. Rename Python packages `workers_polars`/equivalent → `workers_steropes`; proto imports → generated `org.tatrman.worker.v1` + `org.tatrman.plan.v1` packages (from Stage 1.2's PY codegen). `just test-py steropes` green; ruff + mypy clean.
  *Done 2026-06-15: forked `workers/polars` → `workers/steropes`. Python package `workers_polars` → `workers_steropes` (dir + imports + `WorkersPolarsConfig`→`WorkersSteropesConfig`); pyproject name `workers-steropes`; HOCON namespace `workers-polars`→`workers-steropes`; env `POLARS_WORKER_*`/`WORKERS_POLARS_CONFIG` → `STEROPES_*`/`WORKERS_STEROPES_CONFIG`; ports 7232/7132 → **gRPC 7301 / HTTP 7300**. Proto imports remapped: `cz.dfpartner.worker.v1`→`org.tatrman.worker.v1`, `cz.dfpartner.plan.v1`→`org.tatrman.plan.v1`, and `cz.dfpartner.metadata.v1.metadata_pb2` split into `org.tatrman.ariadne.v1.ariadne_pb2` (OverallStatus.OK) + `org.tatrman.kantheon.common.v1.response_message_pb2` (ResponseMessage/ERROR); gRPC registration name → `org.tatrman.worker.v1.WorkerService`. engine-name stays `polars` (engine identity, like Brontes keeps `mssql`). Unused `metadata` config block repointed to Ariadne (7261); Dockerfile paths/ports + catalog-info (`name: steropes`, `system: kantheon`) updated; README provenance header. `just test-py workers/steropes` green (44 base tests; one config-port assertion updated 7232→7301), ruff clean. (No mypy in the source's dev-deps — same as ai-platform; ruff is the configured linter.)*

- [x] **T2 — Fingerprint cross-check fixtures (test first).**
  The schema-fingerprint algorithm (`_schema_fingerprint` in the workspace/grpc code) must agree across implementations (Steropes now; Charon `Integrity.kt` and Metis `arrow_io.py` when they land). Create `shared/testdata/fingerprints/` with N reference Arrow IPC files + a `fingerprints.json` of expected values **generated by the forked Steropes implementation at the fork point**. Add a Steropes pytest that recomputes against the fixtures. Note in `charon/architecture.md` §9 + `metis/architecture.md` that their cross-check tests consume this fixture set (the "same algorithm, multiple implementations, must agree" CI pin).
  *Done 2026-06-15 — **with a contract correction (Bora decision: "full unify", AskUserQuestion)**: the task's premise that `_schema_fingerprint` (raw pyarrow IPC bytes) is the cross-engine algorithm was stale — Charon P1 (review-006 R3) had already shown raw IPC bytes are NOT byte-stable across Arrow impls and switched to a **canonical, implementation-independent** logical-schema digest. So the unification target is Charon's canonical algorithm, and BOTH workers adopt it: **Steropes** `_schema_fingerprint` now delegates to a new `workers_steropes/fingerprint.py` (canonical), and **Brontes** `ArrowIpcSerializer.fingerprintFor` was migrated off `serializeAsMessage()` raw bytes to the same canonical algorithm. `shared/testdata/fingerprints/` holds `generate.py` + 4 Arrow IPC fixtures (reference/scalars/list/map) + `fingerprints.json` + `canonical.txt`; the `reference.arrow` digest is **`69779ea6…` — byte-identical to Charon's existing `IntegritySpec` anchor**, proving the algorithm matches Charon. Cross-checks: Steropes `test_fingerprint.py` (6) + Brontes `SchemaFingerprintCrossEngineSpec` (5) both recompute against the shared fixtures and pass — incl. **map** (the Stream B note): the cross-engine form is the **entries-wrapped `{key,value}` struct** (what Arrow Java exposes); the Python generator synthesizes it from pyarrow's key_field/item_field so Arrow Java reading the same bytes agrees. **Follow-up flagged (Stream B):** Charon's `fixtures/integrity/regenerate.py` `children(map)` still uses the flat form — recorded in `kantheon-v1.1.md` + `charon/contracts.md` §6 (latent only; Charon has no map fixture). Metis cross-check note deferred to Metis arc (Metis not yet in-repo).*

- [x] **T3 — Workspace semantics suite.**
  The forked workspace tests (session keying, idle-TTL sweeper, size caps, `assign_to_workspace` flow) green unmodified. These are the patterns Metis ports — flag any local deviation discovered during the fork in the module README so Metis Phase 1 inherits the corrected version.
  *Done 2026-06-15: `tests/test_workspace.py` + `test_execute_rpc.py` (session keying `(session_id, df_name)`, mock-clock idle-TTL eviction, max-sessions/max-dfs-per-session/max-bytes-per-df/max-total-bytes caps, `assign_to_workspace` → `WorkspaceRef` read-back) all green unmodified. **No deviation from the ai-platform source found at the fork point** — recorded in `workers/steropes/README.md` ("the pattern Metis ports") so Metis Phase 1 inherits this version as-is.*

- [x] **T4 — k8s + dispatch component test.**
  Per the testing policy (planning-conventions.md §4): mocked unit tests only; integration suite is separate. `k8s/{base,overlays/local}` (Dockerfile-based deploy via the Python lane recipe). Deploy; probes green. Enable the `steropes` capability block in Kyklop's HOCON (commented placeholder from 3.3 T2); mocked component test (Kyklop with an in-process/mocked Steropes worker): Kyklop routes a DataFrame-capable fixture plan to Steropes; Arrow IPC streams back; `supports_stateful_sessions=true` path exercised (stage a DF into a session, second plan reads it). (True on-K3s dispatch integration deferred to the separate integration-test suite.)
  *Done 2026-06-15: `workers/steropes/k8s/{base,overlays/local}` — Deployment (`steropes:dev`, HTTP 7300 + gRPC 7301, FastAPI `/ready`+`/health` probes) + Service; local overlay `imagePullPolicy: Never` + `OTEL_SDK_DISABLED`. Renders clean via `kubectl kustomize`. **Kyklop HOCON**: the commented Steropes slot (3.3 T2) is now active (`KYKLOP_WORKER_STEROPES_ENDPOINT`, role-hint `polars`); Kyklop's k8s base adds `KYKLOP_WORKER_STEROPES_ENDPOINT=steropes:7301`. **Component test** `KyklopSteropesDispatchComponentSpec` (Kotlin, in-process stateful worker): (1) a WorkspaceRef plan + session_id routes to Steropes (engine `polars`, `supports_stateful_sessions=true`, no supported_connections) and the Arrow frame + fingerprint stream back intact with the `routing_decision` warning; (2) the stage-then-read flow across two stateful workers — same `session_id` sticky-pins to ONE worker for both dispatches (asserts served counts `[0, 2]`). Live deploy → integration track (no cluster/Docker).*

- [x] **T5 — Charon re-point.**
  Contracts edit (always): `docs/architecture/charon/contracts.md` + `architecture.md` — WorkerEndpoint targets Steropes (`org.tatrman.worker.v1`, in-repo service DNS `steropes:7301`); "ai-platform workers/polars" references replaced; the Phase-3-pre-flight "scan-plan read-out verification" now verifies against Steropes. Code edit (only if `services/charon` exists by now): swap its worker channel config + run its WorkerEndpoint tests against a mocked Steropes worker client at component level (real on-K3s Steropes verification deferred to the separate integration-test suite).
  *Done 2026-06-15: **docs re-pointed** — `charon/architecture.md` (mermaid node `ai-platform workers/polars` → `steropes (workers/steropes)`; Phase-3 "live Polars Worker" → "live Steropes" `steropes:7301`; the scan-plan read-out pre-flight + `ReadWorkspace` fallback now against in-repo Steropes; verified-surfaces list → in-repo `org.tatrman.worker.v1` proto + `workers/steropes` + `shared/testdata/fingerprints/`) and `charon/contracts.md` (WorkerSessionDf keying comment → workers/steropes; the fingerprint §6 now names Charon+Brontes+Steropes + the shared fixture pin + the entries-wrapped map rule + the regenerate.py follow-up; config key `charon.worker.polars` → `charon.worker.steropes` `steropes:7301`). **Code:** Charon's WorkerEndpoint is still stubbed (`NotYetImplemented`, wired at Charon's own Stage 3.1) — there is no worker channel config to swap yet, so the only Charon code touch is two now-accurate comments (`application.conf` chunk-rows note + `Integrity.kt` cross-engine note pointing at the in-repo Steropes/Brontes impls). The WorkerEndpoint↔mocked-Steropes component test lands when Charon implements WorkerEndpoint (Charon Stage 3.1); flagged there.*

- [x] **T6 — Stage exit.**
  `just test-all && just lint-all && just test-py steropes`. Tag `steropes/v0.1.0`. Check Stage 3.4 in [`tasks.md`](./tasks.md).
  *Done 2026-06-15: `just test-all` ✓, `just lint-all` ✓ (ktlintFormat applied to kyklop+brontes), `just test-py workers/steropes` ✓ (50), ruff clean, all kustomize overlays render (steropes/kyklop/brontes + deployment/local), proto-verify ✓. New test totals: Steropes 50, Brontes 55 (+5 cross-engine), Kyklop 58 (+2 Steropes component). DONE-grep `rg -i "cz\.dfpartner|workers_polars|workers-polars" workers/steropes/src workers/steropes/tests` → clean. Also fixed an unrelated merge artifact on main: the Charon merge had re-introduced a dangling `:workers:_smoke-worker` include (the module 3.3 deleted) — removed the include + stale dir. **Tag `steropes/v0.1.0` ready but NOT pushed** (coordinated at merge — tracker rule). Stage 3.4 ticked in `tasks.md`. **Not merged** (tracker rule).*

**DONE means:** both Kyklops serve `WorkerService` on K3s; fingerprint fixtures pinned for all future implementations; Charon's docs (and code, if present) point in-repo.
