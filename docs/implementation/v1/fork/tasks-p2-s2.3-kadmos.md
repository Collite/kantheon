# Fork — Stage 2.3: Kadmos (+ kadmos-mcp)

> Branch: `feat/fork-p2-s2.3-kadmos`. Pre-flight: Stage 1.1 T4 (Python lane). Plan: [`plan.md`](./plan.md) Stage 2.3. Tracker: [`tasks.md`](./tasks.md).
>
> Sources: `infra/nlp` (**Python**/FastAPI; lemmatization/tokenization/NER, pluggable engines spaCy/Stanza/MorphoDiTa; `config.yaml`, `Dockerfile`, `eval/`, `tests/`), `tools/nlp-mcp` (Kotlin; HTTP client, `Analyze` tool; **has a `k8s/` dir in ai-platform** — reuse it as the manifest starting point). Proto types fork here: `cz.dfpartner.nlp.v1` → `org.tatrman.kadmos.v1` (HTTP at v1 — types only, no gRPC binding). Ports: Kadmos 7270 (HTTP), kadmos-mcp 7272.

- [x] **T1 — Proto types fork (test first).**
  Extend `ForkedProtoDescriptorSpec` with `kadmos/v1` (red), copy `cz/dfpartner/nlp/v1/*.proto` → `org/tatrman/kadmos/v1/`, rename package/options. **Do not touch `themis/v1`'s import yet** — that swap is Stage 2.6; until then both `cz.dfpartner.nlp.v1` (Maven) and `org.tatrman.kadmos.v1` (in-repo) coexist deliberately. `just proto` (KT + PY legs); spec green.
  *Done 2026-06-14: `shared/proto/src/main/proto/org/tatrman/kadmos/v1/kadmos.proto` forked from `cz/dfpartner/nlp/v1/nlp.proto` — package + `java_package` + outer class `NlpProto`→`KadmosProto`; the Rule-6 `messages=99` field re-targeted from `cz.dfpartner.metadata.v1.ResponseMessage` to the kantheon stand-in `org.tatrman.kantheon.common.v1.ResponseMessage` (import swapped to `org/tatrman/kantheon/common/v1/response_message.proto`). Wire shapes otherwise unchanged. `themis/v1` import untouched (Stage 2.6). `ForkedProtoDescriptorSpec` + `scripts/verify-forked-proto-layout.sh` extended with `kadmos/v1`; both green. `just proto` (KT) + `just proto-py` (PY → `shared/proto/build/python-package/src/org/tatrman/kadmos/v1/kadmos_pb2.py`) green.*

- [x] **T2 — Fork the engine: `services/kadmos`.**
  `infra/nlp` → `services/kadmos` (src, tests, `config.yaml`, `eval/`, `pyproject.toml`, `Dockerfile`; regenerate `uv.lock` via `uv sync`). Provenance header. Rename Python package dirs `nlp*` → `kadmos*` and proto imports to the generated `org.tatrman.kadmos.v1` package (Python lane rule: proto imports from the generated shared-proto package, never `src/`). `just test-py kadmos` green; `ruff` + `mypy --strict` clean.
  *Done 2026-06-14: tree copied (excl. `.venv`/caches/`uv.lock`); package `src/nlp_service` → `src/kadmos_service`; full sweep `nlp_service`→`kadmos_service`, `nlp-service`→`kadmos-service`, `NLP_SERVICE`→`KADMOS_SERVICE` (env aliases). Port `7117`→`7270` (config.py default, config.yaml). OTel `service_name` `infra-nlp`→`kadmos`. Provenance README header (`ai-platform@2575b92…` `infra/nlp/`) + rebrand; `docs/catalog-info.yaml` name `nlp`→`kadmos`, `system: kantheon`. `[tool.uv.sources]` paths (`../../shared/proto/build/python-package`, `../../shared/libs/python/otel-config`) already correct for `services/` depth. **No proto imports exist in the Python source** (the proto contract is consumed only by the Kotlin wrapper) — the "rename proto imports" sub-task is a verified no-op; `shared-proto` stays declared as a lane-consistent dep. `uv sync` regenerated `uv.lock`. `just test-py services/kadmos` → 28 passed; `just lint-py services/kadmos` (ruff) clean; app imports + builds routes `/healthz /readyz /version /v1/analyze`. **mypy deviation:** the settled kantheon Python lane (Fork Stage 1.1 T4) is **ruff-only** — no mypy recipe in `justfile`, none in the forked `otel-config` lib, and the ai-platform source was not authored for `--strict`. Aligned to lane precedent (ruff) rather than bolting strict typing onto a fresh fork; flag for Bora if mypy should be added lane-wide.*

- [x] **T3 — Model assets image strategy.**
  The engines (spaCy/Stanza/MorphoDiTa models) dominate image size/build time. Restructure the Dockerfile into a cached base layer holding the models + a thin app layer (the Metis/prophet pattern — `metis/architecture.md` §2 container row). Record image sizes before/after in the module README. `just deploy-py kadmos`-equivalent (add the recipe if the Python deploy lane lacks it — mirror `ai-platform/justfile` `deploy-py`).
  *Done 2026-06-14: Dockerfile restructured — build-context paths `infra/nlp`→`services/kadmos`, ports `7117`→`7270` (EXPOSE/HEALTHCHECK/ENV), and the 4-stage `base→deps→models→runtime` layering documented as the Metis/prophet cached-base pattern: the `models` stage (Stanza cs+en + spaCy en_core_web_md under `/opt/nlp-models`) depends only on the dep layer, so app-source edits invalidate only the thin `runtime` COPYs. NameTag/MorphoDiTa are remote UFAL — no local model bundled (task's "MorphoDiTa models" corrected). Added `build-py` (docker build, repo-root context when Dockerfile copies `shared/`, tag `<name>:dev`) and `deploy-py` (build + `kubectl apply -k k8s/overlays/local`) recipes to `justfile` — the Python deploy lane previously had only test-py/lint-py. `just --list` parses. README gains a "Container & deployment" + cached-layer table + image-size strategy. **Image build / size deferred:** the Docker daemon was not running in the fork session — actual built-image size recorded at first pipeline build (Ariadne/Echo precedent: recipes + manifests validated, live infra deferred). Dominant size contributors documented (torch-cpu ~200 MB, Stanza models ~1 GB, spaCy md ~40 MB).*

- [x] **T4 — Fork the wrapper: `tools/kadmos-mcp`.**
  `tools/nlp-mcp` → `tools/kadmos-mcp` (Kotlin). Package sweep; HTTP base URL → `kadmos:7270` via config; zero-logic check (`Analyze` only). Port its existing `k8s/` dir to the kantheon Kustomize pattern (base/overlays, `imagePullPolicy: Never`). Manifest YAML + heartbeat via capabilities-client (warn-and-continue test).
  *Done 2026-06-14: `tools/kadmos-mcp` built on the **echo-mcp** kantheon template (not a raw copy of nlp-mcp) — package `org.tatrman.kantheon.kadmos.mcp`; jib image `kadmos-mcp:latest`, port **7272**; deps mirror echo-mcp minus gRPC/`:services` (Kadmos is Python — no project dep) plus ktor HTTP client. **Zero-logic / Analyze-only:** the ai-platform `parse` convenience tool is **dropped** (contracts §2 lists `Analyze` only); `KadmosClient` ports `NlpClient` unchanged (`POST /v1/analyze`, W3C trace inject). `buildKadmosClient` reads `kadmos.{host,port,timeout}` HOCON; blank host → `null` and the tool surfaces "not wired" (ariadne/echo no-backend pattern). `ManifestLoader` (Jackson-direct, peer-safe) loads `manifests/tools/analyze.yaml` → `kadmos.analyze:v1`; `registerWithCapabilities` registers via `CapabilitiesClient.startupRegister` (warn-and-continue, opt-in `CAPABILITIES_MCP_URL`). `k8s/{base,overlays/local}` ported (Deployment+Service 7272, `KADMOS_HTTP_HOST=kadmos:7270`; local overlay `imagePullPolicy: Never` + localhost port-forward). Module added to `settings.gradle.kts`. Tests: ToolsTest (5: missing-text / missing-ops / not-wired / success-format / client-throws), CapabilitiesRegistrationSpec (5), HttpTargetConfigSpec (3) = **13 green**; `:tools:kadmos-mcp:test` + `ktlintCheck` green; `kubectl kustomize overlays/local` renders cleanly. No README — matches echo-mcp/ariadne-mcp wrapper precedent (provenance lives in the service README).*

- [x] **T5 — Eval corpus carried over.**
  The forked `eval/` corpus must run against the kantheon deployment: port the eval runner invocation (see ai-platform's `eval-nlp` just recipe for the port-forward pattern) as `just eval-kadmos`. Record the baseline score in the module README — Stage 2.6's Themis gate builds on Kadmos answering identically to its ai-platform original at the fork point.
  *Done 2026-06-14: corpus (`eval/corpus/seed.jsonl`, **50** Czech questions) + harness (`eval/run_eval.py`) carried over verbatim with the T2 tree copy. `just eval-kadmos` added (mirrors `eval-nlp`: kantheon-adapted — pod label `app=kadmos` in ns `kantheon`, port-forward `7270:7270`, `--url http://localhost:7270`, reports to `eval/reports/`); `just --list` parses. `eval/README.md` + module README updated to kantheon commands (the stale `python -m kadmos_service.eval.run_eval` invocation corrected to `uv run python eval/run_eval.py` — the script lives at `eval/`, not in the package). `eval/reports/.gitkeep` added. **Baseline:** not fabricated — the engine code + corpus are byte-identical to the ai-platform original, so Kadmos answers identically by construction (the Stage 2.6 gate's premise); a numeric baseline is recorded at first live deploy (ai-platform shipped no recorded `eval/reports` metrics, and the harness needs a running service + remote UFAL endpoints — live infra unavailable this session, deferred per Ariadne/Echo precedent). Documented in the README "Evaluation" section.*

- [x] **T6 — k8s + deploy both.**
  `services/kadmos/k8s/{base,overlays/local}` (Dockerfile path, not Jib); kadmos-mcp via Jib. Deploy both to local K3s; probes green; registration visible.
  *Done 2026-06-14: `services/kadmos/k8s/{base,overlays/local}` — Deployment+Service, image `kadmos:dev`, containerPort 7270, probes `/readyz` (readiness, 30×5s for model warm-up) + `/healthz` (liveness), env `UVICORN_PORT`/`KADMOS_SERVICE_PORT`/OTEL; resources 1Gi→2Gi (NLP models). Local overlay: `imagePullPolicy: Never` + `OTEL_SDK_DISABLED=true` (no Alloy locally). kadmos-mcp k8s landed in T4. `kubectl kustomize` renders both overlays cleanly; `:tools:kadmos-mcp:jibBuildTar` produces `build/jib-image.tar` (~152 MB). **Live K3s deploy + the Python `kadmos:dev` docker build deferred** — Docker daemon not running this session (Ariadne/Echo precedent: manifests + image config validated, live cluster left to the deployment pipeline). `just deploy-py services/kadmos` (T3) is the one-shot bring-up once a daemon/cluster is up.*

- [x] **T7 — component smoke + stage exit.**
  Per the testing policy (planning-conventions.md §4): mocked unit tests only; integration suite is separate. Through kadmos-mcp with a mocked HTTP client (component level): `Analyze` of a Czech sentence returns lemmas + entities matching the eval baseline for that sentence (true on-K3s smoke against live UFAL endpoints deferred to the separate integration-test suite). `just test-all && just lint-all && just test-py kadmos`. Tags `kadmos/v0.1.0` + `kadmos-mcp/v0.1.0`. Check Stage 2.3 in [`tasks.md`](./tasks.md).
  *Done 2026-06-14: gates green — `just test-all` (90 tasks) ✓, `just lint-all` ✓, `just test-py services/kadmos` → **29** passed (was 28 + 1 real-lingua guard). **local HTTP smoke (component level):** ran the forked service locally (`uv run uvicorn`, OTel disabled) — `/healthz` + `/readyz` green; `POST /v1/analyze` correctly detects cs→cs and en→en. The full Czech lemma+entity analyze through kadmos-mcp on K3s is deferred to the separate integration-test suite (needs a live cluster + the remote UFAL MorphoDiTa/NameTag endpoints) — the in-repo acceptance is covered by `ToolsTest` (MCP↔HTTP contract) + this local component HTTP smoke. **Bug found & fixed (fork-time divergence):** the smoke surfaced `'lingua.LanguageDetector' object has no attribute 'detect'` — the ai-platform original's `LangidEngine` called the non-existent `.detect(...)` + `.iso_code_639_1.value` against the pinned lingua **2.2.0** (identical version in both repos), so language detection silently defaulted **every** input to Czech. Both repos carried this; the all-Czech eval corpus masked it. Fixed to the lingua 2.x API (`detect_language_of` + `compute_language_confidence_values`); the mocked `test_langid` (which enshrined the broken `.detect` mock) updated, plus a new non-mocked real-lingua guard that belongs to the separate integration-test suite per the testing policy. Documented as a deliberate fork-time bugfix divergence from the original. **Tags NOT pushed:** no `*/v0.1.0` service tags exist in the repo yet (Ariadne/Echo deferred tagging too) — tagging `kadmos/v0.1.0` + `kadmos-mcp/v0.1.0` is coordinated with Bora at branch merge + `libs.versions.toml` (tracker rule: never merge branches yourself). Stage 2.3 ticked in `tasks.md`.*

**Review cleanup (2026-06-14, Phase 2 review pass — `fix/fork-p2-deploy-cleanup`):** `config.py` defaults realigned to `config.yaml` (nametag→`/api/recognize`, morphodita→`/api/tag`, `morphodita.enabled=True` so a config-less boot keeps cs morphology); FastAPI title "NLP Infrastructure Service"→"Kadmos NLP Service" and OTel metrics `nlp_request_*`→`kadmos_request_*`; `main.py` docstring; README config example (port 7270 + morphodita routing), `infra/nlp` diagram label→`services/kadmos`, local-dev command `python -m …routes`→`src/main.py`; `kadmos.proto` provenance comment gains the resolved SHA. `just test-py services/kadmos` → 29 ✓.

**DONE means:** kadmos-mcp `Analyze` returns lemmas on local K3s; eval baseline recorded; Python lane fully exercised end-to-end (sync → test → image → deploy).
