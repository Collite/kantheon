# Fork — Stage 3.2: Argos II — the policy fold

> Branch: `feat/fork-p3-s3.2-argos-fold`. Pre-flight: Stage 3.1. Plan: [`plan.md`](./plan.md) Stage 3.2. Tracker: [`tasks.md`](./tasks.md).
>
> Source: `infra/sql-security` (Kotlin/Ktor; policy evaluation for RLS + column masking; HOCON policy storage; dual endpoints — **v1 RelNode-based comes, legacy SQL-fragment stays behind**). **This is where the whois client actually lives** (`infra/sql-security/.../client/WhoisClient.kt`) — Stage 3.1 was a mechanical validator fork (the validator never had whois; roles arrive in `PipelineContext.auth_roles` from the theseus-mcp edge). So the whois-deletion + bearer-roles default (contracts §3 `roleSource = bearer`) lands **here**: when sql-security folds in, drop its `WhoisClient`/`whois-common` and key policy evaluation on the bearer roles passed in from Argos's context. The `SecurityService` gRPC surface is **not** re-exposed — evaluation becomes in-process inside Argos (contracts §1).

- [x] **T1 — Suite first: the union.**
  Copy sql-security's v1-endpoint test suite + policy fixtures into `services/argos/src/test/` (keep their package separation under `org.tatrman.kantheon.argos.policy.*`). Adapt the call surface in tests from gRPC stubs to the in-process API — define that API signature now.
  *Done 2026-06-14: ported the 4 v1 specs into `…/argos/policy/` — `PolicyEngineSpec` (was SecurityServiceImplSpec), `SecurityColumnRulesSpec`, `PolicyConfigLoaderSpec`, `SecurityMetadataAwareSpec` — renamed off `cz.dfpartner.*` (plan→org.tatrman.plan, security→org.tatrman.security, metadata ResponseMessage/Severity→common). In-process API defined as `PolicyEngine.evaluatePolicies(EvaluatePoliciesRequest): EvaluatePoliciesResponse` (roles read from `context.auth_roles`; metadata via injectable `PolicyMetadataClient`). The whois-enrichment + legacy + opa + check-handler specs were **dropped** (those paths don't fork); replaced the whois spec with `PolicyRolesSpec` (bearer roles drive a roles-gated policy; absent roles → skipped, no whois fallback).*

- [x] **T2 — Fold the engine.**
  Copy sql-security's v1 RelNode policy-evaluation sources into `services/argos/src/main/kotlin/org/tatrman/kantheon/argos/policy/`; package sweep; delete: the gRPC service layer, the legacy SQL-fragment endpoints, **and sql-security's whois client + `whois-common` dep** (bearer-roles change — roles from `PipelineContext.auth_roles`; default `roleSource = bearer`; whois enrichment is Phase-5 additive). T1 suite green.
  *Done 2026-06-14: copied PolicyRegistry, PolicyConfigLoader, PolicyPredicate (+Policy/PolicyValue/ColumnRule/TableMatcher), PolicyToExpression (+ResolvedIdentity), and the metadata client (renamed `PolicyMetadataClient`/`GrpcPolicyMetadataClient`/`StaticPolicyMetadataClient`, mapped metadata→ariadne) into `…/argos/policy`. New `PolicyEngine` lifts `SecurityServiceImpl.evaluatePolicies`; `resolveIdentity` reads roles from `context.auth_roles` (no whois). **Never copied** (dropped): `SecurityServiceImpl` gRPC layer, `PolicyToSqlFragment`, `EvaluatePoliciesLegacy`, `WhoisClient`, `OpaClient`/`OpaModels`, `SecurityCheckHandler`/`SecurityRoutes`, the erp-sql-common `SecurityModels` — so no `whois-common`/`erp-sql-common` dep is pulled. T1 suite green.*

- [x] **T3 — Replace the remote call.**
  *Done 2026-06-14: `GrpcSecurityClient` deleted; `SecurityClient` interface kept; new `LocalPolicyClient(PolicyEngine)` is the production wiring (SecurityApplier/ArgosServiceImpl unchanged — they still depend only on the `SecurityClient` SAM). Application.kt builds `PolicyEngine(PolicyRegistry(PolicyConfigLoader.load(config)), GrpcPolicyMetadataClient(ariadne))` wrapped in LocalPolicyClient; the `security {}` config block + the `security.required` readiness flag (the 3.1 transitional gate) removed; the dependency monitor's sql-security probe removed (Ariadne-only). Validator-core suite (mocked SecurityClient) + policy suite both green in one module. **In-process component spec** `ArgosPolicyFoldComponentSpec`: a fixture plan flows validate → in-process policy-evaluate → tenant-predicate inject in one process (asserts `tenant_isolation` audit + the bare scan is wrapped).*

- [x] **T4 — HOCON policy store.**
  *Done 2026-06-14: `src/main/resources/policies/policies.conf` holds `argos.policies` (default `tenant_isolation`), included by `application.conf` (`include classpath("policies/policies.conf")`); `PolicyConfigLoader` POLICIES_PATH → `argos.policies`. External override: `ARGOS_POLICIES_FILE` (Application.loadArgosConfig parses the file over the base — k8s ConfigMap mount, no rebuild). README documents the Git-workflow authoring rule + the override. `PolicyConfigLoaderSpec`'s bundled-config test now asserts the default policy loads (was size 0 in sql-security).*

- [x] **T5 — Hot-path latency check.**
  *Done 2026-06-14: `PolicyLatencySpec` — a timed Kotest spec (100 warmup + 300 samples) asserts in-process `evaluatePolicies` p50 < **2 ms** for a single-table plan against the default policy set. In practice it's microsecond-scale (no gRPC round-trip), comfortably under the in-cluster network hop the fold removed. Numbers + bound recorded in the README "Performance" note.*

- [x] **T6 — Provenance + stage exit.**
  *Done 2026-06-14: README provenance gains the second source line (`infra/sql-security@2575b923…`); k8s manifests cleaned (obsolete `ARGOS_SECURITY_REQUIRED` env dropped — readiness is Ariadne-only now; local overlay still boots fixtures Ready). `just lint-all` ✓, `just test-all` ✓ (no OOM), proto-verify ✓, `kubectl kustomize` base+local render clean (live deploy → integration track, no cluster/Docker). **Tag `argos/v0.1.0` ready but NOT pushed** (coordinated with Bora at merge — tracker rule). Stage 3.2 ticked in `tasks.md`. DONE-grep `rg -i "sql-fragment|legacy" services/argos/src/main` → clean (only the data-contract proto in shared/proto documents the dropped legacy in comments).*

**DONE means:** one pod, a hundred eyes — validator + policy engine in-process, both forked suites green, legacy endpoints provably absent (`rg -i "sql-fragment|legacy" services/argos/src/main` clean).
