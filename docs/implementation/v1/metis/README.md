# Metis — v1 Implementation

Arc planned 2026-06-12. Pairs with the Charon arc (Charon Stage 3.1 needs Metis Phase 1 live); **`metis/v0.3.0` + `metis-mcp/v0.1.0` gate Pythia Phase 4 Stage 4.2** (forecast/simulation).

| File | What |
|---|---|
| [`plan.md`](./plan.md) | 3 phases × 7 stages (~42 tasks): proto + workspace + kantheon-Python conventions → models + numerical golden suite (LINEAR / ARIMA auto-order / PROPHET, diagnose, project, simulate) → Charon interop + `metis-mcp` + registration + bench. |

Per-stage task lists land here as `tasks-p<n>-s<n.m>-*.md` after plan review. Architecture + contracts: [`../../../architecture/metis/`](../../../architecture/metis/). Up: [`../README.md`](../README.md).
