# WS-C Stage 2 — Integration contexts (incl. `tpcds-query`) + run-set

> **Workstream C (Test suite), Stage 2.** Build the integration-tier contexts: the new **`tpcds-query`** showcase (Goals 2+4), finish the existing draft contexts, and assemble the run-set that runs green via `infra-up --kube dsk` (**MP-4**).
>
> **Reads with.** [`tasks-overview.md`](./tasks-overview.md) §0, [`../../../architecture/deploy-test/contracts.md`](../../../architecture/deploy-test/contracts.md) §6 (integration contract) + §7 (bp-dsk), [`tasks-r1-bp-dsk-run-mode.md`](./tasks-r1-bp-dsk-run-mode.md), [`../testing/plan.md`](../testing/plan.md) §3.1 (existing contexts).
> **Reference.** olymp `test-contexts/theseus-runquery/` (the live, complete example) + `golem-erp/` (draft). kantheon `RunQueryIntegrationSpec` + `@RequiresContext` + `ContextNameRegistrySpec`. Repos: **[K]** kantheon specs+fixtures · **[O]** olymp `test-contexts/`.

## Goal

The run-set — `theseus-runquery` (✓), **`tpcds-query`** (new), `golem-erp`, `themis-routing`, `pythia-rca` — runs green on bp-dsk via `infra-up --kube dsk`; nightly (bp-olymp01) stays green too.

## Pre-flight

- [ ] WS-R1 DONE (`--kube dsk` + reconcile-boundary verified).
- [ ] WS-T2 DONE (TPC-DS model + `pg-tpcds` + Proteus unparse) for the `tpcds-query` context.
- [ ] Query-path services have charts (D2) + are deployable; `test-pg` reachable (T1).
- [ ] Branch `feat/c2-integration-contexts` in **both** repos.

## Tasks

- [x] **T1 [K] — `TpcdsQueryIntegrationSpec` (tests first).** ✅ Done. `@RequiresContext("tpcds-query")` drives the 4 curated shapes through theseus-mcp `query`, asserting the SF1 oracle (12/30/30/3), `{year}`→2002, no-RLS analyst bearer; `assertOk()` surfaces the error envelope on failure. `ContextNameRegistrySpec` auto-scans the name (no manual list). Also fixed `McpQueryDriver` to disable the CIO 15s engine timeout + govern the call with a 3-min MCP `RequestOptions` timeout (heavy SF1 first query).
- [x] **T2 [O] — `tpcds-query` context.** ✅ Done (olymp `feat/c2-integration-contexts`). `test-contexts/tpcds-query/` — 7 services helm-installed, pointed at the **STANDING** test-pg (no per-run warehouse, no mssql/wiremock); Arges restates Proteus + pg-tpcds host/user/password; 6 services inherit chart-default wiring (MP-2's config). The run-ns cred is solved by extending the `pg-tpcds-ro` ClusterExternalSecret namespaceSelectors to match `olymp.collite/managed-by=test-harness`.
- [x] **T3 [K/O] — Run `tpcds-query` on bp-dsk.** ✅ **GREEN on bp-dsk 2026-07-07** — `just it-bp-dsk tpcds-query`: 4 tests, 0 skipped, 0 failures; the 4 curated shapes return the exact SF1 oracle (12/30/30/3) through the full Postgres-worker chain. The Goals-2+4 convergence demo. *(Cold-start note: the first query takes ~20s (Calcite cold-compile + cold Arges pool); one earlier run hit a first-query transient — add a warmup/retry if it recurs. k3d-local leg not run — the bp-dsk green is the deliverable.)*
- [x] **T4 [K/O] — `golem-erp` (fixture agent-showcase).** ✅ **FULLY GREEN on bp-dsk 2026-07-08** — all 3 assertions pass: 401 (missing bearer) + 403 (outsider) PD-8 admission **and** the answer-turn (`STATUS_DONE` render envelope) through the LIVE LLM roundtrip golem→Prometheus→WireMock→render. **The prometheus→WireMock Anthropic roundtrip is now confirmed — unblocks every themis/pythia LLM tier.** Live-run fixes (all on `feat/c2-themis-routing`): Prometheus `/v1/chat` controller alias + `haiku`/`sonnet`/`claude-haiku` model aliases (findByName was exact → fell to the Azure default); `startupProbe`→readiness group + Redis health off (503 CrashLoop); golem `wait-for-ariadne` initContainer (boot race → permanent not-ready); WireMock stub loaded at runtime via `WireMockAdmin` (the in-cluster WireMock starts empty); answer-turn client timeout 3m; `it-bp-dsk` dumps service logs on failure; prometheus jib `-PimageRepo`/`-PimageTag`. Reframed to the **agent showcase** (Bora, 2026-07-08): the fixture query leg returns `detection_failed` not rows (`theseus-runquery` finding), so golem-erp proves the **Golem agent turn**, not real query data (that is `tpcds-query`'s job). **No image rebuilds** — the golem-erp Shem points at the existing bundled Ariadne `accounting` area, and is delivered as a per-run **ConfigMap** (new olymp harness `shems:` field → `<agent_id>-shem`, mounted via golem `shem.configMapName`; prompts fall back to the golem image classpath `prompts/cs/*`).
  - **Landed (kantheon):** `agents/golem/shems/golem-erp/` (Shem + prompts + README); `GolemErpBundleSpec` (green — Shem parses/assembles); `GolemErpIntegrationSpec` restructured to **two gates** — `contextLive=true` (401 missing-bearer + 403 outsider, PD-8 admission — robust) and `answerTurnLive=false` (the render-only LLM-planned `STATUS_DONE` turn); WireMock Anthropic `/v1/messages` → render-only MiniPlan stub.
  - **Landed (olymp):** `just-helper.py` Shem-ConfigMap support (`shems:` + `_apply_shem_configmap`, dry-run verified); `test-contexts/golem-erp/` trimmed to golem+ariadne+prometheus + `platform:[wiremock]` + `shems:[golem-erp]`; prometheus on the Spring `test` profile (H2 — no PG member) with the LLM base-url'd at WireMock. Helm-template + infra-up `--dry-run` both clean.
  - **Admission tier GREEN on bp-dsk 2026-07-08** — `just it-bp-dsk golem-erp` passes the two PD-8 admission assertions (401 missing-bearer + 403 outsider) through a real Golem-ERP pod (Shem via ConfigMap, prometheus on the H2 `test` profile). Fixes the live run forced, all landed: **golem `:testing` image published** (it never existed — only `0.1.0`); **prometheus `startupProbe`** added to the shared chart (the ~97s boot was killed by the 90s liveness budget → CrashLoop); node `fs.inotify` limits raised (fd starvation stalled image pulls). Standing golem flipped `0.1.0`→`:testing`. Dynamic per-Shem golem ApplicationSet (`bp-dsk-golems`) landed for prod multi-instance.
  - **Remaining (gated flip):** `answerTurnLive=true` + re-run to exercise the golem→prometheus→WireMock LLM roundtrip (Spring AI 2.0.0-M2 Anthropic wire shape / gateway path `/v1/chat/completions` vs `/api/v1/chat/completions` may need one live tweak). **No image builds needed** — the render-only MiniPlan stub is authored.
- [ ] **T5 [K/O] — `themis-routing` + `pythia-rca`.** Bring `themis-routing` green (real Kadmos/Echo); stand `pythia-rca` (minimal investigation DAG vs real Charon+Metis) — gated on those services being deployable (D2/D3). Specs first, then contexts, then local→bp-dsk.
  - **`themis-routing` ROBUST TIER GREEN on bp-dsk 2026-07-08 (`feat/c2-themis-routing`).** `just it-bp-dsk themis-routing` passes the MCP `resolve` smoke through a real themis-mcp pod → Kadmos + Echo + capabilities-mcp, LLM via Prometheus. **First in-cluster proof Themis runs:** it boots past its `assertRoutableAgentsAvailable` gate (capabilities-mcp's classpath seed manifests) and the graph terminates in a well-formed outcome, degrading gracefully on the stub-less LLM legs. Live-run fix: dropped a fragile `trace_id` assertion — Themis's MCP resolve only emits trace_id on the `resolved`/`refused` branches, not the `awaiting_clarification` degraded path; `isError=false` already proves Kadmos ran. Also published `capabilities-mcp:testing` + `themis-mcp:testing` (neither existed) and taught `just publish-image` an image-name override (`agents/themis` → image `themis-mcp`). Gated REST routing tier still off (`routingLive=false`). Mirrors golem-erp's two-tier shape:
    - **Robust tier (ACTIVE) — MCP `resolve` smoke.** `ThemisRoutingIntegrationSpec` drives the MCP `resolve` tool over StreamableHTTP against a real themis-mcp pod wired to real **Kadmos** (NLP) + **Echo** (fuzzy) + **capabilities-mcp** (agent registry), LLM upstream via **Prometheus**→WireMock. On the MCP edge Themis sets `routingEnabled=false`, and every LLM node (`classify`/`filter`/`joint`/`route`) degrades with a `.getOrElse` fallback (verified by code map), so the graph terminates in a well-formed `resolved`/`awaiting_clarification`/`refused` outcome **even with an empty WireMock** — never a crash. The smoke asserts `isError=false` + a recognized `outcome` + a non-blank `trace_id` (proves Kadmos ran). This is the **first end-to-end proof Themis boots in-cluster** (past its `assertRoutableAgentsAvailable` gate against capabilities-mcp — which serves its classpath seed manifests golem-erp/pythia/hebe) and runs the resolution graph. No bearer (MCP edge has no identity check).
    - **Routing tier (GATED `routingLive=false`) — REST `/v1/resolve`.** The only edge that computes routing (`Resolution.routing`). Asserts a procedural question routes to a concrete `chosen_agent_id`. Driver posts proto-canonical JSON via `JsonFormat`, mirroring iris-bff's `HttpThemisClient`. Enabling needs deterministic WireMock stubs for the `joint` (sonnet) + `route` (haiku) legs (author from the first run's Prometheus logs, the golem-erp method); it also first-exercises the REST edge's server-side proto-JSON receive.
    - **Landed (kantheon):** `agents/themis/build.gradle.kts` (integrationTest deps — harness, component-testkit, mcp-sdk, ktor client, shared:proto + protobuf.java.util for the REST tier); `src/integrationTest/.../integration/ThemisResolveDriver.kt` (MCP `resolve` + REST `resolveRest` + `unsignedJwt`); `ThemisRoutingIntegrationSpec.kt` (two tiers). Root build auto-provides the `integrationTest` suite; the `ContextNameRegistrySpec` drift guard binds `@RequiresContext("themis-routing")` to the olymp context.
    - **Landed (olymp):** `test-contexts/themis-routing/` promoted DRAFT→WIRED — prometheus on the Spring `test` profile (H2, no PG member — resolves the datastore TODO, golem-erp pattern); capabilities-mcp values note the classpath seed (no seed env needed); context.yaml status refreshed. `infra-up --dry-run` + `helm template` (themis-mcp, prometheus) all clean.
    - **Known risk (surface on first run):** Themis fail-fasts if capabilities-mcp isn't up at boot, then self-heals on restart (unlike golem's permanent cache-empty race) — a `wait-for-capabilities` initContainer is only needed if CrashLoopBackoff stalls the readiness gate.
  - **`pythia-rca` WIRED (2026-07-08, `feat/c2-themis-routing`) — awaiting first bp-dsk run.** Spec + driver + context all landed; compiles + ktlint green. Scoping correction: the current Pythia (v0.3.0, Phases 1–3) is a **SQL-only RCA investigator** — its clients are `GatewayClient` (LLM) + `TheseusQueryClient` only; **Charon/Metis are Phase 4, NOT built**, so pythia-rca does **not** depend on them (the D2/D3 gate was a stale premise). Mirrors golem-erp's two-tier shape:
    - **Robust tier (ACTIVE) — PD-8 admission.** Pythia's edge is **async** (`POST /v1/investigations` → 202 `{id,status}`, then `GET /v1/investigations/{id}`; no synchronous run-to-terminal edge), and every endpoint enforces PD-8 admission (auth/Admission.kt, a v1 structural bearer `Bearer <userId>[#roles]`). `PythiaRcaIntegrationSpec` asserts: a **missing bearer → 403 `unauthenticated`**, and a submitted investigation is **visible to its owner (200) but forbidden to another user (403 `forbidden`)** — which also proves the async submit persisted a record. This is the **first in-cluster proof of Pythia**: it boots **standalone** (the only fail-fast is DB migration — the context runs `PYTHIA_DB_ENABLED=false` → in-memory repos, `dbReady=true`; all downstream URLs blank → stubs; capabilities registration is warn-and-continue). No LLM, no query, no other service.
    - **Investigation tier (GATED `investigationLive=false`) — full loop to a terminal Status.** Submit (non-interactive `PLAN_APPROVAL_AUTO` HitlPolicy) → poll `GET` until DONE/FAILED/HALTED/INCONCLUSIVE. Reaching a *confident* DONE needs the SQL-only RCA chain (prometheus LLM + themis resolve + theseus-mcp queries) **and scripted WireMock LLM stubs** — Pythia's LLM legs (planner/synth STRONG, eval/render CHEAP) **do not degrade gracefully** (unlike themis), so an empty stub hard-fails the run. This is the pythia arc's own "live-LLM RCA run" (pythia plan.md Stage 3.3 T4, deferred to this suite). Expand the context + author the stubs, then flip it on.
    - **Landed (kantheon):** `agents/pythia/build.gradle.kts` (integrationTest deps); `src/integrationTest/.../integration/PythiaInvestigateDriver.kt` (submit + get + poll-to-terminal + `minimalRcaInvestigation` + `pythiaToken`; proto-JSON via JsonFormat) + `PythiaRcaIntegrationSpec.kt` (two tiers).
    - **Landed (olymp):** `test-contexts/pythia-rca/` — the leanest context (pythia alone, DB-less, no platform members). `helm template` + `infra-up --dry-run` clean.
    - **Known risk (surface on first run):** the async orchestrator advances on submit even in the robust tier — with blank downstream it should halt/park gracefully (record persists), but if an unhandled failure crashes the pod, the readiness gate would flag it (the golem-erp method: observe first, fix from logs).
- [ ] **T6 [K] — Re-enable `theseus-runquery` result asserts (RLS deferred).** ⏳ **Result path WIRED (2026-07-08, `feat/c2-themis-routing`) — awaiting Proteus `:testing` republish + bp-dsk run.** Scoping correction: the single `modelAlignedContext` flag conflated two paths with different prerequisites, now split:
  - **Result path (`resultAlignedContext=true`) — enabled.** Root cause of the original `detection_failed` was Proteus' fixture model lacking `dbo.sample_orders`. Added that table to `services/proteus/.../model/BootFixtureModel.kt` (`id/tenant_id/region/amount`, aligned with the mssql-init seed — which already exists: 4 rows incl. `t-alpha`). The raw-SQL `query` now resolves + unparses + runs end-to-end **through Brontes → MSSQL** (T1: 4 rows, expected columns, contains `t-alpha`). Independent value from `tpcds-query` — that proves the **Postgres/Arges** result path; this proves the **MSSQL/Brontes** one. `SurfaceType` has no DECIMAL, so `amount` is `FLOAT` (a bare projection never coerces → MSSQL returns the real DECIMAL(18,2) values). Proteus unit tests green; theseus-mcp integrationTest compiles + ktlint green. **Needs `just publish-image services/proteus` → `proteus:testing`, then `just it-bp-dsk theseus-runquery`.**
  - **RLS path (`rlsPolicyContext=false`) — deferred beyond C2.** Not testable in this context: `theseus-runquery` runs Argos with `ARGOS_USE_FIXTURE_MODEL=true`, whose fixture SecurityClient applies **no policies at all** (Argos `Application.kt`: "no row-level policies applied"). So neither the column-DENY the deny-case assumes nor row-level `tenant_isolation` is enforced (the earlier "fixture policy is tenant_isolation" note was wrong — that HOCON policy loads only in **non-fixture** mode, which needs an Ariadne metadata source). Real RLS is a richer Ariadne-backed context + role-based column rules (the engine's column rules are unconditional today) — a follow-up, not a C2 blocker.
- [ ] **T7 [K/O] — Assemble the run-set + bp-dsk full-run.** Define the bp-dsk on-demand run-set (all five contexts) + keep `nightly.txt` (bp-olymp01) authoritative for the scheduled nightly. Prove the **full set green on bp-dsk** via the `just it-bp-dsk` loop (sequential or namespace-isolated parallel). Document the runbook.

## DONE

- [x] `tpcds-query` green on bp-dsk (the four queries return correct SF1 results — 12/30/30/3, 2026-07-07). *(local k3d leg optional; not run.)*
- [ ] `golem-erp` / `themis-routing` / `pythia-rca` green; `theseus-runquery` result/RLS asserts re-enabled.
- [ ] Full run-set green on bp-dsk via `infra-up --kube dsk`; nightly on bp-olymp01 unaffected. **→ MP-4.**

## Follow-ups → next stage

- After MP-4: cut the deferred release tags (contracts §9) — the program finish line.
- Graduate the `iris-session` / `sysifos-workbench` deploy-smokes (testing S3.3/S3.4) to `@RequiresContext` nightly specs (optional, post-program).
