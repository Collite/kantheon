# Golem — v1 Implementation

Arc planned 2026-06-12. Runs after Iris Phase 2 (Phase 1 — `envelope-render` — may start any time; it is the landed form of the format-catalog Koog spike).

| File | What |
|---|---|
| [`plan.md`](./plan.md) | 4 phases × 12 stages (~70 tasks): envelope-render lib → template core → conversational surface + diff harness → Golem-ERP Shem + cutover (retires Python new-golem + the `/v2` adapter). |

Per-stage task lists land here as `tasks-p<n>-s<n.m>-*.md` after plan review. Architecture + contracts: [`../../../architecture/golem/`](../../../architecture/golem/). Up: [`../README.md`](../README.md).
