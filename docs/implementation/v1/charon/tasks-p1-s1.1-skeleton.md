# Charon — Stage 1.1: proto + module skeleton + legality matrix

> Branch: `feat/charon-p1-s1.1-skeleton`. Pre-flight: Fork P1 + P2 closed (the forked stack is live on `main`); `cz.dfpartner:shared-proto` still published, the one remaining consumer (Themis) is being retargeted in fork Stage 2.6. Plan: [`plan.md`](./plan.md) Stage 1.1.
>
> Goal: `org.tatrman.charon.v1` proto compiles; `services/charon` Kotlin module exists with Ktor probes + a gRPC server skeleton (RPCs stubbed `NOT_IMPLEMENTED`); the `MovePlanner` legality matrix is implemented and **table-driven from a single `Legality` matrix** the contracts §2 table derives from; request-validation suite green. Conventions for the first `services/` platform-grade module settled.
>
> **Per AGENTS.md §4 (Kotlin modules) + §4.1 (Python modules):** Charon is Kotlin, so the Kotlin module layout applies. Per `architecture.md` §2: Charon's Kotlin source root is `org.tatrman.kantheon.charon` (decision 2026-06-13; the `org.tatrman.<service>.v1` reservation is for **protos only**). Per §12.1: Charon was *not* forked from ai-platform, so **no provenance header**.
>
> Conventions: TDD (tests first, then impl, then ktlintFormat); every Kotlin file single-class; one route / one RPC / one move-pump piece per file; `messages = 99` Rule 6; `argsJson` Rule 7; `args_json` snake-cased in JSON wire but camelCase on proto field per contracts §1. The five gRPC operations land here as stubs; the full Arrow-pump body fills in Stages 1.2/1.3.

- [x] **T1 — Pin Arrow IPC, AWS SDK, Lettuce, grpc-kotlin versions in `gradle/libs.versions.toml`.**
  `arrow-vector` + `arrow-memory-netty` already pinned at `18.3.0` (used by data-formatter). **Note for Arrow 18.x:** the IPC classes (`ArrowStreamReader`/`Writer`, `ArrowFileReader`/`Writer`) ship INSIDE `arrow-vector` — no separate `arrow-ipc` artifact exists at this major (it was a separate jar pre-15.x and got merged upstream). So no new Arrow artifact is needed for the move pipe; the existing pin already covers it. New for Charon P1: `aws-sdk-s3:2.32.5` (Seaweed endpoint — SeaweedFS is S3-compatible, no custom protocol), `lettuce-core:6.8.0.RELEASE` (Redis binary client). **ADBC + arrow-jdbc are NOT pinned at Stage 1.1** — they land at P2 Stage 2.1 when the spike gate (T3) decides ADBC-JDBC vs plain JDBC + arrow-jdbc per dialect. Catalog drift is a planning bug (AGENTS.md §5), so the deferred decision gets its own catalog entry on its own stage, not here. *Done = `./gradlew help` clean; no module changes yet.*
- [x] **T2 — Write `org/tatrman/charon/v1/charon.proto`.**
  Mirror the contracts doc byte-for-byte (it's the source of truth; this proto is its machine form). Package `org.tatrman.charon.v1` (migrated-service convention per `AGENTS.md` §4 / architecture §2). Imports: `org/tatrman/kantheon/common/v1/response_message.proto` (kantheon Rule-6 stand-in; `cz.dfpartner.common.v1.ResponseMessage` after the proto coord resolves — see contracts §1 note). Per `shared/proto/build.gradle.kts` all `.proto` files in the tree are picked up; verify that the new path (`org/tatrman/charon/v1/charon.proto`) sits at the correct depth and codegens. The `python` builtin is also active in `:shared:proto` (Phase 1.2 T4), so a Python binding for `charon.v1` is also emitted — relevant for the Metis arc, not consumed in P1.
- [x] **T3 — Generate proto bindings; `just proto` clean.**
  `./gradlew :shared:proto:assemble --no-daemon` — Kotlin, Java, Python outputs. Verify `org.tatrman.charon.v1.CharonServiceGrpcKt` and the `CharonServiceGrpc` base class exist under `shared/proto/build/generated/sources/proto/main/`. No Kotest assertions yet — just that codegen runs without error. The second non-`kantheon.*` proto root is now live; same shape as `org.tatrman.ariadne` (Stage 1.2 precedent) — that already worked, so this is a sanity check.
- [x] **T4 — Module skeleton `services/charon`.**
  Create per `AGENTS.md` §4: `build.gradle.kts` (Kotlin 2 + Jib + ktor + protobuf; `org.tatrman.kantheon.charon.*` source root; deps on `:shared:proto` + Arrow + AWS SDK + Lettuce; Jib image `charon:dev`, ports 7250 HTTP + 7251 gRPC per architecture §7), `src/main/kotlin/org/tatrman/kantheon/charon/Application.kt` (entrypoint: Ktor probes + gRPC server bootstrap, ≤45 lines per `EXAMPLES.md` §1b — delegates the heavy wiring to a `Wiring.kt` if it grows), `src/main/resources/application.conf` (HOCON: `charon.http.port=7250`, `charon.grpc.port=7251`, `charon.s3.*`, `charon.redis.*` keys reserved), `src/main/resources/logback.xml` (via `logging-config`), `k8s/{base,overlays/local}/` with `imagePullPolicy: Never` in the local overlay. Add `include(":services:charon")` to `settings.gradle.kts`. The README documents the first-instance `services/` module conventions (Midas's `report-renderer` and any future platform service will follow). **Module compiles; pod starts; RPCs return `UNIMPLEMENTED`.**
- [x] **T5 — Tests first: `MovePlanner` legality matrix + request validation.**
  `src/test/kotlin/.../core/MovePlannerSpec.kt` — table-driven Kotest StringSpec. For each (source_kind, target_kind, rpc_kind) cell in contracts §2, assert: legal pair returns `Right(plan)`, illegal pair returns `Left(CharonError.IllegalPair)` (sealed error type per `AGENTS.md` §9). Same-location no-op is a *third* outcome (no I/O, `MoveResult` with a Rule-6 hint) — assert that explicitly per contracts §2 last row. Plus validation cases: `Materialize` target is *not* a worker session (else `INVALID_ARGUMENT`); `Evict` on a `DbTable` is rejected (DB cleanup is the owner's job); `Stage` target is *only* `WorkerSessionDf`. `MovePlanner` is a **pure** data class — no I/O, no coroutines, no service deps. Implement `MovePlanner` + the `Legality` matrix (the single source of truth that the `contracts.md` table is hand-derived from) to green. **This is the TDD gate for Stage 1.1 — once it's green, the legality contract is provable from the spec.**
- [x] **T6 — `Errors.kt` typed-failure → gRPC status mapping + `messages = 99` plumbing; `CharonServiceImpl` skeleton.**
  Sealed `CharonError` hierarchy (`IllegalPair`, `UnknownConnectionId`, `AllowListViolation`, `MissingDbWriteMode`, `FingerprintMismatch`, `SourceNotFound`, `ByteCapExceeded`, `NotYetImplemented` for the stubs). `CharonError.toStatus(): Status` maps each to the gRPC `Status` per contracts §1 error model (`INVALID_ARGUMENT`/`NOT_FOUND`/`FAILED_PRECONDITION`/`RESOURCE_EXHAUSTED`/`UNAVAILABLE`/`DEADLINE_EXCEEDED`); each carries a Rule-6 `ResponseMessage` payload on the `metadata` key for the wire. `grpc/CharonServiceImpl.kt` — five RPCs delegate to `MovePlanner` (validation) + a `MoveExecutor` seam (the **only** point the real I/O plugs in at Stage 1.2); for now `MoveExecutor` returns `UNIMPLEMENTED` for every operation. A `RequestValidationSpec` proves that *each* of the five RPCs returns the right `INVALID_ARGUMENT` shape for the matched bad input (e.g. `Materialize` with a worker-session target, `Evict` with a `DbTable` location, `Copy` with the same source and target kind on a non-allowed kind pair).
- [x] **T7 — k8s `base/` + `overlays/local/`; lint clean.**
  Pattern source: `services/ariadne/k8s/` (port swap to 7250/7251, image name `charon:dev`); local overlay with `imagePullPolicy: Never` and OTel disabled (mirrors ariadne's local overlay). `kubectl kustomize services/charon/k8s/overlays/local` renders cleanly. `just build-kt charon` produces a Jib tar (the `jibDockerBuild` variant needs `docker` on PATH, which is fine; the deployment pipeline uses the tar/BuildImage variant). `just lint-all` repo-wide — Charon additions must be ktlintFormat-passed. `just test-all` — new specs green, nothing else regresses.
- [x] **T8 — Stage exit checklist.**
  All T1–T7 boxes ticked + verified from `git log` and code inspection (per `AGENTS.md` §12 gotcha: *don't trust `tasks-*.md` checkboxes alone*). Update the per-arc `tasks.md` tracker (when it exists) — Charon's first pass. Tag not yet (Stage 1.1 ships only the skeleton; tag is at Stage 1.3 close: `charon/v0.1.0`).

**DONE (verified at commit `e46aff5` on `feat/charon-p1-s1.1-skeleton`):**
- `services/charon` compiles, ktlintFormat-clean, **102/102 tests pass** (MovePlannerSpec 83 + RequestValidationSpec 7 + ErrorsSpec 12).
- Pod starts (gRPC :7251, HTTP :7250); every RPC returns `UNIMPLEMENTED` for legal requests and the right `INVALID_ARGUMENT` for illegal ones.
- `MovePlanner` legality matrix provable from a single source-of-truth table (`Legality.kt`).
- Proto codegens to Java + Kotlin DSL + gRPC-Kotlin + Python.
- `kubectl kustomize services/charon/k8s/overlays/local` renders cleanly.
- `:services:charon:jibBuildTar` produces a Jib tar; `kubectl apply` path is the next stage's e2e work.
- Full repo `test` + `ktlintCheck` pass — no Stream A regressions.

**Status of the stream-B worktree:** on `stream-b-arcs`, branch `feat/charon-p1-s1.1-skeleton` carries the Stage 1.1 commit. Per your merge policy ("usually at the end of the arc"), `stream-b-arcs` is the integration branch — feature branches land here as each stage closes; merge to `main` happens once the Charon arc (and the rest of Stream B) is ready.

**DONE means:** `services/charon` compiles, ktlintFormat-clean, test suite green; pod starts (gRPC :7251, HTTP :7250); every RPC returns `UNIMPLEMENTED` for legal requests and the right `INVALID_ARGUMENT` for illegal ones; `MovePlanner` legality matrix provable from a single source-of-truth table.
