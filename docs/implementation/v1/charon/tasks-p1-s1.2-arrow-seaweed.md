# Charon — Stage 1.2: ArrowPipe + integrity + Seaweed endpoint

> Branch: `feat/charon-p1-s1.2-arrow-seaweed`. Pre-flight: Stage 1.1 done (commit `e72677a` on the feature branch, merged into `stream-b-arcs`). Plan: [`plan.md`](./plan.md) Stage 1.2.
>
> Goal: `Integrity.kt` (schema fingerprint + row counting) implemented byte-compatibly with `workers/polars` `_schema_fingerprint` (Python reference impl at `ai-platform/workers/polars/src/workers_polars/grpc_service.py:316`); `ArrowPipe.kt` (the single streaming pump over any Source→Target pair — the move core); `SeaweedEndpoint.kt` (S3 get/put with temp-key + rename, multipart over threshold, fault-injection tested). Wire the real `Copy seaweed→seaweed`, `Describe`, `Evict` against Seaweed. Component suite proves no-partial-write under a mid-stream failure.
>
> **The five-RPC legality matrix is unchanged** (Stage 1.1's `Legality.kt` is the source of truth). Stage 1.2 just plugs the real I/O into the seam — the planner still rejects the bad inputs before the executor runs. Stage 1.2 also adds the **shared fingerprint fixture** the contract §6 / Stage 1.2 T1 calls for: a tiny reference dataset whose `MoveResult.schema_fingerprint` is pinned and asserted to match the value the `workers/polars` Python impl produces for the same schema (the test runs *both* algorithms against a Kotlin-built `Schema` and asserts hex equality — this is the byte-compat gate that keeps the cross-engine move pipe honest).
>
> **Status (2026-06-14, after review-006):**
> - **T1 — INTEGRITY / CROSS-CHECK:** **RESOLVED (review-006 R3, 2026-06-15).** The original self-derived IPC-byte digest (H2) is gone. The fingerprint is now a **canonical, implementation-independent logical-schema digest** (`core/Integrity.kt`), proven equal to a byte-identical Python reference (`fixtures/integrity/regenerate.py`) in `IntegritySpec`. Raw IPC bytes were shown to be not cross-impl stable; see `contracts.md` §6.
> - **T2 — IMPLEMENTATION:** shipped (ArrowPipe + Integrity). Note: the streaming-pump claim in §10 is **not** fully implemented (see review-006 H1); the per-batch buffering (O(total bytes) memory) is fixed in Stage 1.4 (multipart upload) — Stage 1.2's single-shot PUT is enough for the unit suite but not for production-scale datasets.
> - **T3 — SeaweedEndpointSpec (Testcontainers S3):** **NOT CREATED.** Per Bora's 2026-06-14 call, the live S3 round-trip + fault injection landed as mocked unit tests against the `S3Client` interface (`CharonMoveExecutorSpec` 10 cases). The deferred integration pass (Stage 1.4) restores the Testcontainers S3 spec on real local K3s infra.
> - **T4 — SeaweedEndpoint IMPLEMENTATION:** shipped, with the caveat that the **multipart upload** mentioned in T4's prose and §10 of the architecture doc is **NOT** implemented at Stage 1.2; the deferred Stage 1.4 pass adds `CreateMultipartUpload` / `UploadPart` / `CompleteMultipartUpload` / `AbortMultipartUpload` and the bounded-memory guarantee.
> - **T5 — WIRE COPY/DESCRIBE/EVICT + METRICS:** shipped.
> - **T6 — SeaweedRoundTripSpec (Testcontainers):** **NOT CREATED.** Same deferral as T3 — folded into the mocked `CharonMoveExecutorSpec` and the Stage 1.4 integration pass.
> - **T7 — ktlint + full-repo test pass + commit:** shipped.

- [x] **T1 — Tests first: `Integrity` fingerprint + cross-check fixtures.**
  `src/test/kotlin/.../core/IntegritySpec.kt` — Kotest StringSpec. (a) Fingerprint over a 3-column schema (`Utf8`, `Int64`, `Float64`) is a stable 64-char hex; calling it twice on the same schema returns the same value; two different schemas produce different values. (b) **Cross-check fixture** — for a known Arrow schema, compute the fingerprint the *Kotlin* way (`ArrowStreamWriter` schema-only) AND the *Python* way (equivalent algorithm) — the Kotlin impl ships a **port of the Python `_schema_fingerprint` algorithm** in `core/Integrity.kt` (same bytes, same SHA-256, lowercase hex), and the spec pins the expected hex digest so any future Arrow version drift that mutates the canonical schema bytes fails the build. **(b) is currently a self-derivation, see "Status" above — Stage 1.4 is the proper home for the Python-generated digest; the algorithm is correct, the assertion is currently tautological.)** (c) Row counting: a stream of N batches summing to R rows reports `R`, an empty stream reports 0. (d) Chunk-boundary cases — a 1-row batch and a 65537-row batch both round-trip cleanly (charon/contracts.md §6 default `chunk_rows=65536`). *Done = green.*
- [x] **T2 — `Integrity.kt` + `ArrowPipe.kt` implementation.** *(Multi-batch round-trip is **fixed** in the `fix/charon-p1-review-006` pass — R1: each batch is materialised into owned data via `TransferPair` and serialised as ONE coherent IPC stream; gate test `MultiBatchMoveSpec`. The remaining caveat is **memory**, not correctness: the P1 path is a single-shot PUT that buffers O(total bytes); bounded-memory streaming/multipart is the Stage 1.4 hardening, see H1.)*
- [ ] **T3 — Tests first: `SeaweedEndpoint` against Testcontainers S3.** **DEFERRED to [`tasks-p1-s1.4-integration.md`](./tasks-p1-s1.4-integration.md) §"Live K3s Seaweed round-trip + fault injection".** Bora 2026-06-14: integration-test split (per `AGENTS.md` §1.4 and the E2E rule). The Stage 1.2 unit suite asserts the move-pipe contract (atomicity, fingerprint, byte cap, error surfacing, metric labels) against a mocked `S3Client`; the live SeaweedFS round-trip + multipart + fault injection land against the real local K3s Seaweed in Stage 1.4.
- [x] **T4 — `SeaweedEndpoint` implementation.** *(Without multipart upload; see H1 status. Atomicity strategy is temp-key + copy + delete (portable across S3 dialects; the copy+delete pattern works against MinIO in Testcontainers and against the real SeaweedFS in K3s). The 8 MB multipart threshold mentioned in T4's prose is **not** wired; the SDK's default PUT path is used. Multipart is added in Stage 1.4.)*
- [x] **T5 — Wire `Copy seaweed→seaweed` + `Describe`/`Evict` for seaweed; metrics counters.**
- [ ] **T6 — Component round-trip + no-partial-write fault-injection spec.** **DEFERRED to [`tasks-p1-s1.4-integration.md`](./tasks-p1-s1.4-integration.md).** Same rationale as T3: integration-test split.
- [x] **T7 — ktlint + full-repo test pass; commit on `feat/charon-p1-s1.2-arrow-seaweed`.**

**DONE means (Stage 1.2 standing close — with the T3/T6/streaming caveats above):** `services/charon` compiles, ktlintFormat-clean, *all tests* green (Stage 1.1's 102 + Stage 1.2's 19 new = 121 Charon tests); the `/metrics` endpoint serves the new counters; the k8s kustomize renders unchanged.

**RE-OPENED for review-006:** review-006 found that
- B1: the multi-batch move silently produced an EMPTY object (the buffered `pendingBatches` all referenced the reader's reused root, which has 0 rows by the time `commit()` runs). The unit suite was single-batch-only, so it slipped through. **Fixed** in `fix/charon-p1-review-006` (R1: per-batch `TransferPair` into owned roots, serialised as ONE coherent stream). NOTE: the first review-006 fix attempt used a per-batch *multipart* upload that **re-introduced** the bug (each part was a self-contained `schema+batch+EOS` stream, so the concatenation read back empty); the re-review reverted it to a single-shot coherent-stream PUT and added a real readback gate (`MultiBatchMoveSpec` "writes one readable object").
- H1: the architecture §10 "streaming pump, no materialisation" claim is not implemented at Stage 1.2's single-shot PUT — bounded-memory streaming is deferred to Stage 1.4 (correctness is fine; only memory is O(total bytes)).
- H2: the cross-check fixture is a tautology (Kotlin output compared to itself); the Python check is deferred to Stage 1.4.
- M1: Redis `max-value-bytes` cap is not enforced (fixed in review-006 R4).
- M2: Rule-6 `ResponseMessage`(s) on error trailers are not attached (fixed in review-006 R5).
- M3: `onCompleted()` is called after `onError()` in gRPC service impl (fixed in review-006 R6).
- M4: this task list not reconciled with what shipped — **the "Status" block at the top of this file is the reconciliation**.

The `charon/v0.1.0` tag is **NOT** re-applied until review-006 R1–R6 are closed in [`tasks-review-006.md`](../../../tasks-review-006.md) and Stage 1.4 is at least stubbed.
